collectorVersion: v0.157.0
distribution: core
generatedAt: "2026-08-02T13:15:54Z"
sources:
  contrib: github.com/open-telemetry/opentelemetry-collector-contrib
  core: go.opentelemetry.io/collector
components:
  connector:
    forward:
      type: forward
      stability:
        logs_to_logs: beta
        metrics_to_metrics: beta
        profiles_to_profiles: alpha
        traces_to_traces: beta
      pairs:
        - from: logs
          to: logs
        - from: metrics
          to: metrics
        - from: profiles
          to: profiles
        - from: traces
          to: traces
      module: go.opentelemetry.io/collector/connector/forwardconnector
  exporter:
    debug:
      type: debug
      signals:
        - traces
        - metrics
        - logs
        - profiles
      stability:
        logs: alpha
        metrics: alpha
        profiles: alpha
        traces: alpha
      module: go.opentelemetry.io/collector/exporter/debugexporter
      fields:
        type: map
        children:
          sampling_initial:
            type: int
            doc: messages logged per second before sampling kicks in
          sampling_thereafter:
            type: int
            doc: log one in every N messages after the initial burst
          use_internal_logger:
            type: bool
          verbosity:
            type: string
            enum:
              - basic
              - normal
              - detailed
            doc: how much of each payload is written to the collector log
    file:
      type: file
      signals:
        - traces
        - metrics
        - logs
        - profiles
      stability:
        logs: alpha
        metrics: alpha
        profiles: development
        traces: alpha
      module: github.com/open-telemetry/opentelemetry-collector-contrib/exporter/fileexporter
      fields:
        type: map
        children:
          append:
            type: bool
            doc: 'Mode defines whether the exporter should append to the file. Options: - false[default]:  truncates the file - true:  appends to the file.'
          compression:
            type: string
            doc: Compression Codec used to export telemetry data Supported compression algorithms:`zstd`
          compression_params:
            type: map
            children:
              level:
                type: int
            doc: 'CompressionParams defines compression parameters. For zstd the following levels are supported: - SpeedFastest: 1 - SpeedDefault: 3 - SpeedBetterCompression: 6 - SpeedBestCompression: 11'
          create_directory:
            type: bool
            doc: CreateDirectory specifies that the parent directory of the output file should be created automatically on start.
          directory_permissions:
            type: string
            doc: DirectoryPermissions specifies permissions used when creating directories (minus process umask). Value must be an octal string like "0755".
          encoding:
            type: map
            open: true
            doc: Encoding defines the encoding of the telemetry data. If specified, it overrides `FormatType` and applies an encoding extension.
          flush_interval:
            type: duration
            doc: FlushInterval is the duration between flushes. See time.ParseDuration for valid values.
          format:
            type: string
            doc: 'FormatType define the data format of encoded telemetry data Options: - json[default]:  OTLP json bytes. - proto:  OTLP binary protobuf bytes.'
          group_by:
            type: map
            children:
              enabled:
                type: bool
                doc: Enables group_by. Default is false.
              max_open_files:
                type: int
                doc: MaxOpenFiles specifies the maximum number of open file descriptors for the output files. The default is 100.
              resource_attribute:
                type: string
                doc: ResourceAttribute specifies the name of the resource attribute that contains the path segment of the file to write to. The final path will be the Path config value, with the * replaced with the value of this resource attribute. Default is "fileexporter.path_segment".
            doc: GroupBy enables writing to separate files based on a resource attribute.
          path:
            type: string
            doc: Path of the file to write to. Path is relative to current directory.
          rotation:
            type: map
            children:
              localtime:
                type: bool
                doc: LocalTime determines if the time used for formatting the timestamps in backup files is the computer's local time.  The default is to use UTC time.
              max_backups:
                type: int
                doc: MaxBackups is the maximum number of old log files to retain. The default is to 100 files.
              max_days:
                type: int
                doc: MaxDays is the maximum number of days to retain old log files based on the timestamp encoded in their filename.  Note that a day is defined as 24 hours and may not exactly correspond to calendar days due to daylight savings, leap seconds, etc. The default is not to remove old log files based on age.
              max_megabytes:
                type: int
                doc: MaxMegabytes is the maximum size in megabytes of the file before it gets rotated. It defaults to 100 megabytes.
            doc: Rotation defines an option about rotation of telemetry files.
        doc: Config defines configuration for file exporter.
    kafka:
      type: kafka
      signals:
        - traces
        - metrics
        - logs
        - profiles
      stability:
        logs: beta
        metrics: beta
        profiles: development
        traces: beta
      module: github.com/open-telemetry/opentelemetry-collector-contrib/exporter/kafkaexporter
      fields:
        type: map
        children:
          auth:
            type: map
            children:
              kerberos:
                type: map
                children:
                  config_file:
                    type: string
                  disable_fast_negotiation:
                    type: bool
                  keytab_file:
                    type: string
                  password:
                    type: string
                  realm:
                    type: string
                  service_name:
                    type: string
                  use_keytab:
                    type: bool
                  username:
                    type: string
                doc: Kerberos holds Kerberos authentication configuration.
              plain_text:
                type: map
                children:
                  password:
                    type: string
                  username:
                    type: string
                doc: 'PlainText is an alias for SASL/PLAIN authentication. Deprecated [v0.123.0]: use SASL with Mechanism set to PLAIN instead.'
              sasl:
                type: map
                children:
                  aws_msk:
                    type: map
                    children:
                      region:
                        type: string
                        doc: Region is the AWS region the MSK cluster is based in
                    doc: AWSMSK holds configuration specific to AWS MSK.
                  mechanism:
                    type: string
                    doc: 'SASL Mechanism to be used, possible values are: (PLAIN, AWS_MSK_IAM_OAUTHBEARER, OAUTHBEARER, SCRAM-SHA-256 or SCRAM-SHA-512).'
                  oauthbearer_token_source:
                    type: map
                    open: true
                    doc: ID of type "extension" providing a TokenSource for OAUTHBEARER Mechanism, typically named "oauth2client"
                  password:
                    type: string
                    doc: Password to be used on authentication
                  username:
                    type: string
                    doc: Username to be used on authentication
                  version:
                    type: int
                    doc: 'Version is ignored, and exists for backwards compatibility in config parsing. Deprecated [v0.153.0]: this field is a no-op since the migration to franz-go, which negotiates the SASL handshake version automatically. This config will be removed in a future release.'
                doc: SASL holds SASL authentication configuration.
              tls:
                type: map
                children:
                  ca_file:
                    type: string
                    doc: Path to the CA cert. For a client this verifies the server certificate. For a server this verifies client certificates. If empty uses system root CA. (optional)
                  ca_pem:
                    type: string
                    doc: In memory PEM encoded cert. (optional)
                  cert_file:
                    type: string
                    doc: Path to the TLS cert to use for TLS required connections. (optional)
                  cert_pem:
                    type: string
                    doc: In memory PEM encoded TLS cert to use for TLS required connections. (optional)
                  cipher_suites:
                    type: list
                    children:
                      item:
                        type: string
                    doc: CipherSuites is a list of TLS cipher suites that the TLS transport can use. If left blank, a safe default list is used. See https://go.dev/src/crypto/tls/cipher_suites.go for a list of supported cipher suites.
                  curve_preferences:
                    type: list
                    children:
                      item:
                        type: string
                    doc: contains the elliptic curves that will be used in an ECDHE handshake, in preference order Defaults to empty list and "crypto/tls" defaults are used, internally.
                  include_insecure_cipher_suites:
                    type: bool
                  include_system_ca_certs_pool:
                    type: bool
                    doc: If true, load system CA certificates pool in addition to the certificates configured in this struct.
                  insecure:
                    type: bool
                    doc: In gRPC and HTTP when set to true, this is used to disable the client transport security. See https://godoc.org/google.golang.org/grpc#WithInsecure for gRPC. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional, default false)
                  insecure_skip_verify:
                    type: bool
                    doc: InsecureSkipVerify will enable TLS but not verify the certificate.
                  key_file:
                    type: string
                    doc: Path to the TLS key to use for TLS required connections. (optional)
                  key_pem:
                    type: string
                    doc: In memory PEM encoded TLS key to use for TLS required connections. (optional)
                  max_version:
                    type: string
                    doc: MaxVersion sets the maximum TLS version that is acceptable. If not set, refer to crypto/tls for defaults. (optional)
                  min_version:
                    type: string
                    doc: MinVersion sets the minimum TLS version that is acceptable. If not set, TLS 1.2 will be used. (optional)
                  reload_interval:
                    type: duration
                    doc: ReloadInterval specifies the duration after which the certificate will be reloaded If not set, it will never be reloaded (optional)
                  server_name_override:
                    type: string
                    doc: ServerName requested by client for virtual hosting. This sets the ServerName in the TLSConfig. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional)
                  tpm:
                    type: map
                    children:
                      auth:
                        type: string
                      enabled:
                        type: bool
                      owner_auth:
                        type: string
                      path:
                        type: string
                        doc: The path to the TPM device or Unix domain socket. For instance /dev/tpm0 or /dev/tpmrm0.
                    doc: Trusted platform module configuration
                doc: 'TLS holds TLS configuration for connecting to Kafka brokers. Deprecated [v0.124.0]: use ClientConfig.TLS instead. This will be used only if ClientConfig.TLS is not set.'
            doc: Authentication holds Kafka authentication details.
          brokers:
            type: list
            children:
              item:
                type: string
            doc: Brokers holds the list of Kafka bootstrap servers (default localhost:9092).
          client_id:
            type: string
            doc: ClientID holds the client ID advertised to Kafka, which can be used for enforcing ACLs, throttling quotas, and more (default "otel-collector")
          conn_idle_timeout:
            type: duration
            doc: ConnIdleTimeout specifies the time after which idle connections are not reused and may be closed.
          enabled:
            type: bool
            doc: Enabled indicates whether to not retry sending batches in case of export failure.
          include_metadata_keys:
            type: list
            children:
              item:
                type: string
            doc: IncludeMetadataKeys indicates the receiver's client metadata keys to propagate as Kafka message headers.
          initial_interval:
            type: duration
            doc: InitialInterval the time to wait after the first failure before retrying.
          logs:
            type: map
            children:
              encoding:
                type: string
                doc: Encoding holds the encoding of messages for the signal type. Defaults to "otlp_proto".
              message_key_from_metadata_key:
                type: string
                doc: MessageKeyFromMetadataKey holds the name of the metadata key whose value will be used as the Kafka record key for this signal type. If the metadata key is absent or empty the record key is left nil. Mutually exclusive with the partition_* flags for the same signal.
              topic:
                type: string
                doc: 'Topic holds the name of the Kafka topic to which messages of the signal type should be produced. The default depends on the signal type: - "otlp_spans" for traces - "otlp_metrics" for metrics - "otlp_logs" for logs - "otlp_profiles" for profiles'
              topic_from_metadata_key:
                type: string
                doc: TopicFromMetadataKey holds the name of the metadata key to use as the topic name for this signal type. If this is set, it takes precedence over the topic name set in the topic field.
            doc: Logs holds configuration about how logs should be sent to Kafka.
          max_elapsed_time:
            type: duration
            doc: MaxElapsedTime is the maximum amount of time (including retries) spent trying to send a request/batch. Once this value is reached, the data is discarded. If set to 0, the retries are never stopped.
          max_interval:
            type: duration
            doc: MaxInterval is the upper bound on backoff interval. Once this value is reached the delay between consecutive retries will always be `MaxInterval`.
          metadata:
            type: map
            children:
              full:
                type: bool
                doc: Whether to maintain a full set of metadata for all topics, or just the minimal set that has been necessary so far. The full set is simpler and usually more convenient, but can take up a substantial amount of memory if you have many topics and partitions. Defaults to true.
              refresh_interval:
                type: duration
                doc: RefreshInterval controls the frequency at which cluster metadata is refreshed. Defaults to 10 minutes.
              retry:
                type: map
                children:
                  backoff:
                    type: duration
                    doc: How long to wait for leader election to occur before retrying (default 250ms). Similar to the JVM's `retry.backoff.ms`.
                  max:
                    type: int
                    doc: The total number of times to retry a metadata request when the cluster is in the middle of a leader election or at startup (default 3).
                doc: Retry configuration for metadata. This configuration is useful to avoid race conditions when broker is starting at the same time as collector.
            doc: Metadata holds metadata-related configuration for producers and consumers.
          metrics:
            type: map
            children:
              encoding:
                type: string
                doc: Encoding holds the encoding of messages for the signal type. Defaults to "otlp_proto".
              message_key_from_metadata_key:
                type: string
                doc: MessageKeyFromMetadataKey holds the name of the metadata key whose value will be used as the Kafka record key for this signal type. If the metadata key is absent or empty the record key is left nil. Mutually exclusive with the partition_* flags for the same signal.
              topic:
                type: string
                doc: 'Topic holds the name of the Kafka topic to which messages of the signal type should be produced. The default depends on the signal type: - "otlp_spans" for traces - "otlp_metrics" for metrics - "otlp_logs" for logs - "otlp_profiles" for profiles'
              topic_from_metadata_key:
                type: string
                doc: TopicFromMetadataKey holds the name of the metadata key to use as the topic name for this signal type. If this is set, it takes precedence over the topic name set in the topic field.
            doc: Metrics holds configuration about how metrics should be sent to Kafka.
          multiplier:
            type: float
            doc: Multiplier is the value multiplied by the backoff interval bounds
          partition_logs_by_resource_attributes:
            type: bool
            doc: PartitionLogsByResourceAttributes controls the partitioning of logs messages by resource. If this is true, then the message key will be set to a hash of the resource's identifying attributes.
          partition_logs_by_trace_id:
            type: bool
            doc: PartitionLogsByTraceID controls partitioning of log messages by trace ID only. When enabled, the exporter splits incoming logs per TraceID (using SplitLogs) and sets the Kafka message key to the 16-byte hex string of that TraceID. If a LogRecord has an empty TraceID, the key may be empty and partition selection falls back to the Kafka client’s default strategy. Resource attributes are not used for the key when this option is enabled.
          partition_metrics_by_resource_attributes:
            type: bool
            doc: PartitionMetricsByResourceAttributes controls the partitioning of metrics messages by resource. If this is true, then the message key will be set to a hash of the resource's identifying attributes.
          partition_traces_by_id:
            type: bool
            doc: 'PartitionTracesByID sets the message key of outgoing trace messages to the trace ID. NOTE: this does not have any effect for Jaeger encodings. Jaeger encodings always use use the trace ID for the message key.'
          producer:
            type: map
            children:
              allow_auto_topic_creation:
                type: bool
                doc: Whether or not to allow automatic topic creation. (default enabled).
              compression:
                type: string
                doc: 'Compression Codec used to produce messages https://pkg.go.dev/github.com/twmb/franz-go/pkg/kgo#CompressionCodec The options are: ''none'' (default), ''gzip'', ''snappy'', ''lz4'', and ''zstd'''
              compression_params:
                type: map
                children:
                  level:
                    type: int
                doc: CompressionParams defines compression parameters for the producer.
              flush_max_messages:
                type: int
                doc: The maximum number of messages the producer will send in a single broker request. Defaults to 10000 (franz-go default). Similar to `queue.buffering.max.messages` in the JVM producer.
              linger:
                type: duration
                doc: Linger controls the linger time for the producer. (default 10ms).
              max_broker_write_bytes:
                type: int
                doc: MaxBrokerWriteBytes is the maximum bytes the producer will write to a broker in a single request. It must be >= MaxMessageBytes. Maps to franz-go's kgo.BrokerMaxWriteBytes, whose default (and minimum accepted value) is 100 MiB. (default 104857600)
              max_message_bytes:
                type: int
                doc: MaxMessageBytes is the maximum message bytes the producer will accept to produce. It must be less than or equal to MaxBrokerWriteBytes, and must fit in an int32 as it maps to franz-go's kgo.ProducerBatchMaxBytes. (default 1000000)
              required_acks:
                type: int
                doc: 'RequiredAcks holds the number acknowledgements required before producing returns successfully. See: https://docs.confluent.io/platform/current/installation/configuration/producer-configs.html#acks Acceptable values are: 0 (NoResponse)   Does not wait for any acknowledgements. 1 (WaitForLocal) Waits for only the leader to write the record to its local log, but does not wait for followers to acknowledge. (default) -1 (WaitForAll)   Waits for all in-sync replicas to acknowledge. In YAML configuration, "all" is accepted as an alias for -1.'
          profiles:
            type: map
            children:
              encoding:
                type: string
                doc: Encoding holds the encoding of messages for the signal type. Defaults to "otlp_proto".
              message_key_from_metadata_key:
                type: string
                doc: MessageKeyFromMetadataKey holds the name of the metadata key whose value will be used as the Kafka record key for this signal type. If the metadata key is absent or empty the record key is left nil. Mutually exclusive with the partition_* flags for the same signal.
              topic:
                type: string
                doc: 'Topic holds the name of the Kafka topic to which messages of the signal type should be produced. The default depends on the signal type: - "otlp_spans" for traces - "otlp_metrics" for metrics - "otlp_logs" for logs - "otlp_profiles" for profiles'
              topic_from_metadata_key:
                type: string
                doc: TopicFromMetadataKey holds the name of the metadata key to use as the topic name for this signal type. If this is set, it takes precedence over the topic name set in the topic field.
            doc: Profiles holds configuration about how profiles should be sent to Kafka.
          protocol_version:
            type: string
            doc: ProtocolVersion defines the Kafka protocol version that the client will assume it is running against.
          rack_id:
            type: string
            doc: RackID provides the rack identifier for this client to enable rack-aware replica selection when supported by the brokers. This maps to Kafka's standard "client.rack" setting. By default, this is empty.
          randomization_factor:
            type: float
            doc: RandomizationFactor is a random factor used to calculate next backoffs Randomized interval = RetryInterval * (1 ± RandomizationFactor)
          record_headers:
            type: list
            children:
              item:
                type: map
                children:
                  name:
                    type: string
                  value:
                    type: string
            doc: RecordHeaders sets static headers on every outgoing Kafka record.
          record_partitioner:
            type: map
            children:
              extension:
                type: map
                open: true
                doc: Extension is the component ID of an extension implementing RecordPartitionerExtension. Setting this field delegates partition assignment to that extension.
              least_backup:
                open: true
                doc: LeastBackup routes each record to the partition with the fewest buffered records.
              round_robin:
                open: true
                doc: RoundRobin distributes records evenly across all available partitions in round-robin order.
              sticky_key:
                type: map
                children:
                  hasher:
                    type: string
                    doc: 'Hasher is the hash algorithm used for key-based partition assignment. Valid values: "sarama_compat" (default). - "sarama_compat": Sarama-compatible FNV-1a hashing (SaramaCompatHasher). - "murmur2": Murmur2 hashing.'
                doc: StickyKey uses StickyKeyPartitioner. When a record key is set, the partition is derived from the key hash.
            doc: RecordPartitioner configures how Kafka records are assigned to partitions. The default ("sarama_compatible") retains the legacy Sarama-compatible hashing behavior. Set to "sticky", "round_robin", or "least_backup" to use one of the built-in franz-go partitioners, or "extension" to delegate to a custom extension.
          resolve_canonical_bootstrap_servers_only:
            type: bool
            doc: 'ResolveCanonicalBootstrapServersOnly is ignored, and exists for backwards compatibility in config parsing. Deprecated [v0.153.0]: this field is a no-op since the migration to franz-go, which has no direct equivalent to the associated Sarama config. This config will be removed in a future release.'
          retry_on_failure:
            type: map
            children:
              enabled:
                type: bool
              initial_interval:
                type: duration
              max_elapsed_time:
                type: duration
              max_interval:
                type: duration
              multiplier:
                type: float
              randomization_factor:
                type: float
          sending_queue:
            type: map
            children:
              batch:
                type: map
                children:
                  flush_timeout:
                    type: duration
                    doc: FlushTimeout sets the time after which a batch will be sent regardless of its size.
                  max_size:
                    type: int
                    doc: MaxSize defines the configuration for the maximum size of a batch.
                  min_size:
                    type: int
                    doc: MinSize defines the configuration for the minimum size of a batch.
                  partition:
                    type: map
                    children:
                      metadata_keys:
                        type: list
                        children:
                          item:
                            type: string
                        doc: MetadataKeys is a list of client.Metadata keys that will be used to partition the data into batches. If this setting is empty, a single batcher instance will be used. When this setting is not empty, one batcher will be used per distinct combination of values for the listed metadata keys. Empty value and unset metadata are treated as distinct cases. Entries are case-insensitive. Duplicated entries will trigger a validation error.
                    doc: Partition defines the partitioning of the batches configuration.
                  sizer:
                    type: map
                    open: true
                    doc: Sizer determines the type of size measurement used by the batch. If not configured, use the same configuration as the queue. It accepts "requests", "items", or "bytes".
                doc: BatchConfig it configures how the requests are consumed from the queue and batch together during consumption.
              block_on_overflow:
                type: bool
                doc: BlockOnOverflow determines the behavior when the component's TotalSize limit is reached. If true, the component will wait for space; otherwise, operations will immediately return a retryable error.
              enabled:
                type: bool
                doc: Enabled indicates whether to not enqueue and batch before exporting.
              num_consumers:
                type: int
                doc: NumConsumers is the maximum number of concurrent consumers from the queue. This applies across all different optional configurations from above (e.g. wait_for_result, block_on_overflow, storage, etc.).
              queue_size:
                type: int
                doc: QueueSize represents the maximum data size allowed for concurrent storage and processing.
              sizer:
                type: map
                open: true
                doc: Sizer determines the type of size measurement used by this component. It accepts "requests", "items", or "bytes".
              storage:
                type: map
                open: true
                doc: 'StorageID if not empty, enables the persistent storage and uses the component specified as a storage extension for the persistent queue. TODO: This will be changed to Optional when available. See https://github.com/open-telemetry/opentelemetry-collector/issues/13822'
              wait_for_result:
                type: bool
                doc: WaitForResult determines if incoming requests are blocked until the request is processed or not. Currently, this option is not available when persistent queue is configured using the storage configuration.
            doc: QueueBatchConfig defines configuration for queueing and batching for the exporter.
          timeout:
            type: duration
            doc: Timeout is the timeout for every attempt to send data to the backend. A zero timeout means no timeout.
          tls:
            type: map
            children:
              ca_file:
                type: string
                doc: Path to the CA cert. For a client this verifies the server certificate. For a server this verifies client certificates. If empty uses system root CA. (optional)
              ca_pem:
                type: string
                doc: In memory PEM encoded cert. (optional)
              cert_file:
                type: string
                doc: Path to the TLS cert to use for TLS required connections. (optional)
              cert_pem:
                type: string
                doc: In memory PEM encoded TLS cert to use for TLS required connections. (optional)
              cipher_suites:
                type: list
                children:
                  item:
                    type: string
                doc: CipherSuites is a list of TLS cipher suites that the TLS transport can use. If left blank, a safe default list is used. See https://go.dev/src/crypto/tls/cipher_suites.go for a list of supported cipher suites.
              curve_preferences:
                type: list
                children:
                  item:
                    type: string
                doc: contains the elliptic curves that will be used in an ECDHE handshake, in preference order Defaults to empty list and "crypto/tls" defaults are used, internally.
              include_insecure_cipher_suites:
                type: bool
              include_system_ca_certs_pool:
                type: bool
                doc: If true, load system CA certificates pool in addition to the certificates configured in this struct.
              insecure:
                type: bool
                doc: In gRPC and HTTP when set to true, this is used to disable the client transport security. See https://godoc.org/google.golang.org/grpc#WithInsecure for gRPC. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional, default false)
              insecure_skip_verify:
                type: bool
                doc: InsecureSkipVerify will enable TLS but not verify the certificate.
              key_file:
                type: string
                doc: Path to the TLS key to use for TLS required connections. (optional)
              key_pem:
                type: string
                doc: In memory PEM encoded TLS key to use for TLS required connections. (optional)
              max_version:
                type: string
                doc: MaxVersion sets the maximum TLS version that is acceptable. If not set, refer to crypto/tls for defaults. (optional)
              min_version:
                type: string
                doc: MinVersion sets the minimum TLS version that is acceptable. If not set, TLS 1.2 will be used. (optional)
              reload_interval:
                type: duration
                doc: ReloadInterval specifies the duration after which the certificate will be reloaded If not set, it will never be reloaded (optional)
              server_name_override:
                type: string
                doc: ServerName requested by client for virtual hosting. This sets the ServerName in the TLSConfig. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional)
              tpm:
                type: map
                children:
                  auth:
                    type: string
                  enabled:
                    type: bool
                  owner_auth:
                    type: string
                  path:
                    type: string
                    doc: The path to the TPM device or Unix domain socket. For instance /dev/tpm0 or /dev/tpmrm0.
                doc: Trusted platform module configuration
            doc: TLS holds TLS-related configuration for connecting to Kafka brokers. By default the client will use an insecure connection unless SASL/AWS_MSK_IAM_OAUTHBEARER auth is configured.
          topic_from_attribute:
            type: string
            doc: TopicFromAttribute is the name of the attribute to use as the topic name.
          traces:
            type: map
            children:
              encoding:
                type: string
                doc: Encoding holds the encoding of messages for the signal type. Defaults to "otlp_proto".
              message_key_from_metadata_key:
                type: string
                doc: MessageKeyFromMetadataKey holds the name of the metadata key whose value will be used as the Kafka record key for this signal type. If the metadata key is absent or empty the record key is left nil. Mutually exclusive with the partition_* flags for the same signal.
              topic:
                type: string
                doc: 'Topic holds the name of the Kafka topic to which messages of the signal type should be produced. The default depends on the signal type: - "otlp_spans" for traces - "otlp_metrics" for metrics - "otlp_logs" for logs - "otlp_profiles" for profiles'
              topic_from_metadata_key:
                type: string
                doc: TopicFromMetadataKey holds the name of the metadata key to use as the topic name for this signal type. If this is set, it takes precedence over the topic name set in the topic field.
            doc: Traces holds configuration about how traces should be sent to Kafka.
          use_leader_epoch:
            type: bool
            doc: 'When enabled, the consumer uses the leader epoch returned by brokers (KIP-320) to detect log truncation. Setting this to false clears the leader epoch from fetch offsets, disabling KIP-320. Disabling can improve compatibility with brokers that don’t fully support leader epochs (e.g., Azure Event Hubs), at the cost of losing automatic log-truncation safety. NOTE: this is experimental and may be removed in a future release.'
        doc: Config defines configuration for Kafka exporter.
    nop:
      type: nop
      signals:
        - traces
        - metrics
        - logs
        - profiles
      stability:
        logs: beta
        metrics: beta
        profiles: alpha
        traces: beta
      module: go.opentelemetry.io/collector/exporter/nopexporter
    otlp:
      type: otlp
      signals:
        - traces
        - metrics
        - logs
        - profiles
      stability:
        logs: stable
        metrics: stable
        profiles: alpha
        traces: stable
      module: go.opentelemetry.io/collector/exporter/otlpexporter
      deprecated: renamed to "otlp_grpc" upstream; the old name still resolves for now
      aliasOf: otlp_grpc
      fields:
        type: map
        children:
          auth:
            type: map
            children:
              authenticator:
                type: string
          authority:
            type: string
          balancer_name:
            type: string
            enum:
              - round_robin
              - pick_first
          compression:
            type: string
            enum:
              - gzip
              - snappy
              - zstd
              - zlib
              - deflate
              - lz4
              - none
              - ""
          endpoint:
            type: string
            doc: host:port of the OTLP gRPC endpoint
          headers:
            type: map
            open: true
            doc: extra gRPC metadata sent with every request
          keepalive:
            type: map
            children:
              permit_without_stream:
                type: bool
              time:
                type: duration
              timeout:
                type: duration
          read_buffer_size:
            type: int
          retry_on_failure:
            type: map
            children:
              enabled:
                type: bool
              initial_interval:
                type: duration
              max_elapsed_time:
                type: duration
              max_interval:
                type: duration
              multiplier:
                type: float
              randomization_factor:
                type: float
          sending_queue:
            type: map
            children:
              blocking:
                type: bool
              enabled:
                type: bool
              num_consumers:
                type: int
              queue_size:
                type: int
              sizer:
                type: string
                enum:
                  - requests
                  - items
                  - bytes
              storage:
                type: string
                doc: id of a storage extension for a persistent queue
              wait_for_result:
                type: bool
            open: true
          timeout:
            type: duration
          tls:
            type: map
            children:
              ca_file:
                type: string
              ca_pem:
                type: string
              cert_file:
                type: string
              cert_pem:
                type: string
              cipher_suites:
                type: list
                children:
                  item:
                    type: string
              include_system_ca_certs_pool:
                type: bool
              insecure:
                type: bool
                doc: send in plaintext; only for local testing
              insecure_skip_verify:
                type: bool
                doc: accept any server certificate
              key_file:
                type: string
              key_pem:
                type: string
              max_version:
                type: string
              min_version:
                type: string
              reload_interval:
                type: duration
              server_name_override:
                type: string
          wait_for_ready:
            type: bool
          write_buffer_size:
            type: int
        required:
          - endpoint
    otlp_grpc:
      type: otlp_grpc
      signals:
        - traces
        - metrics
        - logs
        - profiles
      stability:
        logs: stable
        metrics: stable
        profiles: alpha
        traces: stable
      module: go.opentelemetry.io/collector/exporter/otlpexporter
      alias: otlp
      fields:
        type: map
        children:
          auth:
            type: map
            children:
              authenticator:
                type: map
                open: true
                doc: AuthenticatorID specifies the name of the extension to use in order to authenticate the incoming data point.
            doc: Auth configuration for outgoing RPCs.
          authority:
            type: string
            doc: WithAuthority parameter configures client to rewrite ":authority" header (godoc.org/google.golang.org/grpc#WithAuthority)
          balancer_name:
            type: string
            doc: Sets the balancer in grpclb_policy to discover the servers. Default is pick_first. https://github.com/grpc/grpc-go/blob/master/examples/features/load_balancing/README.md
          compression:
            type: string
            doc: The compression key for supported compression types within collector.
          endpoint:
            type: string
            doc: The target to which the exporter is going to send traces or metrics, using the gRPC protocol. The valid syntax is described at https://github.com/grpc/grpc/blob/master/doc/naming.md.
          headers:
            children:
              item:
                type: map
                children:
                  name:
                    type: string
                  value:
                    type: string
                    doc: String alias that is marshaled and printed in an opaque way. To recover the original value, cast it to a string.
                doc: Pair is an element of a MapList, and consists of a name and an opaque value.
            doc: The headers associated with gRPC requests.
          keepalive:
            type: map
            children:
              permit_without_stream:
                type: bool
              time:
                type: duration
              timeout:
                type: duration
            doc: The keepalive parameters for gRPC client. See grpc.WithKeepaliveParams. (https://godoc.org/google.golang.org/grpc#WithKeepaliveParams).
          middlewares:
            type: list
            children:
              item:
                type: map
                children:
                  id:
                    type: map
                    open: true
                    doc: ID specifies the name of the extension to use.
                doc: Middleware defines the extension ID for a middleware component.
            doc: Middlewares for the gRPC client.
          read_buffer_size:
            type: int
            doc: ReadBufferSize for gRPC client. See grpc.WithReadBufferSize. (https://godoc.org/google.golang.org/grpc#WithReadBufferSize).
          retry_on_failure:
            type: map
            children:
              enabled:
                type: bool
                doc: Enabled indicates whether to not retry sending batches in case of export failure.
              initial_interval:
                type: duration
                doc: InitialInterval the time to wait after the first failure before retrying.
              max_elapsed_time:
                type: duration
                doc: MaxElapsedTime is the maximum amount of time (including retries) spent trying to send a request/batch. Once this value is reached, the data is discarded. If set to 0, the retries are never stopped.
              max_interval:
                type: duration
                doc: MaxInterval is the upper bound on backoff interval. Once this value is reached the delay between consecutive retries will always be `MaxInterval`.
              multiplier:
                type: float
                doc: Multiplier is the value multiplied by the backoff interval bounds
              randomization_factor:
                type: float
                doc: RandomizationFactor is a random factor used to calculate next backoffs Randomized interval = RetryInterval * (1 ± RandomizationFactor)
            doc: BackOffConfig defines configuration for retrying batches in case of export failure. The current supported strategy is exponential backoff.
          sending_queue:
            type: map
            children:
              batch:
                type: map
                children:
                  flush_timeout:
                    type: duration
                    doc: FlushTimeout sets the time after which a batch will be sent regardless of its size.
                  max_size:
                    type: int
                    doc: MaxSize defines the configuration for the maximum size of a batch.
                  min_size:
                    type: int
                    doc: MinSize defines the configuration for the minimum size of a batch.
                  partition:
                    type: map
                    children:
                      metadata_keys:
                        type: list
                        children:
                          item:
                            type: string
                        doc: MetadataKeys is a list of client.Metadata keys that will be used to partition the data into batches. If this setting is empty, a single batcher instance will be used. When this setting is not empty, one batcher will be used per distinct combination of values for the listed metadata keys. Empty value and unset metadata are treated as distinct cases. Entries are case-insensitive. Duplicated entries will trigger a validation error.
                    doc: Partition defines the partitioning of the batches configuration.
                  sizer:
                    type: map
                    open: true
                    doc: Sizer determines the type of size measurement used by the batch. If not configured, use the same configuration as the queue. It accepts "requests", "items", or "bytes".
                doc: BatchConfig it configures how the requests are consumed from the queue and batch together during consumption.
              block_on_overflow:
                type: bool
                doc: BlockOnOverflow determines the behavior when the component's TotalSize limit is reached. If true, the component will wait for space; otherwise, operations will immediately return a retryable error.
              enabled:
                type: bool
                doc: Enabled indicates whether to not enqueue and batch before exporting.
              num_consumers:
                type: int
                doc: NumConsumers is the maximum number of concurrent consumers from the queue. This applies across all different optional configurations from above (e.g. wait_for_result, block_on_overflow, storage, etc.).
              queue_size:
                type: int
                doc: QueueSize represents the maximum data size allowed for concurrent storage and processing.
              sizer:
                type: map
                open: true
                doc: Sizer determines the type of size measurement used by this component. It accepts "requests", "items", or "bytes".
              storage:
                type: map
                open: true
                doc: 'StorageID if not empty, enables the persistent storage and uses the component specified as a storage extension for the persistent queue. TODO: This will be changed to Optional when available. See https://github.com/open-telemetry/opentelemetry-collector/issues/13822'
              wait_for_result:
                type: bool
                doc: WaitForResult determines if incoming requests are blocked until the request is processed or not. Currently, this option is not available when persistent queue is configured using the storage configuration.
            doc: QueueBatchConfig defines configuration for queueing and batching for the exporter.
          timeout:
            type: duration
            doc: Timeout is the timeout for every attempt to send data to the backend. A zero timeout means no timeout.
          tls:
            type: map
            children:
              ca_file:
                type: string
                doc: Path to the CA cert. For a client this verifies the server certificate. For a server this verifies client certificates. If empty uses system root CA. (optional)
              ca_pem:
                type: string
                doc: In memory PEM encoded cert. (optional)
              cert_file:
                type: string
                doc: Path to the TLS cert to use for TLS required connections. (optional)
              cert_pem:
                type: string
                doc: In memory PEM encoded TLS cert to use for TLS required connections. (optional)
              cipher_suites:
                type: list
                children:
                  item:
                    type: string
                doc: CipherSuites is a list of TLS cipher suites that the TLS transport can use. If left blank, a safe default list is used. See https://go.dev/src/crypto/tls/cipher_suites.go for a list of supported cipher suites.
              curve_preferences:
                type: list
                children:
                  item:
                    type: string
                doc: contains the elliptic curves that will be used in an ECDHE handshake, in preference order Defaults to empty list and "crypto/tls" defaults are used, internally.
              include_insecure_cipher_suites:
                type: bool
              include_system_ca_certs_pool:
                type: bool
                doc: If true, load system CA certificates pool in addition to the certificates configured in this struct.
              insecure:
                type: bool
                doc: In gRPC and HTTP when set to true, this is used to disable the client transport security. See https://godoc.org/google.golang.org/grpc#WithInsecure for gRPC. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional, default false)
              insecure_skip_verify:
                type: bool
                doc: InsecureSkipVerify will enable TLS but not verify the certificate.
              key_file:
                type: string
                doc: Path to the TLS key to use for TLS required connections. (optional)
              key_pem:
                type: string
                doc: In memory PEM encoded TLS key to use for TLS required connections. (optional)
              max_version:
                type: string
                doc: MaxVersion sets the maximum TLS version that is acceptable. If not set, refer to crypto/tls for defaults. (optional)
              min_version:
                type: string
                doc: MinVersion sets the minimum TLS version that is acceptable. If not set, TLS 1.2 will be used. (optional)
              reload_interval:
                type: duration
                doc: ReloadInterval specifies the duration after which the certificate will be reloaded If not set, it will never be reloaded (optional)
              server_name_override:
                type: string
                doc: ServerName requested by client for virtual hosting. This sets the ServerName in the TLSConfig. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional)
              tpm:
                type: map
                children:
                  auth:
                    type: string
                  enabled:
                    type: bool
                  owner_auth:
                    type: string
                  path:
                    type: string
                    doc: The path to the TPM device or Unix domain socket. For instance /dev/tpm0 or /dev/tpmrm0.
                doc: Trusted platform module configuration
            doc: TLS struct exposes TLS client configuration.
          user_agent:
            type: string
            doc: UserAgent overrides the default user-agent header sent on gRPC requests. The default is derived from the build info. When empty, the caller controls the user-agent via grpc.WithUserAgent or similar options.
          wait_for_ready:
            type: bool
            doc: WaitForReady parameter configures client to wait for ready state before sending data. (https://github.com/grpc/grpc/blob/master/doc/wait-for-ready.md)
          write_buffer_size:
            type: int
            doc: WriteBufferSize for gRPC gRPC. See grpc.WithWriteBufferSize. (https://godoc.org/google.golang.org/grpc#WithWriteBufferSize).
        doc: Config defines configuration for OTLP exporter.
    otlp_http:
      type: otlp_http
      signals:
        - traces
        - metrics
        - logs
        - profiles
      stability:
        logs: stable
        metrics: stable
        profiles: alpha
        traces: stable
      module: go.opentelemetry.io/collector/exporter/otlphttpexporter
      alias: otlphttp
      fields:
        type: map
        children:
          auth:
            type: map
            children:
              authenticator:
                type: map
                open: true
                doc: AuthenticatorID specifies the name of the extension to use in order to authenticate the incoming data point.
            doc: Auth configuration for outgoing HTTP calls.
          compression:
            type: string
            doc: The compression key for supported compression types within collector.
          compression_params:
            type: map
            children:
              level:
                type: int
            doc: Advanced configuration options for the Compression
          cookies:
            type: map
            open: true
            doc: Cookies configures the cookie management of the HTTP client.
          disable_keep_alives:
            type: bool
            doc: 'DisableKeepAlives, if true, disables HTTP keep-alives and will only use the connection to the server for a single HTTP request. WARNING: enabling this option can result in significant overhead establishing a new HTTP(S) connection for every request. Before enabling this option please consider whether changes to idle connection settings can achieve your goal.'
          encoding:
            type: string
            doc: 'The encoding to export telemetry (default: "proto")'
          endpoint:
            type: string
            doc: 'The target URL to send data to (e.g.: http://some.url:9411/v1/traces).'
          force_attempt_http2:
            type: bool
            doc: 'Enabling ForceAttemptHTTP2 forces the HTTP transport to use the HTTP/2 protocol. By default, this is set to true. NOTE: HTTP/2 does not support settings such as MaxConnsPerHost, MaxIdleConnsPerHost and MaxIdleConns.'
          headers:
            children:
              item:
                type: map
                children:
                  name:
                    type: string
                  value:
                    type: string
                    doc: String alias that is marshaled and printed in an opaque way. To recover the original value, cast it to a string.
                doc: Pair is an element of a MapList, and consists of a name and an opaque value.
            doc: Additional headers attached to each HTTP request sent by the client. Existing header values are overwritten if collision happens. Header values are opaque since they may be sensitive.
          http2_ping_timeout:
            type: duration
            doc: HTTP2PingTimeout if there's no response to the ping within the configured value, the connection will be closed. If not set or set to 0, it defaults to 15s.
          http2_read_idle_timeout:
            type: duration
            doc: This is needed in case you run into https://github.com/golang/go/issues/59690 https://github.com/golang/go/issues/36026 HTTP2ReadIdleTimeout if the connection has been idle for the configured value send a ping frame for health check 0s means no health check will be performed.
          idle_conn_timeout:
            type: duration
            doc: IdleConnTimeout is the maximum amount of time a connection will remain open before closing itself. By default, it is set to 90 seconds.
          logs_endpoint:
            type: string
            doc: The URL to send logs to. If omitted the Endpoint + "/v1/logs" will be used.
          max_conns_per_host:
            type: int
            doc: MaxConnsPerHost limits the total number of connections per host, including connections in the dialing, active, and idle states. Default is 0 (unlimited).
          max_idle_conns:
            type: int
            doc: MaxIdleConns is used to set a limit to the maximum idle HTTP connections the client can keep open. By default, it is set to 100. Zero means no limit.
          max_idle_conns_per_host:
            type: int
            doc: MaxIdleConnsPerHost is used to set a limit to the maximum idle HTTP connections the host can keep open. If zero, [net/http.DefaultMaxIdleConnsPerHost] is used.
          metrics_endpoint:
            type: string
            doc: The URL to send metrics to. If omitted the Endpoint + "/v1/metrics" will be used.
          middlewares:
            type: list
            children:
              item:
                type: map
                children:
                  id:
                    type: map
                    open: true
                    doc: ID specifies the name of the extension to use.
                doc: Middleware defines the extension ID for a middleware component.
            doc: Middlewares are used to add custom functionality to the HTTP client. Middleware handlers are called in the order they appear in this list, with the first middleware becoming the outermost handler.
          profiles_endpoint:
            type: string
            doc: The URL to send profiles to. If omitted the Endpoint + "/v1development/profiles" will be used.
          proxy_url:
            type: string
            doc: ProxyURL setting for the collector
          read_buffer_size:
            type: int
            doc: ReadBufferSize for HTTP client. See http.Transport.ReadBufferSize. Default is 0.
          retry_on_failure:
            type: map
            children:
              enabled:
                type: bool
                doc: Enabled indicates whether to not retry sending batches in case of export failure.
              initial_interval:
                type: duration
                doc: InitialInterval the time to wait after the first failure before retrying.
              max_elapsed_time:
                type: duration
                doc: MaxElapsedTime is the maximum amount of time (including retries) spent trying to send a request/batch. Once this value is reached, the data is discarded. If set to 0, the retries are never stopped.
              max_interval:
                type: duration
                doc: MaxInterval is the upper bound on backoff interval. Once this value is reached the delay between consecutive retries will always be `MaxInterval`.
              multiplier:
                type: float
                doc: Multiplier is the value multiplied by the backoff interval bounds
              randomization_factor:
                type: float
                doc: RandomizationFactor is a random factor used to calculate next backoffs Randomized interval = RetryInterval * (1 ± RandomizationFactor)
            doc: BackOffConfig defines configuration for retrying batches in case of export failure. The current supported strategy is exponential backoff.
          sending_queue:
            type: map
            children:
              batch:
                type: map
                children:
                  flush_timeout:
                    type: duration
                    doc: FlushTimeout sets the time after which a batch will be sent regardless of its size.
                  max_size:
                    type: int
                    doc: MaxSize defines the configuration for the maximum size of a batch.
                  min_size:
                    type: int
                    doc: MinSize defines the configuration for the minimum size of a batch.
                  partition:
                    type: map
                    children:
                      metadata_keys:
                        type: list
                        children:
                          item:
                            type: string
                        doc: MetadataKeys is a list of client.Metadata keys that will be used to partition the data into batches. If this setting is empty, a single batcher instance will be used. When this setting is not empty, one batcher will be used per distinct combination of values for the listed metadata keys. Empty value and unset metadata are treated as distinct cases. Entries are case-insensitive. Duplicated entries will trigger a validation error.
                    doc: Partition defines the partitioning of the batches configuration.
                  sizer:
                    type: map
                    open: true
                    doc: Sizer determines the type of size measurement used by the batch. If not configured, use the same configuration as the queue. It accepts "requests", "items", or "bytes".
                doc: BatchConfig it configures how the requests are consumed from the queue and batch together during consumption.
              block_on_overflow:
                type: bool
                doc: BlockOnOverflow determines the behavior when the component's TotalSize limit is reached. If true, the component will wait for space; otherwise, operations will immediately return a retryable error.
              enabled:
                type: bool
                doc: Enabled indicates whether to not enqueue and batch before exporting.
              num_consumers:
                type: int
                doc: NumConsumers is the maximum number of concurrent consumers from the queue. This applies across all different optional configurations from above (e.g. wait_for_result, block_on_overflow, storage, etc.).
              queue_size:
                type: int
                doc: QueueSize represents the maximum data size allowed for concurrent storage and processing.
              sizer:
                type: map
                open: true
                doc: Sizer determines the type of size measurement used by this component. It accepts "requests", "items", or "bytes".
              storage:
                type: map
                open: true
                doc: 'StorageID if not empty, enables the persistent storage and uses the component specified as a storage extension for the persistent queue. TODO: This will be changed to Optional when available. See https://github.com/open-telemetry/opentelemetry-collector/issues/13822'
              wait_for_result:
                type: bool
                doc: WaitForResult determines if incoming requests are blocked until the request is processed or not. Currently, this option is not available when persistent queue is configured using the storage configuration.
            doc: QueueBatchConfig defines configuration for queueing and batching for the exporter.
          timeout:
            type: duration
            doc: Timeout parameter configures `http.Client.Timeout`. Default is 0 (unlimited).
          tls:
            type: map
            children:
              ca_file:
                type: string
                doc: Path to the CA cert. For a client this verifies the server certificate. For a server this verifies client certificates. If empty uses system root CA. (optional)
              ca_pem:
                type: string
                doc: In memory PEM encoded cert. (optional)
              cert_file:
                type: string
                doc: Path to the TLS cert to use for TLS required connections. (optional)
              cert_pem:
                type: string
                doc: In memory PEM encoded TLS cert to use for TLS required connections. (optional)
              cipher_suites:
                type: list
                children:
                  item:
                    type: string
                doc: CipherSuites is a list of TLS cipher suites that the TLS transport can use. If left blank, a safe default list is used. See https://go.dev/src/crypto/tls/cipher_suites.go for a list of supported cipher suites.
              curve_preferences:
                type: list
                children:
                  item:
                    type: string
                doc: contains the elliptic curves that will be used in an ECDHE handshake, in preference order Defaults to empty list and "crypto/tls" defaults are used, internally.
              include_insecure_cipher_suites:
                type: bool
              include_system_ca_certs_pool:
                type: bool
                doc: If true, load system CA certificates pool in addition to the certificates configured in this struct.
              insecure:
                type: bool
                doc: In gRPC and HTTP when set to true, this is used to disable the client transport security. See https://godoc.org/google.golang.org/grpc#WithInsecure for gRPC. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional, default false)
              insecure_skip_verify:
                type: bool
                doc: InsecureSkipVerify will enable TLS but not verify the certificate.
              key_file:
                type: string
                doc: Path to the TLS key to use for TLS required connections. (optional)
              key_pem:
                type: string
                doc: In memory PEM encoded TLS key to use for TLS required connections. (optional)
              max_version:
                type: string
                doc: MaxVersion sets the maximum TLS version that is acceptable. If not set, refer to crypto/tls for defaults. (optional)
              min_version:
                type: string
                doc: MinVersion sets the minimum TLS version that is acceptable. If not set, TLS 1.2 will be used. (optional)
              reload_interval:
                type: duration
                doc: ReloadInterval specifies the duration after which the certificate will be reloaded If not set, it will never be reloaded (optional)
              server_name_override:
                type: string
                doc: ServerName requested by client for virtual hosting. This sets the ServerName in the TLSConfig. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional)
              tpm:
                type: map
                children:
                  auth:
                    type: string
                  enabled:
                    type: bool
                  owner_auth:
                    type: string
                  path:
                    type: string
                    doc: The path to the TPM device or Unix domain socket. For instance /dev/tpm0 or /dev/tpmrm0.
                doc: Trusted platform module configuration
            doc: TLS struct exposes TLS client configuration.
          traces_endpoint:
            type: string
            doc: The URL to send traces to. If omitted the Endpoint + "/v1/traces" will be used.
          write_buffer_size:
            type: int
            doc: WriteBufferSize for HTTP client. See http.Transport.WriteBufferSize. Default is 0.
        doc: Config defines configuration for OTLP/HTTP exporter.
    otlphttp:
      type: otlphttp
      signals:
        - traces
        - metrics
        - logs
        - profiles
      stability:
        logs: stable
        metrics: stable
        profiles: alpha
        traces: stable
      module: go.opentelemetry.io/collector/exporter/otlphttpexporter
      deprecated: renamed to "otlp_http" upstream; the old name still resolves for now
      aliasOf: otlp_http
      fields:
        type: map
        children:
          auth:
            type: map
            children:
              authenticator:
                type: map
                open: true
                doc: AuthenticatorID specifies the name of the extension to use in order to authenticate the incoming data point.
            doc: Auth configuration for outgoing HTTP calls.
          compression:
            type: string
            doc: The compression key for supported compression types within collector.
          compression_params:
            type: map
            children:
              level:
                type: int
            doc: Advanced configuration options for the Compression
          cookies:
            type: map
            open: true
            doc: Cookies configures the cookie management of the HTTP client.
          disable_keep_alives:
            type: bool
            doc: 'DisableKeepAlives, if true, disables HTTP keep-alives and will only use the connection to the server for a single HTTP request. WARNING: enabling this option can result in significant overhead establishing a new HTTP(S) connection for every request. Before enabling this option please consider whether changes to idle connection settings can achieve your goal.'
          encoding:
            type: string
            doc: 'The encoding to export telemetry (default: "proto")'
          endpoint:
            type: string
            doc: 'The target URL to send data to (e.g.: http://some.url:9411/v1/traces).'
          force_attempt_http2:
            type: bool
            doc: 'Enabling ForceAttemptHTTP2 forces the HTTP transport to use the HTTP/2 protocol. By default, this is set to true. NOTE: HTTP/2 does not support settings such as MaxConnsPerHost, MaxIdleConnsPerHost and MaxIdleConns.'
          headers:
            children:
              item:
                type: map
                children:
                  name:
                    type: string
                  value:
                    type: string
                    doc: String alias that is marshaled and printed in an opaque way. To recover the original value, cast it to a string.
                doc: Pair is an element of a MapList, and consists of a name and an opaque value.
            doc: Additional headers attached to each HTTP request sent by the client. Existing header values are overwritten if collision happens. Header values are opaque since they may be sensitive.
          http2_ping_timeout:
            type: duration
            doc: HTTP2PingTimeout if there's no response to the ping within the configured value, the connection will be closed. If not set or set to 0, it defaults to 15s.
          http2_read_idle_timeout:
            type: duration
            doc: This is needed in case you run into https://github.com/golang/go/issues/59690 https://github.com/golang/go/issues/36026 HTTP2ReadIdleTimeout if the connection has been idle for the configured value send a ping frame for health check 0s means no health check will be performed.
          idle_conn_timeout:
            type: duration
            doc: IdleConnTimeout is the maximum amount of time a connection will remain open before closing itself. By default, it is set to 90 seconds.
          logs_endpoint:
            type: string
            doc: The URL to send logs to. If omitted the Endpoint + "/v1/logs" will be used.
          max_conns_per_host:
            type: int
            doc: MaxConnsPerHost limits the total number of connections per host, including connections in the dialing, active, and idle states. Default is 0 (unlimited).
          max_idle_conns:
            type: int
            doc: MaxIdleConns is used to set a limit to the maximum idle HTTP connections the client can keep open. By default, it is set to 100. Zero means no limit.
          max_idle_conns_per_host:
            type: int
            doc: MaxIdleConnsPerHost is used to set a limit to the maximum idle HTTP connections the host can keep open. If zero, [net/http.DefaultMaxIdleConnsPerHost] is used.
          metrics_endpoint:
            type: string
            doc: The URL to send metrics to. If omitted the Endpoint + "/v1/metrics" will be used.
          middlewares:
            type: list
            children:
              item:
                type: map
                children:
                  id:
                    type: map
                    open: true
                    doc: ID specifies the name of the extension to use.
                doc: Middleware defines the extension ID for a middleware component.
            doc: Middlewares are used to add custom functionality to the HTTP client. Middleware handlers are called in the order they appear in this list, with the first middleware becoming the outermost handler.
          profiles_endpoint:
            type: string
            doc: The URL to send profiles to. If omitted the Endpoint + "/v1development/profiles" will be used.
          proxy_url:
            type: string
            doc: ProxyURL setting for the collector
          read_buffer_size:
            type: int
            doc: ReadBufferSize for HTTP client. See http.Transport.ReadBufferSize. Default is 0.
          retry_on_failure:
            type: map
            children:
              enabled:
                type: bool
                doc: Enabled indicates whether to not retry sending batches in case of export failure.
              initial_interval:
                type: duration
                doc: InitialInterval the time to wait after the first failure before retrying.
              max_elapsed_time:
                type: duration
                doc: MaxElapsedTime is the maximum amount of time (including retries) spent trying to send a request/batch. Once this value is reached, the data is discarded. If set to 0, the retries are never stopped.
              max_interval:
                type: duration
                doc: MaxInterval is the upper bound on backoff interval. Once this value is reached the delay between consecutive retries will always be `MaxInterval`.
              multiplier:
                type: float
                doc: Multiplier is the value multiplied by the backoff interval bounds
              randomization_factor:
                type: float
                doc: RandomizationFactor is a random factor used to calculate next backoffs Randomized interval = RetryInterval * (1 ± RandomizationFactor)
            doc: BackOffConfig defines configuration for retrying batches in case of export failure. The current supported strategy is exponential backoff.
          sending_queue:
            type: map
            children:
              batch:
                type: map
                children:
                  flush_timeout:
                    type: duration
                    doc: FlushTimeout sets the time after which a batch will be sent regardless of its size.
                  max_size:
                    type: int
                    doc: MaxSize defines the configuration for the maximum size of a batch.
                  min_size:
                    type: int
                    doc: MinSize defines the configuration for the minimum size of a batch.
                  partition:
                    type: map
                    children:
                      metadata_keys:
                        type: list
                        children:
                          item:
                            type: string
                        doc: MetadataKeys is a list of client.Metadata keys that will be used to partition the data into batches. If this setting is empty, a single batcher instance will be used. When this setting is not empty, one batcher will be used per distinct combination of values for the listed metadata keys. Empty value and unset metadata are treated as distinct cases. Entries are case-insensitive. Duplicated entries will trigger a validation error.
                    doc: Partition defines the partitioning of the batches configuration.
                  sizer:
                    type: map
                    open: true
                    doc: Sizer determines the type of size measurement used by the batch. If not configured, use the same configuration as the queue. It accepts "requests", "items", or "bytes".
                doc: BatchConfig it configures how the requests are consumed from the queue and batch together during consumption.
              block_on_overflow:
                type: bool
                doc: BlockOnOverflow determines the behavior when the component's TotalSize limit is reached. If true, the component will wait for space; otherwise, operations will immediately return a retryable error.
              enabled:
                type: bool
                doc: Enabled indicates whether to not enqueue and batch before exporting.
              num_consumers:
                type: int
                doc: NumConsumers is the maximum number of concurrent consumers from the queue. This applies across all different optional configurations from above (e.g. wait_for_result, block_on_overflow, storage, etc.).
              queue_size:
                type: int
                doc: QueueSize represents the maximum data size allowed for concurrent storage and processing.
              sizer:
                type: map
                open: true
                doc: Sizer determines the type of size measurement used by this component. It accepts "requests", "items", or "bytes".
              storage:
                type: map
                open: true
                doc: 'StorageID if not empty, enables the persistent storage and uses the component specified as a storage extension for the persistent queue. TODO: This will be changed to Optional when available. See https://github.com/open-telemetry/opentelemetry-collector/issues/13822'
              wait_for_result:
                type: bool
                doc: WaitForResult determines if incoming requests are blocked until the request is processed or not. Currently, this option is not available when persistent queue is configured using the storage configuration.
            doc: QueueBatchConfig defines configuration for queueing and batching for the exporter.
          timeout:
            type: duration
            doc: Timeout parameter configures `http.Client.Timeout`. Default is 0 (unlimited).
          tls:
            type: map
            children:
              ca_file:
                type: string
                doc: Path to the CA cert. For a client this verifies the server certificate. For a server this verifies client certificates. If empty uses system root CA. (optional)
              ca_pem:
                type: string
                doc: In memory PEM encoded cert. (optional)
              cert_file:
                type: string
                doc: Path to the TLS cert to use for TLS required connections. (optional)
              cert_pem:
                type: string
                doc: In memory PEM encoded TLS cert to use for TLS required connections. (optional)
              cipher_suites:
                type: list
                children:
                  item:
                    type: string
                doc: CipherSuites is a list of TLS cipher suites that the TLS transport can use. If left blank, a safe default list is used. See https://go.dev/src/crypto/tls/cipher_suites.go for a list of supported cipher suites.
              curve_preferences:
                type: list
                children:
                  item:
                    type: string
                doc: contains the elliptic curves that will be used in an ECDHE handshake, in preference order Defaults to empty list and "crypto/tls" defaults are used, internally.
              include_insecure_cipher_suites:
                type: bool
              include_system_ca_certs_pool:
                type: bool
                doc: If true, load system CA certificates pool in addition to the certificates configured in this struct.
              insecure:
                type: bool
                doc: In gRPC and HTTP when set to true, this is used to disable the client transport security. See https://godoc.org/google.golang.org/grpc#WithInsecure for gRPC. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional, default false)
              insecure_skip_verify:
                type: bool
                doc: InsecureSkipVerify will enable TLS but not verify the certificate.
              key_file:
                type: string
                doc: Path to the TLS key to use for TLS required connections. (optional)
              key_pem:
                type: string
                doc: In memory PEM encoded TLS key to use for TLS required connections. (optional)
              max_version:
                type: string
                doc: MaxVersion sets the maximum TLS version that is acceptable. If not set, refer to crypto/tls for defaults. (optional)
              min_version:
                type: string
                doc: MinVersion sets the minimum TLS version that is acceptable. If not set, TLS 1.2 will be used. (optional)
              reload_interval:
                type: duration
                doc: ReloadInterval specifies the duration after which the certificate will be reloaded If not set, it will never be reloaded (optional)
              server_name_override:
                type: string
                doc: ServerName requested by client for virtual hosting. This sets the ServerName in the TLSConfig. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional)
              tpm:
                type: map
                children:
                  auth:
                    type: string
                  enabled:
                    type: bool
                  owner_auth:
                    type: string
                  path:
                    type: string
                    doc: The path to the TPM device or Unix domain socket. For instance /dev/tpm0 or /dev/tpmrm0.
                doc: Trusted platform module configuration
            doc: TLS struct exposes TLS client configuration.
          traces_endpoint:
            type: string
            doc: The URL to send traces to. If omitted the Endpoint + "/v1/traces" will be used.
          write_buffer_size:
            type: int
            doc: WriteBufferSize for HTTP client. See http.Transport.WriteBufferSize. Default is 0.
        doc: Config defines configuration for OTLP/HTTP exporter.
    prometheus:
      type: prometheus
      signals:
        - metrics
      stability:
        metrics: beta
      module: github.com/open-telemetry/opentelemetry-collector-contrib/exporter/prometheusexporter
      fields:
        type: map
        children:
          add_metric_suffixes:
            type: bool
            doc: 'AddMetricSuffixes controls whether suffixes are added to metric names. Defaults to true. Deprecated: Use TranslationStrategy instead. This setting is ignored when TranslationStrategy is explicitly set.'
          auth:
            type: map
            children:
              authenticator:
                type: map
                open: true
                doc: AuthenticatorID specifies the name of the extension to use in order to authenticate the incoming data point.
              request_params:
                type: list
                children:
                  item:
                    type: string
                doc: RequestParameters is a list of parameters that should be extracted from the request and added to the context. When a parameter is found in both the query string and the header, the value from the query string will be used.
            doc: Auth for this receiver
          compression_algorithms:
            type: list
            children:
              item:
                type: string
            doc: 'CompressionAlgorithms configures the list of compression algorithms the server can accept. Default: ["", "gzip", "zstd", "zlib", "snappy", "deflate"]'
          const_labels:
            type: map
            open: true
            doc: ConstLabels are values that are applied for every exported metric.
          cors:
            type: map
            children:
              allowed_headers:
                type: list
                children:
                  item:
                    type: string
                doc: AllowedHeaders sets what headers will be allowed in CORS requests. The Accept, Accept-Language, Content-Type, and Content-Language headers are implicitly allowed. If no headers are listed, X-Requested-With will also be accepted by default. Include "*" to allow any request header.
              allowed_origins:
                type: list
                children:
                  item:
                    type: string
                doc: AllowedOrigins sets the allowed values of the Origin header for HTTP/JSON requests to an OTLP receiver. An origin may contain a wildcard (*) to replace 0 or more characters (e.g., "http://*.domain.com", or "*" to allow any origin).
              exposed_headers:
                type: list
                children:
                  item:
                    type: string
                doc: ExposedHeaders sets the value of the Access-Control-Expose-Headers response header, indicating which headers are safe to expose to the API of a CORS response.
              max_age:
                type: int
                doc: MaxAge sets the value of the Access-Control-Max-Age response header. Set it to the number of seconds that browsers should cache a CORS preflight response for.
            doc: CORS configures the server for HTTP cross-origin resource sharing (CORS).
          dialer:
            type: map
            children:
              timeout:
                type: duration
          enable_open_metrics:
            type: bool
            doc: EnableOpenMetrics enables the use of the OpenMetrics encoding option for the prometheus exporter.
          endpoint:
            type: string
            doc: Endpoint configures the listening address for the server.
          idle_timeout:
            type: duration
            doc: IdleTimeout is the maximum amount of time to wait for the next request when keep-alives are enabled. If IdleTimeout is zero, the value of ReadTimeout is used. If both are zero, there is no timeout.
          include_metadata:
            type: bool
            doc: IncludeMetadata propagates the client metadata from the incoming requests to the downstream consumers
          keep_alives_enabled:
            type: bool
            doc: KeepAlivesEnabled controls whether HTTP keep-alives are enabled. By default, keep-alives are always enabled. Only very resource-constrained environments should disable them.
          max_request_body_size:
            type: int
            doc: 'MaxRequestBodySize sets the maximum request body size in bytes. Default: 20MiB.'
          metric_expiration:
            type: duration
            doc: MetricExpiration defines how long metrics are kept without updates
          middlewares:
            type: list
            children:
              item:
                type: map
                children:
                  id:
                    type: map
                    open: true
                    doc: ID specifies the name of the extension to use.
                doc: Middleware defines the extension ID for a middleware component.
            doc: Middlewares are used to add custom functionality to the HTTP server. Middleware handlers are called in the order they appear in this list, with the first middleware becoming the outermost handler.
          namespace:
            type: string
            doc: Namespace if set, exports metrics under the provided value.
          read_header_timeout:
            type: duration
            doc: ReadHeaderTimeout is the amount of time allowed to read request headers. The connection's read deadline is reset after reading the headers and the Handler can decide what is considered too slow for the body. If ReadHeaderTimeout is zero, the value of ReadTimeout is used. If both are zero, there is no timeout.
          read_timeout:
            type: duration
            doc: ReadTimeout is the maximum duration for reading the entire request, including the body. A zero or negative value means there will be no timeout. Because ReadTimeout does not let Handlers make per-request decisions on each request body's acceptable deadline or upload rate, most users will prefer to use ReadHeaderTimeout. It is valid to use them both.
          resource_to_telemetry_conversion:
            type: map
            children:
              enabled:
                type: bool
                doc: Enabled indicates whether to convert resource attributes to telemetry attributes. Default is `false`.
              exclude_service_attributes:
                type: bool
                doc: ExcludeServiceAttributes indicates whether to exclude `service.name`, `service.instance.id` and `service.namespace` resource attributes from being converted to metric attributes. Default is `false`. When set to `true`, these attributes will not be added to metric labels since they are already mapped to Prometheus `job` and `instance` labels respectively.
            doc: ResourceToTelemetrySettings defines configuration for converting resource attributes to metric labels.
          response_headers:
            children:
              item:
                type: map
                children:
                  name:
                    type: string
                  value:
                    type: string
                    doc: String alias that is marshaled and printed in an opaque way. To recover the original value, cast it to a string.
                doc: Pair is an element of a MapList, and consists of a name and an opaque value.
            doc: Additional headers attached to each HTTP response sent to the client. Header values are opaque since they may be sensitive.
          send_timestamps:
            type: bool
            doc: SendTimestamps will send the underlying scrape timestamp with the export
          sending_queue:
            type: map
            children:
              batch:
                type: map
                children:
                  flush_timeout:
                    type: duration
                    doc: FlushTimeout sets the time after which a batch will be sent regardless of its size.
                  max_size:
                    type: int
                    doc: MaxSize defines the configuration for the maximum size of a batch.
                  min_size:
                    type: int
                    doc: MinSize defines the configuration for the minimum size of a batch.
                  partition:
                    type: map
                    children:
                      metadata_keys:
                        type: list
                        children:
                          item:
                            type: string
                        doc: MetadataKeys is a list of client.Metadata keys that will be used to partition the data into batches. If this setting is empty, a single batcher instance will be used. When this setting is not empty, one batcher will be used per distinct combination of values for the listed metadata keys. Empty value and unset metadata are treated as distinct cases. Entries are case-insensitive. Duplicated entries will trigger a validation error.
                    doc: Partition defines the partitioning of the batches configuration.
                  sizer:
                    type: map
                    open: true
                    doc: Sizer determines the type of size measurement used by the batch. If not configured, use the same configuration as the queue. It accepts "requests", "items", or "bytes".
                doc: BatchConfig it configures how the requests are consumed from the queue and batch together during consumption.
              block_on_overflow:
                type: bool
                doc: BlockOnOverflow determines the behavior when the component's TotalSize limit is reached. If true, the component will wait for space; otherwise, operations will immediately return a retryable error.
              enabled:
                type: bool
                doc: Enabled indicates whether to not enqueue and batch before exporting.
              num_consumers:
                type: int
                doc: NumConsumers is the maximum number of concurrent consumers from the queue. This applies across all different optional configurations from above (e.g. wait_for_result, block_on_overflow, storage, etc.).
              queue_size:
                type: int
                doc: QueueSize represents the maximum data size allowed for concurrent storage and processing.
              sizer:
                type: map
                open: true
                doc: Sizer determines the type of size measurement used by this component. It accepts "requests", "items", or "bytes".
              storage:
                type: map
                open: true
                doc: 'StorageID if not empty, enables the persistent storage and uses the component specified as a storage extension for the persistent queue. TODO: This will be changed to Optional when available. See https://github.com/open-telemetry/opentelemetry-collector/issues/13822'
              wait_for_result:
                type: bool
                doc: WaitForResult determines if incoming requests are blocked until the request is processed or not. Currently, this option is not available when persistent queue is configured using the storage configuration.
            doc: QueueBatchConfig defines the queue configuration.
          tls:
            type: map
            children:
              ca_file:
                type: string
                doc: Path to the CA cert. For a client this verifies the server certificate. For a server this verifies client certificates. If empty uses system root CA. (optional)
              ca_pem:
                type: string
                doc: In memory PEM encoded cert. (optional)
              cert_file:
                type: string
                doc: Path to the TLS cert to use for TLS required connections. (optional)
              cert_pem:
                type: string
                doc: In memory PEM encoded TLS cert to use for TLS required connections. (optional)
              cipher_suites:
                type: list
                children:
                  item:
                    type: string
                doc: CipherSuites is a list of TLS cipher suites that the TLS transport can use. If left blank, a safe default list is used. See https://go.dev/src/crypto/tls/cipher_suites.go for a list of supported cipher suites.
              client_ca_file:
                type: string
                doc: Path to the TLS cert to use by the server to verify a client certificate. (optional) This sets the ClientCAs and ClientAuth to RequireAndVerifyClientCert in the TLSConfig. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional)
              client_ca_file_reload:
                type: bool
                doc: Reload the ClientCAs file when it is modified (optional, default false)
              curve_preferences:
                type: list
                children:
                  item:
                    type: string
                doc: contains the elliptic curves that will be used in an ECDHE handshake, in preference order Defaults to empty list and "crypto/tls" defaults are used, internally.
              include_insecure_cipher_suites:
                type: bool
              include_system_ca_certs_pool:
                type: bool
                doc: If true, load system CA certificates pool in addition to the certificates configured in this struct.
              key_file:
                type: string
                doc: Path to the TLS key to use for TLS required connections. (optional)
              key_pem:
                type: string
                doc: In memory PEM encoded TLS key to use for TLS required connections. (optional)
              max_version:
                type: string
                doc: MaxVersion sets the maximum TLS version that is acceptable. If not set, refer to crypto/tls for defaults. (optional)
              min_version:
                type: string
                doc: MinVersion sets the minimum TLS version that is acceptable. If not set, TLS 1.2 will be used. (optional)
              reload_interval:
                type: duration
                doc: ReloadInterval specifies the duration after which the certificate will be reloaded If not set, it will never be reloaded (optional)
              tpm:
                type: map
                children:
                  auth:
                    type: string
                  enabled:
                    type: bool
                  owner_auth:
                    type: string
                  path:
                    type: string
                    doc: The path to the TPM device or Unix domain socket. For instance /dev/tpm0 or /dev/tpmrm0.
                doc: Trusted platform module configuration
            doc: TLS struct exposes TLS client configuration.
          translation_strategy:
            type: string
            doc: TranslationStrategy controls how OTLP metric and attribute names are translated into Prometheus metric and label names. When set, this takes precedence over AddMetricSuffixes.
          transport:
            type: string
          without_scope_info:
            type: bool
            doc: WithoutScopeInfo controls the addition of labels for the instrumentation scope.
          write_timeout:
            type: duration
            doc: WriteTimeout is the maximum duration before timing out writes of the response. It is reset whenever a new request's header is read. Like ReadTimeout, it does not let Handlers make decisions on a per-request basis. A zero or negative value means there will be no timeout.
        doc: Config defines configuration for Prometheus exporter.
    prometheus_remote_write:
      type: prometheus_remote_write
      signals:
        - metrics
      stability:
        metrics: beta
      module: github.com/open-telemetry/opentelemetry-collector-contrib/exporter/prometheusremotewriteexporter
      alias: prometheusremotewrite
      fields:
        type: map
        children:
          add_metric_suffixes:
            type: bool
            doc: 'AddMetricSuffixes controls whether unit and type suffixes are added to metrics on export Deprecated: Use TranslationStrategy instead. It will be removed in v0.153.0.'
          auth:
            type: map
            children:
              authenticator:
                type: map
                open: true
                doc: AuthenticatorID specifies the name of the extension to use in order to authenticate the incoming data point.
            doc: Auth configuration for outgoing HTTP calls.
          compression:
            type: string
            doc: The compression key for supported compression types within collector.
          compression_params:
            type: map
            children:
              level:
                type: int
            doc: Advanced configuration options for the Compression
          cookies:
            type: map
            open: true
            doc: Cookies configures the cookie management of the HTTP client.
          disable_keep_alives:
            type: bool
            doc: 'DisableKeepAlives, if true, disables HTTP keep-alives and will only use the connection to the server for a single HTTP request. WARNING: enabling this option can result in significant overhead establishing a new HTTP(S) connection for every request. Before enabling this option please consider whether changes to idle connection settings can achieve your goal.'
          disable_scope_info:
            type: bool
            doc: DisableScopeInfo allows disabling the export of the scope info labels
          enabled:
            type: bool
            doc: Enabled indicates whether to not retry sending batches in case of export failure.
          endpoint:
            type: string
            doc: 'The target URL to send data to (e.g.: http://some.url:9411/v1/traces).'
          external_labels:
            type: map
            open: true
            doc: ExternalLabels defines a map of label keys and values that are allowed to start with reserved prefix "__"
          force_attempt_http2:
            type: bool
            doc: 'Enabling ForceAttemptHTTP2 forces the HTTP transport to use the HTTP/2 protocol. By default, this is set to true. NOTE: HTTP/2 does not support settings such as MaxConnsPerHost, MaxIdleConnsPerHost and MaxIdleConns.'
          headers:
            children:
              item:
                type: map
                children:
                  name:
                    type: string
                  value:
                    type: string
                    doc: String alias that is marshaled and printed in an opaque way. To recover the original value, cast it to a string.
                doc: Pair is an element of a MapList, and consists of a name and an opaque value.
            doc: Additional headers attached to each HTTP request sent by the client. Existing header values are overwritten if collision happens. Header values are opaque since they may be sensitive.
          http2_ping_timeout:
            type: duration
            doc: HTTP2PingTimeout if there's no response to the ping within the configured value, the connection will be closed. If not set or set to 0, it defaults to 15s.
          http2_read_idle_timeout:
            type: duration
            doc: This is needed in case you run into https://github.com/golang/go/issues/59690 https://github.com/golang/go/issues/36026 HTTP2ReadIdleTimeout if the connection has been idle for the configured value send a ping frame for health check 0s means no health check will be performed.
          idle_conn_timeout:
            type: duration
            doc: IdleConnTimeout is the maximum amount of time a connection will remain open before closing itself. By default, it is set to 90 seconds.
          initial_interval:
            type: duration
            doc: InitialInterval the time to wait after the first failure before retrying.
          max_batch_request_parallelism:
            type: int
            doc: maximum amount of parallel requests to do when handling large batch request
          max_batch_size_bytes:
            type: int
            doc: maximum size in bytes of time series batch sent to remote storage
          max_conns_per_host:
            type: int
            doc: MaxConnsPerHost limits the total number of connections per host, including connections in the dialing, active, and idle states. Default is 0 (unlimited).
          max_elapsed_time:
            type: duration
            doc: MaxElapsedTime is the maximum amount of time (including retries) spent trying to send a request/batch. Once this value is reached, the data is discarded. If set to 0, the retries are never stopped.
          max_idle_conns:
            type: int
            doc: MaxIdleConns is used to set a limit to the maximum idle HTTP connections the client can keep open. By default, it is set to 100. Zero means no limit.
          max_idle_conns_per_host:
            type: int
            doc: MaxIdleConnsPerHost is used to set a limit to the maximum idle HTTP connections the host can keep open. If zero, [net/http.DefaultMaxIdleConnsPerHost] is used.
          max_interval:
            type: duration
            doc: MaxInterval is the upper bound on backoff interval. Once this value is reached the delay between consecutive retries will always be `MaxInterval`.
          middlewares:
            type: list
            children:
              item:
                type: map
                children:
                  id:
                    type: map
                    open: true
                    doc: ID specifies the name of the extension to use.
                doc: Middleware defines the extension ID for a middleware component.
            doc: Middlewares are used to add custom functionality to the HTTP client. Middleware handlers are called in the order they appear in this list, with the first middleware becoming the outermost handler.
          multiplier:
            type: float
            doc: Multiplier is the value multiplied by the backoff interval bounds
          namespace:
            type: string
            doc: 'prefix attached to each exported metric name See: https://prometheus.io/docs/practices/naming/#metric-names'
          protobuf_message:
            type: map
            open: true
            doc: RemoteWriteProtoMsg controls whether prometheus remote write v1 or v2 is sent.
          proxy_url:
            type: string
            doc: ProxyURL setting for the collector
          randomization_factor:
            type: float
            doc: RandomizationFactor is a random factor used to calculate next backoffs Randomized interval = RetryInterval * (1 ± RandomizationFactor)
          read_buffer_size:
            type: int
            doc: ReadBufferSize for HTTP client. See http.Transport.ReadBufferSize. Default is 0.
          remote_write_queue:
            type: map
            children:
              enabled:
                type: bool
                doc: Enabled if false the queue is not enabled, the export requests are executed synchronously.
              num_consumers:
                type: int
                doc: NumWorkers configures the number of workers used by the collector to fan out remote write requests.
              queue_size:
                type: int
                doc: QueueSize is the maximum number of OTLP metric batches allowed in the queue at a given time. Ignored if Enabled is false.
            doc: QueueConfig allows users to fine tune the queues that handle outgoing requests.
          resource_to_telemetry_conversion:
            type: map
            children:
              enabled:
                type: bool
                doc: Enabled indicates whether to convert resource attributes to telemetry attributes. Default is `false`.
              exclude_service_attributes:
                type: bool
                doc: ExcludeServiceAttributes indicates whether to exclude `service.name`, `service.instance.id` and `service.namespace` resource attributes from being converted to metric attributes. Default is `false`. When set to `true`, these attributes will not be added to metric labels since they are already mapped to Prometheus `job` and `instance` labels respectively.
            doc: ResourceToTelemetrySettings is the option for converting resource attributes to telemetry attributes. "Enabled" - A boolean field to enable/disable this option. Default is `false`. If enabled, all the resource attributes will be converted to metric labels by default. "ExcludeServiceAttributes" - If set to `true`, the `service.name`, `service.instance.id` and `service.namespace` resource attributes, which are already converted to `job` and `instance` labels respectively, will be excluded from the final metrics.
          retry_on_failure:
            type: map
            children:
              enabled:
                type: bool
              initial_interval:
                type: duration
              max_elapsed_time:
                type: duration
              max_interval:
                type: duration
              multiplier:
                type: float
              randomization_factor:
                type: float
          send_metadata:
            type: bool
            doc: SendMetadata controls whether prometheus metadata will be generated and sent, this option is ignored when using PRW 2.0, which always includes metadata.
          target_info:
            type: map
            children:
              enabled:
                type: bool
                doc: Enabled if false the target_info metric is not generated by the exporter
            doc: TargetInfo allows customizing the target_info metric
          timeout:
            type: duration
            doc: Timeout is the timeout for every attempt to send data to the backend. A zero timeout means no timeout.
          tls:
            type: map
            children:
              ca_file:
                type: string
                doc: Path to the CA cert. For a client this verifies the server certificate. For a server this verifies client certificates. If empty uses system root CA. (optional)
              ca_pem:
                type: string
                doc: In memory PEM encoded cert. (optional)
              cert_file:
                type: string
                doc: Path to the TLS cert to use for TLS required connections. (optional)
              cert_pem:
                type: string
                doc: In memory PEM encoded TLS cert to use for TLS required connections. (optional)
              cipher_suites:
                type: list
                children:
                  item:
                    type: string
                doc: CipherSuites is a list of TLS cipher suites that the TLS transport can use. If left blank, a safe default list is used. See https://go.dev/src/crypto/tls/cipher_suites.go for a list of supported cipher suites.
              curve_preferences:
                type: list
                children:
                  item:
                    type: string
                doc: contains the elliptic curves that will be used in an ECDHE handshake, in preference order Defaults to empty list and "crypto/tls" defaults are used, internally.
              include_insecure_cipher_suites:
                type: bool
              include_system_ca_certs_pool:
                type: bool
                doc: If true, load system CA certificates pool in addition to the certificates configured in this struct.
              insecure:
                type: bool
                doc: In gRPC and HTTP when set to true, this is used to disable the client transport security. See https://godoc.org/google.golang.org/grpc#WithInsecure for gRPC. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional, default false)
              insecure_skip_verify:
                type: bool
                doc: InsecureSkipVerify will enable TLS but not verify the certificate.
              key_file:
                type: string
                doc: Path to the TLS key to use for TLS required connections. (optional)
              key_pem:
                type: string
                doc: In memory PEM encoded TLS key to use for TLS required connections. (optional)
              max_version:
                type: string
                doc: MaxVersion sets the maximum TLS version that is acceptable. If not set, refer to crypto/tls for defaults. (optional)
              min_version:
                type: string
                doc: MinVersion sets the minimum TLS version that is acceptable. If not set, TLS 1.2 will be used. (optional)
              reload_interval:
                type: duration
                doc: ReloadInterval specifies the duration after which the certificate will be reloaded If not set, it will never be reloaded (optional)
              server_name_override:
                type: string
                doc: ServerName requested by client for virtual hosting. This sets the ServerName in the TLSConfig. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional)
              tpm:
                type: map
                children:
                  auth:
                    type: string
                  enabled:
                    type: bool
                  owner_auth:
                    type: string
                  path:
                    type: string
                    doc: The path to the TPM device or Unix domain socket. For instance /dev/tpm0 or /dev/tpmrm0.
                doc: Trusted platform module configuration
            doc: TLS struct exposes TLS client configuration.
          translation_strategy:
            type: string
            doc: TranslationStrategy controls how OTLP metric and attribute names are translated into Prometheus metric and label names. When set, this takes precedence over AddMetricSuffixes.
          wal:
            type: map
            children:
              buffer_size:
                type: int
              directory:
                type: string
              lag_record_frequency:
                type: duration
              truncate_frequency:
                type: duration
            doc: WAL enables persisting metrics to a write-ahead-log before sending to the remote storage.
          write_buffer_size:
            type: int
            doc: WriteBufferSize for HTTP client. See http.Transport.WriteBufferSize. Default is 0.
        doc: Config defines configuration for Remote Write exporter.
    prometheusremotewrite:
      type: prometheusremotewrite
      signals:
        - metrics
      stability:
        metrics: beta
      module: github.com/open-telemetry/opentelemetry-collector-contrib/exporter/prometheusremotewriteexporter
      deprecated: renamed to "prometheus_remote_write" upstream; the old name still resolves for now
      aliasOf: prometheus_remote_write
      fields:
        type: map
        children:
          add_metric_suffixes:
            type: bool
            doc: 'AddMetricSuffixes controls whether unit and type suffixes are added to metrics on export Deprecated: Use TranslationStrategy instead. It will be removed in v0.153.0.'
          auth:
            type: map
            children:
              authenticator:
                type: map
                open: true
                doc: AuthenticatorID specifies the name of the extension to use in order to authenticate the incoming data point.
            doc: Auth configuration for outgoing HTTP calls.
          compression:
            type: string
            doc: The compression key for supported compression types within collector.
          compression_params:
            type: map
            children:
              level:
                type: int
            doc: Advanced configuration options for the Compression
          cookies:
            type: map
            open: true
            doc: Cookies configures the cookie management of the HTTP client.
          disable_keep_alives:
            type: bool
            doc: 'DisableKeepAlives, if true, disables HTTP keep-alives and will only use the connection to the server for a single HTTP request. WARNING: enabling this option can result in significant overhead establishing a new HTTP(S) connection for every request. Before enabling this option please consider whether changes to idle connection settings can achieve your goal.'
          disable_scope_info:
            type: bool
            doc: DisableScopeInfo allows disabling the export of the scope info labels
          enabled:
            type: bool
            doc: Enabled indicates whether to not retry sending batches in case of export failure.
          endpoint:
            type: string
            doc: 'The target URL to send data to (e.g.: http://some.url:9411/v1/traces).'
          external_labels:
            type: map
            open: true
            doc: ExternalLabels defines a map of label keys and values that are allowed to start with reserved prefix "__"
          force_attempt_http2:
            type: bool
            doc: 'Enabling ForceAttemptHTTP2 forces the HTTP transport to use the HTTP/2 protocol. By default, this is set to true. NOTE: HTTP/2 does not support settings such as MaxConnsPerHost, MaxIdleConnsPerHost and MaxIdleConns.'
          headers:
            children:
              item:
                type: map
                children:
                  name:
                    type: string
                  value:
                    type: string
                    doc: String alias that is marshaled and printed in an opaque way. To recover the original value, cast it to a string.
                doc: Pair is an element of a MapList, and consists of a name and an opaque value.
            doc: Additional headers attached to each HTTP request sent by the client. Existing header values are overwritten if collision happens. Header values are opaque since they may be sensitive.
          http2_ping_timeout:
            type: duration
            doc: HTTP2PingTimeout if there's no response to the ping within the configured value, the connection will be closed. If not set or set to 0, it defaults to 15s.
          http2_read_idle_timeout:
            type: duration
            doc: This is needed in case you run into https://github.com/golang/go/issues/59690 https://github.com/golang/go/issues/36026 HTTP2ReadIdleTimeout if the connection has been idle for the configured value send a ping frame for health check 0s means no health check will be performed.
          idle_conn_timeout:
            type: duration
            doc: IdleConnTimeout is the maximum amount of time a connection will remain open before closing itself. By default, it is set to 90 seconds.
          initial_interval:
            type: duration
            doc: InitialInterval the time to wait after the first failure before retrying.
          max_batch_request_parallelism:
            type: int
            doc: maximum amount of parallel requests to do when handling large batch request
          max_batch_size_bytes:
            type: int
            doc: maximum size in bytes of time series batch sent to remote storage
          max_conns_per_host:
            type: int
            doc: MaxConnsPerHost limits the total number of connections per host, including connections in the dialing, active, and idle states. Default is 0 (unlimited).
          max_elapsed_time:
            type: duration
            doc: MaxElapsedTime is the maximum amount of time (including retries) spent trying to send a request/batch. Once this value is reached, the data is discarded. If set to 0, the retries are never stopped.
          max_idle_conns:
            type: int
            doc: MaxIdleConns is used to set a limit to the maximum idle HTTP connections the client can keep open. By default, it is set to 100. Zero means no limit.
          max_idle_conns_per_host:
            type: int
            doc: MaxIdleConnsPerHost is used to set a limit to the maximum idle HTTP connections the host can keep open. If zero, [net/http.DefaultMaxIdleConnsPerHost] is used.
          max_interval:
            type: duration
            doc: MaxInterval is the upper bound on backoff interval. Once this value is reached the delay between consecutive retries will always be `MaxInterval`.
          middlewares:
            type: list
            children:
              item:
                type: map
                children:
                  id:
                    type: map
                    open: true
                    doc: ID specifies the name of the extension to use.
                doc: Middleware defines the extension ID for a middleware component.
            doc: Middlewares are used to add custom functionality to the HTTP client. Middleware handlers are called in the order they appear in this list, with the first middleware becoming the outermost handler.
          multiplier:
            type: float
            doc: Multiplier is the value multiplied by the backoff interval bounds
          namespace:
            type: string
            doc: 'prefix attached to each exported metric name See: https://prometheus.io/docs/practices/naming/#metric-names'
          protobuf_message:
            type: map
            open: true
            doc: RemoteWriteProtoMsg controls whether prometheus remote write v1 or v2 is sent.
          proxy_url:
            type: string
            doc: ProxyURL setting for the collector
          randomization_factor:
            type: float
            doc: RandomizationFactor is a random factor used to calculate next backoffs Randomized interval = RetryInterval * (1 ± RandomizationFactor)
          read_buffer_size:
            type: int
            doc: ReadBufferSize for HTTP client. See http.Transport.ReadBufferSize. Default is 0.
          remote_write_queue:
            type: map
            children:
              enabled:
                type: bool
                doc: Enabled if false the queue is not enabled, the export requests are executed synchronously.
              num_consumers:
                type: int
                doc: NumWorkers configures the number of workers used by the collector to fan out remote write requests.
              queue_size:
                type: int
                doc: QueueSize is the maximum number of OTLP metric batches allowed in the queue at a given time. Ignored if Enabled is false.
            doc: QueueConfig allows users to fine tune the queues that handle outgoing requests.
          resource_to_telemetry_conversion:
            type: map
            children:
              enabled:
                type: bool
                doc: Enabled indicates whether to convert resource attributes to telemetry attributes. Default is `false`.
              exclude_service_attributes:
                type: bool
                doc: ExcludeServiceAttributes indicates whether to exclude `service.name`, `service.instance.id` and `service.namespace` resource attributes from being converted to metric attributes. Default is `false`. When set to `true`, these attributes will not be added to metric labels since they are already mapped to Prometheus `job` and `instance` labels respectively.
            doc: ResourceToTelemetrySettings is the option for converting resource attributes to telemetry attributes. "Enabled" - A boolean field to enable/disable this option. Default is `false`. If enabled, all the resource attributes will be converted to metric labels by default. "ExcludeServiceAttributes" - If set to `true`, the `service.name`, `service.instance.id` and `service.namespace` resource attributes, which are already converted to `job` and `instance` labels respectively, will be excluded from the final metrics.
          retry_on_failure:
            type: map
            children:
              enabled:
                type: bool
              initial_interval:
                type: duration
              max_elapsed_time:
                type: duration
              max_interval:
                type: duration
              multiplier:
                type: float
              randomization_factor:
                type: float
          send_metadata:
            type: bool
            doc: SendMetadata controls whether prometheus metadata will be generated and sent, this option is ignored when using PRW 2.0, which always includes metadata.
          target_info:
            type: map
            children:
              enabled:
                type: bool
                doc: Enabled if false the target_info metric is not generated by the exporter
            doc: TargetInfo allows customizing the target_info metric
          timeout:
            type: duration
            doc: Timeout is the timeout for every attempt to send data to the backend. A zero timeout means no timeout.
          tls:
            type: map
            children:
              ca_file:
                type: string
                doc: Path to the CA cert. For a client this verifies the server certificate. For a server this verifies client certificates. If empty uses system root CA. (optional)
              ca_pem:
                type: string
                doc: In memory PEM encoded cert. (optional)
              cert_file:
                type: string
                doc: Path to the TLS cert to use for TLS required connections. (optional)
              cert_pem:
                type: string
                doc: In memory PEM encoded TLS cert to use for TLS required connections. (optional)
              cipher_suites:
                type: list
                children:
                  item:
                    type: string
                doc: CipherSuites is a list of TLS cipher suites that the TLS transport can use. If left blank, a safe default list is used. See https://go.dev/src/crypto/tls/cipher_suites.go for a list of supported cipher suites.
              curve_preferences:
                type: list
                children:
                  item:
                    type: string
                doc: contains the elliptic curves that will be used in an ECDHE handshake, in preference order Defaults to empty list and "crypto/tls" defaults are used, internally.
              include_insecure_cipher_suites:
                type: bool
              include_system_ca_certs_pool:
                type: bool
                doc: If true, load system CA certificates pool in addition to the certificates configured in this struct.
              insecure:
                type: bool
                doc: In gRPC and HTTP when set to true, this is used to disable the client transport security. See https://godoc.org/google.golang.org/grpc#WithInsecure for gRPC. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional, default false)
              insecure_skip_verify:
                type: bool
                doc: InsecureSkipVerify will enable TLS but not verify the certificate.
              key_file:
                type: string
                doc: Path to the TLS key to use for TLS required connections. (optional)
              key_pem:
                type: string
                doc: In memory PEM encoded TLS key to use for TLS required connections. (optional)
              max_version:
                type: string
                doc: MaxVersion sets the maximum TLS version that is acceptable. If not set, refer to crypto/tls for defaults. (optional)
              min_version:
                type: string
                doc: MinVersion sets the minimum TLS version that is acceptable. If not set, TLS 1.2 will be used. (optional)
              reload_interval:
                type: duration
                doc: ReloadInterval specifies the duration after which the certificate will be reloaded If not set, it will never be reloaded (optional)
              server_name_override:
                type: string
                doc: ServerName requested by client for virtual hosting. This sets the ServerName in the TLSConfig. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional)
              tpm:
                type: map
                children:
                  auth:
                    type: string
                  enabled:
                    type: bool
                  owner_auth:
                    type: string
                  path:
                    type: string
                    doc: The path to the TPM device or Unix domain socket. For instance /dev/tpm0 or /dev/tpmrm0.
                doc: Trusted platform module configuration
            doc: TLS struct exposes TLS client configuration.
          translation_strategy:
            type: string
            doc: TranslationStrategy controls how OTLP metric and attribute names are translated into Prometheus metric and label names. When set, this takes precedence over AddMetricSuffixes.
          wal:
            type: map
            children:
              buffer_size:
                type: int
              directory:
                type: string
              lag_record_frequency:
                type: duration
              truncate_frequency:
                type: duration
            doc: WAL enables persisting metrics to a write-ahead-log before sending to the remote storage.
          write_buffer_size:
            type: int
            doc: WriteBufferSize for HTTP client. See http.Transport.WriteBufferSize. Default is 0.
        doc: Config defines configuration for Remote Write exporter.
    zipkin:
      type: zipkin
      signals:
        - traces
      stability:
        traces: beta
      module: github.com/open-telemetry/opentelemetry-collector-contrib/exporter/zipkinexporter
      fields:
        type: map
        children:
          auth:
            type: map
            children:
              authenticator:
                type: map
                open: true
                doc: AuthenticatorID specifies the name of the extension to use in order to authenticate the incoming data point.
            doc: Auth configuration for outgoing HTTP calls.
          compression:
            type: string
            doc: The compression key for supported compression types within collector.
          compression_params:
            type: map
            children:
              level:
                type: int
            doc: Advanced configuration options for the Compression
          cookies:
            type: map
            open: true
            doc: Cookies configures the cookie management of the HTTP client.
          default_service_name:
            type: string
          disable_keep_alives:
            type: bool
            doc: 'DisableKeepAlives, if true, disables HTTP keep-alives and will only use the connection to the server for a single HTTP request. WARNING: enabling this option can result in significant overhead establishing a new HTTP(S) connection for every request. Before enabling this option please consider whether changes to idle connection settings can achieve your goal.'
          enabled:
            type: bool
            doc: Enabled indicates whether to not retry sending batches in case of export failure.
          endpoint:
            type: string
            doc: 'The target URL to send data to (e.g.: http://some.url:9411/v1/traces).'
          force_attempt_http2:
            type: bool
            doc: 'Enabling ForceAttemptHTTP2 forces the HTTP transport to use the HTTP/2 protocol. By default, this is set to true. NOTE: HTTP/2 does not support settings such as MaxConnsPerHost, MaxIdleConnsPerHost and MaxIdleConns.'
          format:
            type: string
          headers:
            children:
              item:
                type: map
                children:
                  name:
                    type: string
                  value:
                    type: string
                    doc: String alias that is marshaled and printed in an opaque way. To recover the original value, cast it to a string.
                doc: Pair is an element of a MapList, and consists of a name and an opaque value.
            doc: Additional headers attached to each HTTP request sent by the client. Existing header values are overwritten if collision happens. Header values are opaque since they may be sensitive.
          http2_ping_timeout:
            type: duration
            doc: HTTP2PingTimeout if there's no response to the ping within the configured value, the connection will be closed. If not set or set to 0, it defaults to 15s.
          http2_read_idle_timeout:
            type: duration
            doc: This is needed in case you run into https://github.com/golang/go/issues/59690 https://github.com/golang/go/issues/36026 HTTP2ReadIdleTimeout if the connection has been idle for the configured value send a ping frame for health check 0s means no health check will be performed.
          idle_conn_timeout:
            type: duration
            doc: IdleConnTimeout is the maximum amount of time a connection will remain open before closing itself. By default, it is set to 90 seconds.
          initial_interval:
            type: duration
            doc: InitialInterval the time to wait after the first failure before retrying.
          max_conns_per_host:
            type: int
            doc: MaxConnsPerHost limits the total number of connections per host, including connections in the dialing, active, and idle states. Default is 0 (unlimited).
          max_elapsed_time:
            type: duration
            doc: MaxElapsedTime is the maximum amount of time (including retries) spent trying to send a request/batch. Once this value is reached, the data is discarded. If set to 0, the retries are never stopped.
          max_idle_conns:
            type: int
            doc: MaxIdleConns is used to set a limit to the maximum idle HTTP connections the client can keep open. By default, it is set to 100. Zero means no limit.
          max_idle_conns_per_host:
            type: int
            doc: MaxIdleConnsPerHost is used to set a limit to the maximum idle HTTP connections the host can keep open. If zero, [net/http.DefaultMaxIdleConnsPerHost] is used.
          max_interval:
            type: duration
            doc: MaxInterval is the upper bound on backoff interval. Once this value is reached the delay between consecutive retries will always be `MaxInterval`.
          middlewares:
            type: list
            children:
              item:
                type: map
                children:
                  id:
                    type: map
                    open: true
                    doc: ID specifies the name of the extension to use.
                doc: Middleware defines the extension ID for a middleware component.
            doc: Middlewares are used to add custom functionality to the HTTP client. Middleware handlers are called in the order they appear in this list, with the first middleware becoming the outermost handler.
          multiplier:
            type: float
            doc: Multiplier is the value multiplied by the backoff interval bounds
          proxy_url:
            type: string
            doc: ProxyURL setting for the collector
          randomization_factor:
            type: float
            doc: RandomizationFactor is a random factor used to calculate next backoffs Randomized interval = RetryInterval * (1 ± RandomizationFactor)
          read_buffer_size:
            type: int
            doc: ReadBufferSize for HTTP client. See http.Transport.ReadBufferSize. Default is 0.
          retry_on_failure:
            type: map
            children:
              enabled:
                type: bool
              initial_interval:
                type: duration
              max_elapsed_time:
                type: duration
              max_interval:
                type: duration
              multiplier:
                type: float
              randomization_factor:
                type: float
          sending_queue:
            type: map
            children:
              batch:
                type: map
                children:
                  flush_timeout:
                    type: duration
                    doc: FlushTimeout sets the time after which a batch will be sent regardless of its size.
                  max_size:
                    type: int
                    doc: MaxSize defines the configuration for the maximum size of a batch.
                  min_size:
                    type: int
                    doc: MinSize defines the configuration for the minimum size of a batch.
                  partition:
                    type: map
                    children:
                      metadata_keys:
                        type: list
                        children:
                          item:
                            type: string
                        doc: MetadataKeys is a list of client.Metadata keys that will be used to partition the data into batches. If this setting is empty, a single batcher instance will be used. When this setting is not empty, one batcher will be used per distinct combination of values for the listed metadata keys. Empty value and unset metadata are treated as distinct cases. Entries are case-insensitive. Duplicated entries will trigger a validation error.
                    doc: Partition defines the partitioning of the batches configuration.
                  sizer:
                    type: map
                    open: true
                    doc: Sizer determines the type of size measurement used by the batch. If not configured, use the same configuration as the queue. It accepts "requests", "items", or "bytes".
                doc: BatchConfig it configures how the requests are consumed from the queue and batch together during consumption.
              block_on_overflow:
                type: bool
                doc: BlockOnOverflow determines the behavior when the component's TotalSize limit is reached. If true, the component will wait for space; otherwise, operations will immediately return a retryable error.
              enabled:
                type: bool
                doc: Enabled indicates whether to not enqueue and batch before exporting.
              num_consumers:
                type: int
                doc: NumConsumers is the maximum number of concurrent consumers from the queue. This applies across all different optional configurations from above (e.g. wait_for_result, block_on_overflow, storage, etc.).
              queue_size:
                type: int
                doc: QueueSize represents the maximum data size allowed for concurrent storage and processing.
              sizer:
                type: map
                open: true
                doc: Sizer determines the type of size measurement used by this component. It accepts "requests", "items", or "bytes".
              storage:
                type: map
                open: true
                doc: 'StorageID if not empty, enables the persistent storage and uses the component specified as a storage extension for the persistent queue. TODO: This will be changed to Optional when available. See https://github.com/open-telemetry/opentelemetry-collector/issues/13822'
              wait_for_result:
                type: bool
                doc: WaitForResult determines if incoming requests are blocked until the request is processed or not. Currently, this option is not available when persistent queue is configured using the storage configuration.
            doc: QueueBatchConfig defines configuration for queueing and batching for the exporter.
          timeout:
            type: duration
            doc: Timeout parameter configures `http.Client.Timeout`. Default is 0 (unlimited).
          tls:
            type: map
            children:
              ca_file:
                type: string
                doc: Path to the CA cert. For a client this verifies the server certificate. For a server this verifies client certificates. If empty uses system root CA. (optional)
              ca_pem:
                type: string
                doc: In memory PEM encoded cert. (optional)
              cert_file:
                type: string
                doc: Path to the TLS cert to use for TLS required connections. (optional)
              cert_pem:
                type: string
                doc: In memory PEM encoded TLS cert to use for TLS required connections. (optional)
              cipher_suites:
                type: list
                children:
                  item:
                    type: string
                doc: CipherSuites is a list of TLS cipher suites that the TLS transport can use. If left blank, a safe default list is used. See https://go.dev/src/crypto/tls/cipher_suites.go for a list of supported cipher suites.
              curve_preferences:
                type: list
                children:
                  item:
                    type: string
                doc: contains the elliptic curves that will be used in an ECDHE handshake, in preference order Defaults to empty list and "crypto/tls" defaults are used, internally.
              include_insecure_cipher_suites:
                type: bool
              include_system_ca_certs_pool:
                type: bool
                doc: If true, load system CA certificates pool in addition to the certificates configured in this struct.
              insecure:
                type: bool
                doc: In gRPC and HTTP when set to true, this is used to disable the client transport security. See https://godoc.org/google.golang.org/grpc#WithInsecure for gRPC. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional, default false)
              insecure_skip_verify:
                type: bool
                doc: InsecureSkipVerify will enable TLS but not verify the certificate.
              key_file:
                type: string
                doc: Path to the TLS key to use for TLS required connections. (optional)
              key_pem:
                type: string
                doc: In memory PEM encoded TLS key to use for TLS required connections. (optional)
              max_version:
                type: string
                doc: MaxVersion sets the maximum TLS version that is acceptable. If not set, refer to crypto/tls for defaults. (optional)
              min_version:
                type: string
                doc: MinVersion sets the minimum TLS version that is acceptable. If not set, TLS 1.2 will be used. (optional)
              reload_interval:
                type: duration
                doc: ReloadInterval specifies the duration after which the certificate will be reloaded If not set, it will never be reloaded (optional)
              server_name_override:
                type: string
                doc: ServerName requested by client for virtual hosting. This sets the ServerName in the TLSConfig. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional)
              tpm:
                type: map
                children:
                  auth:
                    type: string
                  enabled:
                    type: bool
                  owner_auth:
                    type: string
                  path:
                    type: string
                    doc: The path to the TPM device or Unix domain socket. For instance /dev/tpm0 or /dev/tpmrm0.
                doc: Trusted platform module configuration
            doc: TLS struct exposes TLS client configuration.
          write_buffer_size:
            type: int
            doc: WriteBufferSize for HTTP client. See http.Transport.WriteBufferSize. Default is 0.
        doc: Config defines configuration settings for the Zipkin exporter.
  extension:
    health_check:
      type: health_check
      stability:
        extension: alpha
      module: github.com/open-telemetry/opentelemetry-collector-contrib/extension/healthcheckextension
      fields:
        type: map
        children:
          auth:
            type: map
            children:
              authenticator:
                type: map
                open: true
              request_params:
                type: list
                children:
                  item:
                    type: string
          check_collector_pipeline:
            type: map
            children:
              enabled:
                type: bool
              exporter_failure_threshold:
                type: int
              interval:
                type: string
          component_health:
            type: map
            children:
              include_permanent_errors:
                type: bool
              include_recoverable_errors:
                type: bool
              recovery_duration:
                type: duration
          compression_algorithms:
            type: list
            children:
              item:
                type: string
          cors:
            type: map
            children:
              allowed_headers:
                type: list
                children:
                  item:
                    type: string
              allowed_origins:
                type: list
                children:
                  item:
                    type: string
              exposed_headers:
                type: list
                children:
                  item:
                    type: string
              max_age:
                type: int
          dialer:
            type: map
            children:
              timeout:
                type: duration
          endpoint:
            type: string
          grpc:
            type: map
            children:
              auth:
                type: map
                children:
                  authenticator:
                    type: map
                    open: true
              dialer:
                type: map
                children:
                  timeout:
                    type: duration
              endpoint:
                type: string
              include_metadata:
                type: bool
              keepalive:
                type: map
                children:
                  enforcement_policy:
                    type: map
                    children:
                      min_time:
                        type: duration
                      permit_without_stream:
                        type: bool
                  server_parameters:
                    type: map
                    children:
                      max_connection_age:
                        type: duration
                      max_connection_age_grace:
                        type: duration
                      max_connection_idle:
                        type: duration
                      time:
                        type: duration
                      timeout:
                        type: duration
              max_concurrent_streams:
                type: int
              max_recv_msg_size_mib:
                type: int
              middlewares:
                type: list
                children:
                  item:
                    type: map
                    children:
                      id:
                        type: map
                        open: true
              read_buffer_size:
                type: int
              tls:
                type: map
                children:
                  ca_file:
                    type: string
                  ca_pem:
                    type: string
                  cert_file:
                    type: string
                  cert_pem:
                    type: string
                  cipher_suites:
                    type: list
                    children:
                      item:
                        type: string
                  client_ca_file:
                    type: string
                  client_ca_file_reload:
                    type: bool
                  curve_preferences:
                    type: list
                    children:
                      item:
                        type: string
                  include_insecure_cipher_suites:
                    type: bool
                  include_system_ca_certs_pool:
                    type: bool
                  key_file:
                    type: string
                  key_pem:
                    type: string
                  max_version:
                    type: string
                  min_version:
                    type: string
                  reload_interval:
                    type: duration
                  tpm:
                    type: map
                    children:
                      auth:
                        type: string
                      enabled:
                        type: bool
                      owner_auth:
                        type: string
                      path:
                        type: string
              transport:
                type: string
              write_buffer_size:
                type: int
          http:
            type: map
            children:
              auth:
                type: map
                children:
                  authenticator:
                    type: map
                    open: true
                  request_params:
                    type: list
                    children:
                      item:
                        type: string
              compression_algorithms:
                type: list
                children:
                  item:
                    type: string
              config:
                type: map
                children:
                  enabled:
                    type: bool
                  include_attributes:
                    type: bool
                  path:
                    type: string
              cors:
                type: map
                children:
                  allowed_headers:
                    type: list
                    children:
                      item:
                        type: string
                  allowed_origins:
                    type: list
                    children:
                      item:
                        type: string
                  exposed_headers:
                    type: list
                    children:
                      item:
                        type: string
                  max_age:
                    type: int
              dialer:
                type: map
                children:
                  timeout:
                    type: duration
              endpoint:
                type: string
              idle_timeout:
                type: duration
              include_metadata:
                type: bool
              keep_alives_enabled:
                type: bool
              max_request_body_size:
                type: int
              middlewares:
                type: list
                children:
                  item:
                    type: map
                    children:
                      id:
                        type: map
                        open: true
              read_header_timeout:
                type: duration
              read_timeout:
                type: duration
              response_headers: {}
              status:
                type: map
                children:
                  enabled:
                    type: bool
                  include_attributes:
                    type: bool
                  path:
                    type: string
              tls:
                type: map
                children:
                  ca_file:
                    type: string
                  ca_pem:
                    type: string
                  cert_file:
                    type: string
                  cert_pem:
                    type: string
                  cipher_suites:
                    type: list
                    children:
                      item:
                        type: string
                  client_ca_file:
                    type: string
                  client_ca_file_reload:
                    type: bool
                  curve_preferences:
                    type: list
                    children:
                      item:
                        type: string
                  include_insecure_cipher_suites:
                    type: bool
                  include_system_ca_certs_pool:
                    type: bool
                  key_file:
                    type: string
                  key_pem:
                    type: string
                  max_version:
                    type: string
                  min_version:
                    type: string
                  reload_interval:
                    type: duration
                  tpm:
                    type: map
                    children:
                      auth:
                        type: string
                      enabled:
                        type: bool
                      owner_auth:
                        type: string
                      path:
                        type: string
              transport:
                type: string
              write_timeout:
                type: duration
          idle_timeout:
            type: duration
          include_metadata:
            type: bool
          keep_alives_enabled:
            type: bool
          max_request_body_size:
            type: int
          middlewares:
            type: list
            children:
              item:
                type: map
                children:
                  id:
                    type: map
                    open: true
          path:
            type: string
          read_header_timeout:
            type: duration
          read_timeout:
            type: duration
          response_body:
            type: map
            children:
              healthy:
                type: string
              unhealthy:
                type: string
          response_headers: {}
          tls:
            type: map
            children:
              ca_file:
                type: string
              ca_pem:
                type: string
              cert_file:
                type: string
              cert_pem:
                type: string
              cipher_suites:
                type: list
                children:
                  item:
                    type: string
              client_ca_file:
                type: string
              client_ca_file_reload:
                type: bool
              curve_preferences:
                type: list
                children:
                  item:
                    type: string
              include_insecure_cipher_suites:
                type: bool
              include_system_ca_certs_pool:
                type: bool
              key_file:
                type: string
              key_pem:
                type: string
              max_version:
                type: string
              min_version:
                type: string
              reload_interval:
                type: duration
              tpm:
                type: map
                children:
                  auth:
                    type: string
                  enabled:
                    type: bool
                  owner_auth:
                    type: string
                  path:
                    type: string
          transport:
            type: string
          use_v2:
            type: bool
          write_timeout:
            type: duration
    memory_limiter:
      type: memory_limiter
      stability:
        extension: development
      module: go.opentelemetry.io/collector/extension/memorylimiterextension
      fields:
        type: map
        children:
          check_interval:
            type: duration
            doc: CheckInterval is the time between measurements of memory usage for the purposes of avoiding going over the limits. Defaults to zero, so no checks will be performed.
          limit_mib:
            type: int
            doc: MemoryLimitMiB is the maximum amount of memory, in MiB, targeted to be allocated by the process.
          limit_percentage:
            type: int
            doc: MemoryLimitPercentage is the maximum amount of memory, in %, targeted to be allocated by the process. The fixed memory settings MemoryLimitMiB has a higher precedence.
          max_gc_interval_when_hard_limited:
            type: duration
            doc: MaxGCIntervalWhenHardLimited caps the exponential backoff between forced GC calls in hard-limited mode. Same semantics as MaxGCIntervalWhenSoftLimited but applies to the hard-limit path. Set to 0 to disable the exponential backoff on this path.
          max_gc_interval_when_soft_limited:
            type: duration
            doc: MaxGCIntervalWhenSoftLimited caps the exponential backoff between forced GC calls in soft-limited mode. When a forced GC fails to reclaim memory (e.g., held by live references in exporter queues during a downstream outage), the interval doubles starting from min_gc_interval_when_soft_limited (or 95% of check_interval, whichever is larger) up to this cap. It resets to zero whenever a GC is effective or memory drops on its own. Set to 0 to disable the exponential backoff on this path; the interval will not grow beyond the configured min.
          min_gc_interval_when_hard_limited:
            type: duration
            doc: MinGCIntervalWhenHardLimited minimum interval between forced GC when in hard (=limit_mib) limited mode. Zero value means no minimum interval. GCs is a CPU-heavy operation and executing it too frequently may affect the recovery capabilities of the collector.
          min_gc_interval_when_soft_limited:
            type: duration
            doc: MinGCIntervalWhenSoftLimited minimum interval between forced GC when in soft (=limit_mib - spike_limit_mib) limited mode. Zero value means no minimum interval. GCs is a CPU-heavy operation and executing it too frequently may affect the recovery capabilities of the collector.
          spike_limit_mib:
            type: int
            doc: MemorySpikeLimitMiB is the maximum, in MiB, spike expected between the measurements of memory usage.
          spike_limit_percentage:
            type: int
            doc: MemorySpikePercentage is the maximum, in percents against the total memory, spike expected between the measurements of memory usage.
        doc: Config defines configuration for memory memoryLimiter processor.
    pprof:
      type: pprof
      stability:
        extension: beta
      module: github.com/open-telemetry/opentelemetry-collector-contrib/extension/pprofextension
      fields:
        type: map
        children:
          block_profile_fraction:
            type: int
            doc: Fraction of blocking events that are profiled. A value <= 0 disables profiling. See https://golang.org/pkg/runtime/#SetBlockProfileRate for details.
          dialer:
            type: map
            children:
              timeout:
                type: duration
                doc: Timeout is the maximum amount of time a dial will wait for a connect to complete. The default is no timeout.
            doc: DialerConfig contains options for connecting to an address.
          endpoint:
            type: string
            doc: Endpoint configures the address for this network connection. The address has the form "host:port". The host must be a literal IP address, or a host name that can be resolved to IP addresses. The port must be a literal port number or a service name. If the host is a literal IPv6 address it must be enclosed in square brackets, as in "[2001:db8::1]:80" or "[fe80::1%zone]:80". The zone specifies the scope of the literal IPv6 address as defined in RFC 4007.
          mutex_profile_fraction:
            type: int
            doc: Fraction of mutex contention events that are profiled. A value <= 0 disables profiling. See https://golang.org/pkg/runtime/#SetMutexProfileFraction for details.
          save_to_file:
            type: string
            doc: Optional file name to save the CPU profile to. The profiling starts when the Collector starts and is saved to the file when the Collector is terminated.
        doc: Config has the configuration for the extension enabling the golang net/http/pprof (Performance Profiler) extension.
    zpages:
      type: zpages
      stability:
        extension: beta
      module: go.opentelemetry.io/collector/extension/zpagesextension
      fields:
        type: map
        children:
          auth:
            type: map
            children:
              authenticator:
                type: map
                open: true
                doc: AuthenticatorID specifies the name of the extension to use in order to authenticate the incoming data point.
              request_params:
                type: list
                children:
                  item:
                    type: string
                doc: RequestParameters is a list of parameters that should be extracted from the request and added to the context. When a parameter is found in both the query string and the header, the value from the query string will be used.
            doc: Auth for this receiver
          compression_algorithms:
            type: list
            children:
              item:
                type: string
            doc: 'CompressionAlgorithms configures the list of compression algorithms the server can accept. Default: ["", "gzip", "zstd", "zlib", "snappy", "deflate"]'
          cors:
            type: map
            children:
              allowed_headers:
                type: list
                children:
                  item:
                    type: string
                doc: AllowedHeaders sets what headers will be allowed in CORS requests. The Accept, Accept-Language, Content-Type, and Content-Language headers are implicitly allowed. If no headers are listed, X-Requested-With will also be accepted by default. Include "*" to allow any request header.
              allowed_origins:
                type: list
                children:
                  item:
                    type: string
                doc: AllowedOrigins sets the allowed values of the Origin header for HTTP/JSON requests to an OTLP receiver. An origin may contain a wildcard (*) to replace 0 or more characters (e.g., "http://*.domain.com", or "*" to allow any origin).
              exposed_headers:
                type: list
                children:
                  item:
                    type: string
                doc: ExposedHeaders sets the value of the Access-Control-Expose-Headers response header, indicating which headers are safe to expose to the API of a CORS response.
              max_age:
                type: int
                doc: MaxAge sets the value of the Access-Control-Max-Age response header. Set it to the number of seconds that browsers should cache a CORS preflight response for.
            doc: CORS configures the server for HTTP cross-origin resource sharing (CORS).
          dialer:
            type: map
            children:
              timeout:
                type: duration
          endpoint:
            type: string
            doc: Endpoint configures the listening address for the server.
          expvar:
            type: map
            children:
              enabled:
                type: bool
                doc: Enabled indicates whether to enable expvar service. (default = false)
            doc: ExpvarConfig has the configuration for the expvar service.
          idle_timeout:
            type: duration
            doc: IdleTimeout is the maximum amount of time to wait for the next request when keep-alives are enabled. If IdleTimeout is zero, the value of ReadTimeout is used. If both are zero, there is no timeout.
          include_metadata:
            type: bool
            doc: IncludeMetadata propagates the client metadata from the incoming requests to the downstream consumers
          keep_alives_enabled:
            type: bool
            doc: KeepAlivesEnabled controls whether HTTP keep-alives are enabled. By default, keep-alives are always enabled. Only very resource-constrained environments should disable them.
          max_request_body_size:
            type: int
            doc: 'MaxRequestBodySize sets the maximum request body size in bytes. Default: 20MiB.'
          middlewares:
            type: list
            children:
              item:
                type: map
                children:
                  id:
                    type: map
                    open: true
                    doc: ID specifies the name of the extension to use.
                doc: Middleware defines the extension ID for a middleware component.
            doc: Middlewares are used to add custom functionality to the HTTP server. Middleware handlers are called in the order they appear in this list, with the first middleware becoming the outermost handler.
          read_header_timeout:
            type: duration
            doc: ReadHeaderTimeout is the amount of time allowed to read request headers. The connection's read deadline is reset after reading the headers and the Handler can decide what is considered too slow for the body. If ReadHeaderTimeout is zero, the value of ReadTimeout is used. If both are zero, there is no timeout.
          read_timeout:
            type: duration
            doc: ReadTimeout is the maximum duration for reading the entire request, including the body. A zero or negative value means there will be no timeout. Because ReadTimeout does not let Handlers make per-request decisions on each request body's acceptable deadline or upload rate, most users will prefer to use ReadHeaderTimeout. It is valid to use them both.
          response_headers:
            children:
              item:
                type: map
                children:
                  name:
                    type: string
                  value:
                    type: string
                    doc: String alias that is marshaled and printed in an opaque way. To recover the original value, cast it to a string.
                doc: Pair is an element of a MapList, and consists of a name and an opaque value.
            doc: Additional headers attached to each HTTP response sent to the client. Header values are opaque since they may be sensitive.
          tls:
            type: map
            children:
              ca_file:
                type: string
                doc: Path to the CA cert. For a client this verifies the server certificate. For a server this verifies client certificates. If empty uses system root CA. (optional)
              ca_pem:
                type: string
                doc: In memory PEM encoded cert. (optional)
              cert_file:
                type: string
                doc: Path to the TLS cert to use for TLS required connections. (optional)
              cert_pem:
                type: string
                doc: In memory PEM encoded TLS cert to use for TLS required connections. (optional)
              cipher_suites:
                type: list
                children:
                  item:
                    type: string
                doc: CipherSuites is a list of TLS cipher suites that the TLS transport can use. If left blank, a safe default list is used. See https://go.dev/src/crypto/tls/cipher_suites.go for a list of supported cipher suites.
              client_ca_file:
                type: string
                doc: Path to the TLS cert to use by the server to verify a client certificate. (optional) This sets the ClientCAs and ClientAuth to RequireAndVerifyClientCert in the TLSConfig. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional)
              client_ca_file_reload:
                type: bool
                doc: Reload the ClientCAs file when it is modified (optional, default false)
              curve_preferences:
                type: list
                children:
                  item:
                    type: string
                doc: contains the elliptic curves that will be used in an ECDHE handshake, in preference order Defaults to empty list and "crypto/tls" defaults are used, internally.
              include_insecure_cipher_suites:
                type: bool
              include_system_ca_certs_pool:
                type: bool
                doc: If true, load system CA certificates pool in addition to the certificates configured in this struct.
              key_file:
                type: string
                doc: Path to the TLS key to use for TLS required connections. (optional)
              key_pem:
                type: string
                doc: In memory PEM encoded TLS key to use for TLS required connections. (optional)
              max_version:
                type: string
                doc: MaxVersion sets the maximum TLS version that is acceptable. If not set, refer to crypto/tls for defaults. (optional)
              min_version:
                type: string
                doc: MinVersion sets the minimum TLS version that is acceptable. If not set, TLS 1.2 will be used. (optional)
              reload_interval:
                type: duration
                doc: ReloadInterval specifies the duration after which the certificate will be reloaded If not set, it will never be reloaded (optional)
              tpm:
                type: map
                children:
                  auth:
                    type: string
                  enabled:
                    type: bool
                  owner_auth:
                    type: string
                  path:
                    type: string
                    doc: The path to the TPM device or Unix domain socket. For instance /dev/tpm0 or /dev/tpmrm0.
                doc: Trusted platform module configuration
            doc: TLS struct exposes TLS client configuration.
          transport:
            type: string
          write_timeout:
            type: duration
            doc: WriteTimeout is the maximum duration before timing out writes of the response. It is reset whenever a new request's header is read. Like ReadTimeout, it does not let Handlers make decisions on a per-request basis. A zero or negative value means there will be no timeout.
        doc: Config has the configuration for the extension enabling the zPages extension.
  processor:
    attributes:
      type: attributes
      signals:
        - traces
        - metrics
        - logs
      stability:
        logs: beta
        metrics: beta
        traces: beta
      module: github.com/open-telemetry/opentelemetry-collector-contrib/processor/attributesprocessor
      fields:
        type: map
        children:
          actions:
            type: list
            children:
              item:
                type: map
                children:
                  action:
                    type: string
                    doc: Action specifies the type of action to perform. The set of values are {INSERT, UPDATE, UPSERT, DELETE, HASH}. Both lower case and upper case are supported. INSERT -  Inserts the key/value to attributes when the key does not exist. No action is applied to attributes where the key already exists. Either Value, FromAttribute or FromContext must be set. UPDATE -  Updates an existing key with a value. No action is applied to attributes where the key does not exist. Either Value, FromAttribute or FromContext must be set. UPSERT -  Performs insert or update action depending on the attributes containing the key. The key/value is inserted to attributes that did not originally have the key. The key/value is updated for attributes where the key already existed. Either Value, FromAttribute or FromContext must be set. DELETE  - Deletes the attribute. If the key doesn't exist, no action is performed. HASH    - Calculates the SHA-1 hash of an existing value and overwrites the value with its SHA-1 hash result. If the feature gate `coreinternal.attraction.hash.sha256` is enabled, it uses SHA2-256 instead. EXTRACT - Extracts values using a regular expression rule from the input 'key' to target keys specified in the 'rule'. If a target key already exists, it will be overridden. CONVERT  - converts the type of an existing attribute, if convertable This is a required field.
                  converted_type:
                    type: string
                    doc: ConvertedType specifies the target type of an attribute to be converted If the key doesn't exist, no action is performed. If the value cannot be converted, the original value will be left as-is
                  default_value:
                    type: map
                    open: true
                    doc: DefaultValue specifies the value to use if Value/FromAttribute/FromContext doesn't provide a value (e.g., environment variable not set, attribute doesn't exist). Only used with INSERT, UPDATE, and UPSERT actions.
                  from_attribute:
                    type: string
                    doc: FromAttribute specifies the attribute to use to populate the value. If the attribute doesn't exist, no action is performed.
                  from_context:
                    type: string
                    doc: FromContext specifies the context value to use to populate the value. The values would be searched in client.Info.Metadata. If the key doesn't exist, no action is performed. If the key has multiple values the values will be joined with `;` separator.
                  key:
                    type: string
                    doc: Key specifies the attribute to act upon. This is a required field.
                  pattern:
                    type: string
                    doc: 'A regex pattern must be specified for the action EXTRACT. It uses the attribute specified by `key'' to extract values from The target keys are inferred based on the names of the matcher groups provided and the names will be inferred based on the values of the matcher group. Note: All subexpressions must have a name. Note: The value type of the source key must be a string. If it isn''t, no extraction will occur.'
                  value:
                    type: map
                    open: true
                    doc: Value specifies the value to populate for the key. The type of the value is inferred from the configuration.
                doc: ActionKeyValue specifies the attribute key to act upon.
            doc: Actions specifies the list of attributes to act on. The set of actions are {INSERT, UPDATE, UPSERT, DELETE, HASH, EXTRACT, CONVERT}. This is a required field.
          exclude:
            type: map
            children:
              attributes:
                type: list
                children:
                  item:
                    type: map
                    children:
                      key:
                        type: string
                        doc: Key specifies the attribute key.
                      value:
                        type: map
                        open: true
                        doc: Values specifies the value to match against. If it is not set, any value will match.
                    doc: Attribute specifies the attribute key and optional value to match against.
                doc: Attributes specifies the list of attributes to match against. All of these attributes must match for a match to occur. This is an optional field.
              libraries:
                type: list
                children:
                  item:
                    type: map
                    children:
                      name:
                        type: string
                      version:
                        type: string
                        doc: version match expected actual  match nil      <blank> yes nil      1       yes <blank>  <blank> yes <blank>  1       no 1        <blank> no 1        1       yes
                    doc: InstrumentationLibrary specifies the instrumentation library and optional version to match against.
                doc: Libraries specify the list of items to match the implementation library against. A match occurs if the span's implementation library matches at least one item in this list. This is an optional field.
              log_bodies:
                type: list
                children:
                  item:
                    type: string
                doc: LogBodies is a list of strings that the LogRecord's body field must match against.
              log_severity_number:
                type: map
                children:
                  match_undefined:
                    type: bool
                    doc: MatchUndefined controls whether logs with "undefined" severity matches. If this is true, entries with undefined severity will match.
                  min:
                    type: int
                    doc: Min is the lowest severity that may be matched. e.g. if this is plog.SeverityNumberInfo, INFO, WARN, ERROR, and FATAL logs will match.
                doc: LogSeverityNumber defines how to match against a log record's SeverityNumber, if defined.
              log_severity_texts:
                type: list
                children:
                  item:
                    type: string
                doc: LogSeverityTexts is a list of strings that the LogRecord's severity text field must match against.
              match_type:
                type: string
                doc: MatchType describes the type of pattern matching a FilterSet uses to filter strings.
              metric_names:
                type: list
                children:
                  item:
                    type: string
                doc: MetricNames is a list of strings to match metric name against. A match occurs if metric name matches at least one item in the list. This field is optional.
              regexp:
                type: map
                children:
                  cacheenabled:
                    type: bool
                    doc: CacheEnabled determines whether match results are LRU cached to make subsequent matches faster. Cache size is unlimited unless CacheMaxNumEntries is also specified.
                  cachemaxnumentries:
                    type: int
                    doc: CacheMaxNumEntries is the max number of entries of the LRU cache that stores match results. CacheMaxNumEntries is ignored if CacheEnabled is false.
                doc: Config represents the options for a NewFilterSet.
              resources:
                type: list
                children:
                  item:
                    type: map
                    children:
                      key:
                        type: string
                        doc: Key specifies the attribute key.
                      value:
                        type: map
                        open: true
                        doc: Values specifies the value to match against. If it is not set, any value will match.
                    doc: Attribute specifies the attribute key and optional value to match against.
                doc: Resources specify the list of items to match the resources against. A match occurs if the data's resources match at least one item in this list. This is an optional field.
              services:
                type: list
                children:
                  item:
                    type: string
                doc: Services specify the list of items to match service name against. A match occurs if the span's service name matches at least one item in this list. This is an optional field.
              span_kinds:
                type: list
                children:
                  item:
                    type: string
                doc: SpanKinds specify the list of items to match the span kind against. A match occurs if the span's span kind matches at least one item in this list. This is an optional field
              span_names:
                type: list
                children:
                  item:
                    type: string
                doc: SpanNames specify the list of items to match span name against. A match occurs if the span name matches at least one item in this list. This is an optional field.
            doc: 'Exclude specifies when this processor will not be applied to the input data which match the specified properties. Note: The `exclude` properties are checked after the `include` properties, if they exist, are checked. If `include` isn''t specified, the `exclude` properties are checked against all input data. This is an optional field. If neither `include` and `exclude` are set, all input data is processed. If `exclude` is set and `include` isn''t set, then all the input data that does not match the properties in this structure are processed.'
          include:
            type: map
            children:
              attributes:
                type: list
                children:
                  item:
                    type: map
                    children:
                      key:
                        type: string
                        doc: Key specifies the attribute key.
                      value:
                        type: map
                        open: true
                        doc: Values specifies the value to match against. If it is not set, any value will match.
                    doc: Attribute specifies the attribute key and optional value to match against.
                doc: Attributes specifies the list of attributes to match against. All of these attributes must match for a match to occur. This is an optional field.
              libraries:
                type: list
                children:
                  item:
                    type: map
                    children:
                      name:
                        type: string
                      version:
                        type: string
                        doc: version match expected actual  match nil      <blank> yes nil      1       yes <blank>  <blank> yes <blank>  1       no 1        <blank> no 1        1       yes
                    doc: InstrumentationLibrary specifies the instrumentation library and optional version to match against.
                doc: Libraries specify the list of items to match the implementation library against. A match occurs if the span's implementation library matches at least one item in this list. This is an optional field.
              log_bodies:
                type: list
                children:
                  item:
                    type: string
                doc: LogBodies is a list of strings that the LogRecord's body field must match against.
              log_severity_number:
                type: map
                children:
                  match_undefined:
                    type: bool
                    doc: MatchUndefined controls whether logs with "undefined" severity matches. If this is true, entries with undefined severity will match.
                  min:
                    type: int
                    doc: Min is the lowest severity that may be matched. e.g. if this is plog.SeverityNumberInfo, INFO, WARN, ERROR, and FATAL logs will match.
                doc: LogSeverityNumber defines how to match against a log record's SeverityNumber, if defined.
              log_severity_texts:
                type: list
                children:
                  item:
                    type: string
                doc: LogSeverityTexts is a list of strings that the LogRecord's severity text field must match against.
              match_type:
                type: string
                doc: MatchType describes the type of pattern matching a FilterSet uses to filter strings.
              metric_names:
                type: list
                children:
                  item:
                    type: string
                doc: MetricNames is a list of strings to match metric name against. A match occurs if metric name matches at least one item in the list. This field is optional.
              regexp:
                type: map
                children:
                  cacheenabled:
                    type: bool
                    doc: CacheEnabled determines whether match results are LRU cached to make subsequent matches faster. Cache size is unlimited unless CacheMaxNumEntries is also specified.
                  cachemaxnumentries:
                    type: int
                    doc: CacheMaxNumEntries is the max number of entries of the LRU cache that stores match results. CacheMaxNumEntries is ignored if CacheEnabled is false.
                doc: Config represents the options for a NewFilterSet.
              resources:
                type: list
                children:
                  item:
                    type: map
                    children:
                      key:
                        type: string
                        doc: Key specifies the attribute key.
                      value:
                        type: map
                        open: true
                        doc: Values specifies the value to match against. If it is not set, any value will match.
                    doc: Attribute specifies the attribute key and optional value to match against.
                doc: Resources specify the list of items to match the resources against. A match occurs if the data's resources match at least one item in this list. This is an optional field.
              services:
                type: list
                children:
                  item:
                    type: string
                doc: Services specify the list of items to match service name against. A match occurs if the span's service name matches at least one item in this list. This is an optional field.
              span_kinds:
                type: list
                children:
                  item:
                    type: string
                doc: SpanKinds specify the list of items to match the span kind against. A match occurs if the span's span kind matches at least one item in this list. This is an optional field
              span_names:
                type: list
                children:
                  item:
                    type: string
                doc: SpanNames specify the list of items to match span name against. A match occurs if the span name matches at least one item in this list. This is an optional field.
            doc: 'Include specifies the set of input data properties that must be present in order for this processor to apply to it. Note: If `exclude` is specified, the input data is compared against those properties after the `include` properties. This is an optional field. If neither `include` and `exclude` are set, all input data are processed. If `include` is set and `exclude` isn''t set, then all input data matching the properties in this structure are processed.'
        doc: Config specifies the set of attributes to be inserted, updated, upserted and deleted and the properties to include/exclude a span from being processed. This processor handles all forms of modifications to attributes within a span, log, or metric. Prior to any actions being applied, each span is compared against the include properties and then the exclude properties if they are specified. This determines if a span is to be processed or not. The list of actions is applied in order specified in the configuration.
    batch:
      type: batch
      signals:
        - traces
        - metrics
        - logs
      stability:
        logs: beta
        metrics: beta
        traces: beta
      module: go.opentelemetry.io/collector/processor/batchprocessor
      fields:
        type: map
        children:
          metadata_cardinality_limit:
            type: int
            doc: maximum number of distinct metadata_keys combinations
          metadata_keys:
            type: list
            children:
              item:
                type: string
            doc: client metadata keys that get their own batcher
          send_batch_max_size:
            type: int
            doc: hard upper bound on a batch; must be >= send_batch_size when set
          send_batch_size:
            type: int
            doc: number of spans, metric data points or log records per batch
          timeout:
            type: duration
            doc: how long to wait before sending a batch that is not yet full
    filter:
      type: filter
      signals:
        - traces
        - metrics
        - logs
        - profiles
      stability:
        logs: alpha
        metrics: alpha
        profiles: development
        traces: alpha
      module: github.com/open-telemetry/opentelemetry-collector-contrib/processor/filterprocessor
      fields:
        type: map
        children:
          error_mode:
            type: string
            doc: ErrorMode determines how the processor reacts to errors that occur while processing an OTTL condition. Valid values are `ignore` and `propagate`. `ignore` means the processor ignores errors returned by conditions and continues on to the next condition. This is the recommended mode. `propagate` means the processor returns the error up the pipeline.  This will result in the payload being dropped from the collector. The default value is `ignore`.
          log_conditions:
            type: list
            children:
              item:
                type: map
                children:
                  conditions:
                    type: list
                    children:
                      item:
                        type: string
                  context:
                    type: string
                  error_mode:
                    type: string
          logs:
            type: map
            children:
              exclude:
                type: map
                children:
                  bodies:
                    type: list
                    children:
                      item:
                        type: string
                    doc: LogBodies is a list of strings that the LogRecord's body field must match against.
                  match_type:
                    type: string
                    doc: LogMatchType specifies the type of matching desired
                  record_attributes:
                    type: list
                    children:
                      item:
                        type: map
                        children:
                          key:
                            type: string
                            doc: Key specifies the attribute key.
                          value:
                            type: map
                            open: true
                            doc: Values specifies the value to match against. If it is not set, any value will match.
                        doc: Attribute specifies the attribute key and optional value to match against.
                    doc: RecordAttributes defines a list of possible record attributes to match logs against. A match occurs if any record attribute matches at least one expression in this given list.
                  resource_attributes:
                    type: list
                    children:
                      item:
                        type: map
                        children:
                          key:
                            type: string
                            doc: Key specifies the attribute key.
                          value:
                            type: map
                            open: true
                            doc: Values specifies the value to match against. If it is not set, any value will match.
                        doc: Attribute specifies the attribute key and optional value to match against.
                    doc: ResourceAttributes defines a list of possible resource attributes to match logs against. A match occurs if any resource attribute matches all expressions in this given list.
                  severity_number:
                    type: map
                    children:
                      match_undefined:
                        type: bool
                        doc: MatchUndefined lets logs records with "unknown" severity match. If MinSeverity is not set, this field is ignored, as fields are not matched based on severity.
                      min:
                        type: string
                        doc: 'Min is the minimum severity needed for the log record to match. This corresponds to the short names specified here: https://github.com/open-telemetry/opentelemetry-specification/blob/main/specification/logs/data-model.md#displaying-severity this field is case-insensitive ("INFO" == "info")'
                    doc: SeverityNumberProperties defines how to match against a log record's SeverityNumber, if defined.
                  severity_texts:
                    type: list
                    children:
                      item:
                        type: string
                    doc: SeverityTexts is a list of strings that the LogRecord's severity text field must match against.
                doc: Exclude match properties describe logs that should be excluded from the Collector Service pipeline, all other logs should be included. If both Include and Exclude are specified, Include filtering occurs first.
              include:
                type: map
                children:
                  bodies:
                    type: list
                    children:
                      item:
                        type: string
                    doc: LogBodies is a list of strings that the LogRecord's body field must match against.
                  match_type:
                    type: string
                    doc: LogMatchType specifies the type of matching desired
                  record_attributes:
                    type: list
                    children:
                      item:
                        type: map
                        children:
                          key:
                            type: string
                            doc: Key specifies the attribute key.
                          value:
                            type: map
                            open: true
                            doc: Values specifies the value to match against. If it is not set, any value will match.
                        doc: Attribute specifies the attribute key and optional value to match against.
                    doc: RecordAttributes defines a list of possible record attributes to match logs against. A match occurs if any record attribute matches at least one expression in this given list.
                  resource_attributes:
                    type: list
                    children:
                      item:
                        type: map
                        children:
                          key:
                            type: string
                            doc: Key specifies the attribute key.
                          value:
                            type: map
                            open: true
                            doc: Values specifies the value to match against. If it is not set, any value will match.
                        doc: Attribute specifies the attribute key and optional value to match against.
                    doc: ResourceAttributes defines a list of possible resource attributes to match logs against. A match occurs if any resource attribute matches all expressions in this given list.
                  severity_number:
                    type: map
                    children:
                      match_undefined:
                        type: bool
                        doc: MatchUndefined lets logs records with "unknown" severity match. If MinSeverity is not set, this field is ignored, as fields are not matched based on severity.
                      min:
                        type: string
                        doc: 'Min is the minimum severity needed for the log record to match. This corresponds to the short names specified here: https://github.com/open-telemetry/opentelemetry-specification/blob/main/specification/logs/data-model.md#displaying-severity this field is case-insensitive ("INFO" == "info")'
                    doc: SeverityNumberProperties defines how to match against a log record's SeverityNumber, if defined.
                  severity_texts:
                    type: list
                    children:
                      item:
                        type: string
                    doc: SeverityTexts is a list of strings that the LogRecord's severity text field must match against.
                doc: Include match properties describe logs that should be included in the Collector Service pipeline, all other logs should be dropped from further processing. If both Include and Exclude are specified, Include filtering occurs first.
              log_record:
                type: list
                children:
                  item:
                    type: string
                doc: LogConditions is a list of OTTL conditions for an ottllog context. If any condition resolves to true, the log event will be dropped. Supports `and`, `or`, and `()`
              resource:
                type: list
                children:
                  item:
                    type: string
                doc: ResourceConditions is a list of OTTL conditions for an ottlresource context. If any condition resolves to true, the whole resource will be dropped. Supports `and`, `or`, and `()`
            doc: 'Deprecated: use LogConditions instead.'
          metric_conditions:
            type: list
            children:
              item:
                type: map
                children:
                  conditions:
                    type: list
                    children:
                      item:
                        type: string
                  context:
                    type: string
                  error_mode:
                    type: string
          metrics:
            type: map
            children:
              datapoint:
                type: list
                children:
                  item:
                    type: string
                doc: DataPointConditions is a list of OTTL conditions for an ottldatapoint context. If any condition resolves to true, the datapoint will be dropped. Supports `and`, `or`, and `()`
              exclude:
                type: map
                children:
                  expressions:
                    type: list
                    children:
                      item:
                        type: string
                    doc: Expressions specifies the list of expr expressions to match metrics against. A match occurs if any datapoint in a metric matches at least one expression in this list.
                  match_type:
                    type: string
                    doc: MatchType specifies the type of matching desired
                  metric_names:
                    type: list
                    children:
                      item:
                        type: string
                    doc: MetricNames specifies the list of string patterns to match metric names against. A match occurs if the metric name matches at least one string pattern in this list.
                  regexp:
                    type: map
                    children:
                      cacheenabled:
                        type: bool
                        doc: CacheEnabled determines whether match results are LRU cached to make subsequent matches faster. Cache size is unlimited unless CacheMaxNumEntries is also specified.
                      cachemaxnumentries:
                        type: int
                        doc: CacheMaxNumEntries is the max number of entries of the LRU cache that stores match results. CacheMaxNumEntries is ignored if CacheEnabled is false.
                    doc: RegexpConfig specifies options for the MetricRegexp match type
                  resource_attributes:
                    type: list
                    children:
                      item:
                        type: map
                        children:
                          key:
                            type: string
                            doc: Key specifies the attribute key.
                          value:
                            type: map
                            open: true
                            doc: Values specifies the value to match against. If it is not set, any value will match.
                        doc: Attribute specifies the attribute key and optional value to match against.
                    doc: ResourceAttributes defines a list of possible resource attributes to match metrics against. A match occurs if any resource attribute matches all expressions in this given list.
                doc: Exclude match properties describe metrics that should be excluded from the Collector Service pipeline, all other metrics should be included. If both Include and Exclude are specified, Include filtering occurs first.
              include:
                type: map
                children:
                  expressions:
                    type: list
                    children:
                      item:
                        type: string
                    doc: Expressions specifies the list of expr expressions to match metrics against. A match occurs if any datapoint in a metric matches at least one expression in this list.
                  match_type:
                    type: string
                    doc: MatchType specifies the type of matching desired
                  metric_names:
                    type: list
                    children:
                      item:
                        type: string
                    doc: MetricNames specifies the list of string patterns to match metric names against. A match occurs if the metric name matches at least one string pattern in this list.
                  regexp:
                    type: map
                    children:
                      cacheenabled:
                        type: bool
                        doc: CacheEnabled determines whether match results are LRU cached to make subsequent matches faster. Cache size is unlimited unless CacheMaxNumEntries is also specified.
                      cachemaxnumentries:
                        type: int
                        doc: CacheMaxNumEntries is the max number of entries of the LRU cache that stores match results. CacheMaxNumEntries is ignored if CacheEnabled is false.
                    doc: RegexpConfig specifies options for the MetricRegexp match type
                  resource_attributes:
                    type: list
                    children:
                      item:
                        type: map
                        children:
                          key:
                            type: string
                            doc: Key specifies the attribute key.
                          value:
                            type: map
                            open: true
                            doc: Values specifies the value to match against. If it is not set, any value will match.
                        doc: Attribute specifies the attribute key and optional value to match against.
                    doc: ResourceAttributes defines a list of possible resource attributes to match metrics against. A match occurs if any resource attribute matches all expressions in this given list.
                doc: Include match properties describe metrics that should be included in the Collector Service pipeline, all other metrics should be dropped from further processing. If both Include and Exclude are specified, Include filtering occurs first.
              metric:
                type: list
                children:
                  item:
                    type: string
                doc: MetricConditions is a list of OTTL conditions for an ottlmetric context. If any condition resolves to true, the metric will be dropped. Supports `and`, `or`, and `()`
              regexp:
                type: map
                children:
                  cacheenabled:
                    type: bool
                    doc: CacheEnabled determines whether match results are LRU cached to make subsequent matches faster. Cache size is unlimited unless CacheMaxNumEntries is also specified.
                  cachemaxnumentries:
                    type: int
                    doc: CacheMaxNumEntries is the max number of entries of the LRU cache that stores match results. CacheMaxNumEntries is ignored if CacheEnabled is false.
                doc: RegexpConfig specifies options for the regexp match type
              resource:
                type: list
                children:
                  item:
                    type: string
                doc: ResourceConditions is a list of OTTL conditions for an ottlresource context. If any condition resolves to true, the whole resource will be dropped. Supports `and`, `or`, and `()`
            doc: 'Deprecated: use MetricConditions instead.'
          profile_conditions:
            type: list
            children:
              item:
                type: map
                children:
                  conditions:
                    type: list
                    children:
                      item:
                        type: string
                  context:
                    type: string
                  error_mode:
                    type: string
          profiles:
            type: map
            children:
              profile:
                type: list
                children:
                  item:
                    type: string
                doc: ProfileConditions is a list of OTTL conditions for an ottlprofile context. If any condition resolves to true, the profile will be dropped. Supports `and`, `or`, and `()`
              resource:
                type: list
                children:
                  item:
                    type: string
                doc: ResourceConditions is a list of OTTL conditions for an ottlresource context. If any condition resolves to true, the whole resource will be dropped. Supports `and`, `or`, and `()`
            doc: 'Deprecated: use ProfileConditions instead.'
          spans:
            type: map
            children:
              exclude:
                type: map
                children:
                  attributes:
                    type: list
                    children:
                      item:
                        type: map
                        children:
                          key:
                            type: string
                            doc: Key specifies the attribute key.
                          value:
                            type: map
                            open: true
                            doc: Values specifies the value to match against. If it is not set, any value will match.
                        doc: Attribute specifies the attribute key and optional value to match against.
                    doc: Attributes specifies the list of attributes to match against. All of these attributes must match for a match to occur. This is an optional field.
                  libraries:
                    type: list
                    children:
                      item:
                        type: map
                        children:
                          name:
                            type: string
                          version:
                            type: string
                            doc: version match expected actual  match nil      <blank> yes nil      1       yes <blank>  <blank> yes <blank>  1       no 1        <blank> no 1        1       yes
                        doc: InstrumentationLibrary specifies the instrumentation library and optional version to match against.
                    doc: Libraries specify the list of items to match the implementation library against. A match occurs if the span's implementation library matches at least one item in this list. This is an optional field.
                  log_bodies:
                    type: list
                    children:
                      item:
                        type: string
                    doc: LogBodies is a list of strings that the LogRecord's body field must match against.
                  log_severity_number:
                    type: map
                    children:
                      match_undefined:
                        type: bool
                        doc: MatchUndefined controls whether logs with "undefined" severity matches. If this is true, entries with undefined severity will match.
                      min:
                        type: int
                        doc: Min is the lowest severity that may be matched. e.g. if this is plog.SeverityNumberInfo, INFO, WARN, ERROR, and FATAL logs will match.
                    doc: LogSeverityNumber defines how to match against a log record's SeverityNumber, if defined.
                  log_severity_texts:
                    type: list
                    children:
                      item:
                        type: string
                    doc: LogSeverityTexts is a list of strings that the LogRecord's severity text field must match against.
                  match_type:
                    type: string
                    doc: MatchType describes the type of pattern matching a FilterSet uses to filter strings.
                  metric_names:
                    type: list
                    children:
                      item:
                        type: string
                    doc: MetricNames is a list of strings to match metric name against. A match occurs if metric name matches at least one item in the list. This field is optional.
                  regexp:
                    type: map
                    children:
                      cacheenabled:
                        type: bool
                        doc: CacheEnabled determines whether match results are LRU cached to make subsequent matches faster. Cache size is unlimited unless CacheMaxNumEntries is also specified.
                      cachemaxnumentries:
                        type: int
                        doc: CacheMaxNumEntries is the max number of entries of the LRU cache that stores match results. CacheMaxNumEntries is ignored if CacheEnabled is false.
                    doc: Config represents the options for a NewFilterSet.
                  resources:
                    type: list
                    children:
                      item:
                        type: map
                        children:
                          key:
                            type: string
                            doc: Key specifies the attribute key.
                          value:
                            type: map
                            open: true
                            doc: Values specifies the value to match against. If it is not set, any value will match.
                        doc: Attribute specifies the attribute key and optional value to match against.
                    doc: Resources specify the list of items to match the resources against. A match occurs if the data's resources match at least one item in this list. This is an optional field.
                  services:
                    type: list
                    children:
                      item:
                        type: string
                    doc: Services specify the list of items to match service name against. A match occurs if the span's service name matches at least one item in this list. This is an optional field.
                  span_kinds:
                    type: list
                    children:
                      item:
                        type: string
                    doc: SpanKinds specify the list of items to match the span kind against. A match occurs if the span's span kind matches at least one item in this list. This is an optional field
                  span_names:
                    type: list
                    children:
                      item:
                        type: string
                    doc: SpanNames specify the list of items to match span name against. A match occurs if the span name matches at least one item in this list. This is an optional field.
                doc: 'Exclude specifies when this processor will not be applied to the input data which match the specified properties. Note: The `exclude` properties are checked after the `include` properties, if they exist, are checked. If `include` isn''t specified, the `exclude` properties are checked against all input data. This is an optional field. If neither `include` and `exclude` are set, all input data is processed. If `exclude` is set and `include` isn''t set, then all the input data that does not match the properties in this structure are processed.'
              include:
                type: map
                children:
                  attributes:
                    type: list
                    children:
                      item:
                        type: map
                        children:
                          key:
                            type: string
                            doc: Key specifies the attribute key.
                          value:
                            type: map
                            open: true
                            doc: Values specifies the value to match against. If it is not set, any value will match.
                        doc: Attribute specifies the attribute key and optional value to match against.
                    doc: Attributes specifies the list of attributes to match against. All of these attributes must match for a match to occur. This is an optional field.
                  libraries:
                    type: list
                    children:
                      item:
                        type: map
                        children:
                          name:
                            type: string
                          version:
                            type: string
                            doc: version match expected actual  match nil      <blank> yes nil      1       yes <blank>  <blank> yes <blank>  1       no 1        <blank> no 1        1       yes
                        doc: InstrumentationLibrary specifies the instrumentation library and optional version to match against.
                    doc: Libraries specify the list of items to match the implementation library against. A match occurs if the span's implementation library matches at least one item in this list. This is an optional field.
                  log_bodies:
                    type: list
                    children:
                      item:
                        type: string
                    doc: LogBodies is a list of strings that the LogRecord's body field must match against.
                  log_severity_number:
                    type: map
                    children:
                      match_undefined:
                        type: bool
                        doc: MatchUndefined controls whether logs with "undefined" severity matches. If this is true, entries with undefined severity will match.
                      min:
                        type: int
                        doc: Min is the lowest severity that may be matched. e.g. if this is plog.SeverityNumberInfo, INFO, WARN, ERROR, and FATAL logs will match.
                    doc: LogSeverityNumber defines how to match against a log record's SeverityNumber, if defined.
                  log_severity_texts:
                    type: list
                    children:
                      item:
                        type: string
                    doc: LogSeverityTexts is a list of strings that the LogRecord's severity text field must match against.
                  match_type:
                    type: string
                    doc: MatchType describes the type of pattern matching a FilterSet uses to filter strings.
                  metric_names:
                    type: list
                    children:
                      item:
                        type: string
                    doc: MetricNames is a list of strings to match metric name against. A match occurs if metric name matches at least one item in the list. This field is optional.
                  regexp:
                    type: map
                    children:
                      cacheenabled:
                        type: bool
                        doc: CacheEnabled determines whether match results are LRU cached to make subsequent matches faster. Cache size is unlimited unless CacheMaxNumEntries is also specified.
                      cachemaxnumentries:
                        type: int
                        doc: CacheMaxNumEntries is the max number of entries of the LRU cache that stores match results. CacheMaxNumEntries is ignored if CacheEnabled is false.
                    doc: Config represents the options for a NewFilterSet.
                  resources:
                    type: list
                    children:
                      item:
                        type: map
                        children:
                          key:
                            type: string
                            doc: Key specifies the attribute key.
                          value:
                            type: map
                            open: true
                            doc: Values specifies the value to match against. If it is not set, any value will match.
                        doc: Attribute specifies the attribute key and optional value to match against.
                    doc: Resources specify the list of items to match the resources against. A match occurs if the data's resources match at least one item in this list. This is an optional field.
                  services:
                    type: list
                    children:
                      item:
                        type: string
                    doc: Services specify the list of items to match service name against. A match occurs if the span's service name matches at least one item in this list. This is an optional field.
                  span_kinds:
                    type: list
                    children:
                      item:
                        type: string
                    doc: SpanKinds specify the list of items to match the span kind against. A match occurs if the span's span kind matches at least one item in this list. This is an optional field
                  span_names:
                    type: list
                    children:
                      item:
                        type: string
                    doc: SpanNames specify the list of items to match span name against. A match occurs if the span name matches at least one item in this list. This is an optional field.
                doc: 'Include specifies the set of input data properties that must be present in order for this processor to apply to it. Note: If `exclude` is specified, the input data is compared against those properties after the `include` properties. This is an optional field. If neither `include` and `exclude` are set, all input data are processed. If `include` is set and `exclude` isn''t set, then all input data matching the properties in this structure are processed.'
            doc: 'Deprecated: use TraceConditions instead.'
          trace_conditions:
            type: list
            children:
              item:
                type: map
                children:
                  conditions:
                    type: list
                    children:
                      item:
                        type: string
                  context:
                    type: string
                  error_mode:
                    type: string
          traces:
            type: map
            children:
              resource:
                type: list
                children:
                  item:
                    type: string
                doc: ResourceConditions is a list of OTTL conditions for an ottlresource context. If any condition resolves to true, the whole resource will be dropped. Supports `and`, `or`, and `()`
              span:
                type: list
                children:
                  item:
                    type: string
                doc: SpanConditions is a list of OTTL conditions for an ottlspan context. If any condition resolves to true, the span will be dropped. Supports `and`, `or`, and `()`
              spanevent:
                type: list
                children:
                  item:
                    type: string
                doc: SpanEventConditions is a list of OTTL conditions for an ottlspanevent context. If any condition resolves to true, the span event will be dropped. Supports `and`, `or`, and `()`
            doc: 'Deprecated: use TraceConditions instead.'
        doc: Config defines configuration for Resource processor.
    memory_limiter:
      type: memory_limiter
      signals:
        - traces
        - metrics
        - logs
        - profiles
      stability:
        logs: beta
        metrics: beta
        profiles: alpha
        traces: beta
      module: go.opentelemetry.io/collector/processor/memorylimiterprocessor
      fields:
        type: map
        children:
          check_interval:
            type: duration
            doc: how often memory use is measured; 1s is the usual value
          limit_mib:
            type: int
            doc: hard limit in MiB; above it the processor starts refusing data
          limit_percentage:
            type: int
            doc: hard limit as a percentage of total memory, for containers
          spike_limit_mib:
            type: int
            doc: headroom in MiB reserved for allocation spikes
          spike_limit_percentage:
            type: int
            doc: spike headroom as a percentage of total memory
        required:
          - check_interval
    probabilistic_sampler:
      type: probabilistic_sampler
      signals:
        - traces
        - logs
      stability:
        logs: alpha
        traces: beta
      module: github.com/open-telemetry/opentelemetry-collector-contrib/processor/probabilisticsamplerprocessor
      fields:
        type: map
        children:
          attribute_source:
            type: string
          fail_closed:
            type: bool
            doc: FailClosed indicates to not sample data (the processor will fail "closed") in case of error, such as failure to parse the tracestate field or missing the randomness attribute. By default, failure cases are sampled (the processor is fails "open").  Sampling priority-based decisions are made after FailClosed is processed, making it possible to sample despite errors using priority.
          from_attribute:
            type: string
            doc: FromAttribute (logs only) The optional name of a log record attribute used for sampling purposes, such as a unique log record ID. The value of the attribute is only used if the trace ID is absent or if `attribute_source` is set to `record`.
          hash_seed:
            type: int
            doc: 'HashSeed allows one to configure the hashing seed. This is important in scenarios where multiple layers of collectors have different sampling rates: if they use the same seed all passing one layer may pass the other even if they have different sampling rates, configuring different seeds avoids that.'
          mode:
            type: string
            doc: 'Mode selects the sampling behavior. Supported values: - "hash_seed": the legacy behavior of this processor. Using an FNV hash combined with the HashSeed value, this sampler performs a non-consistent probabilistic downsampling.  The number of spans output is expected to equal SamplingPercentage (as a ratio) times the number of spans inpout, assuming good behavior from FNV and good entropy in the hashed attributes or TraceID. - "equalizing": Using an OTel-specified consistent sampling mechanism, this sampler selectively reduces the effective sampling probability of arriving spans.  This can be useful to select a small fraction of complete traces from a stream with mixed sampling rates.  The rate of spans passing through depends on how much sampling has already been applied.  If an arriving span was head sampled at the same probability it passes through.  If the span arrives with lower probability, a warning is logged because it means this sampler is configured with too large a sampling probability to ensure complete traces. - "proportional": Using an OTel-specified consistent sampling mechanism, this sampler reduces the effective sampling probability of each span by `SamplingProbability`.'
          sampling_percentage:
            type: float
            doc: 'SamplingPercentage is the percentage rate at which traces or logs are going to be sampled. Defaults to zero, i.e.: no sample. Values greater or equal 100 are treated as "sample all traces/logs".'
          sampling_precision:
            type: int
            doc: SamplingPrecision is how many hex digits of sampling threshold will be encoded, from 1 up to 14.  Default is 4. 0 is treated as full precision.
          sampling_priority:
            type: string
            doc: SamplingPriority (logs only) enables using a log record attribute as the sampling priority of the log record.
        doc: Config has the configuration guiding the sampler processor.
    resource:
      type: resource
      signals:
        - traces
        - metrics
        - logs
        - profiles
      stability:
        logs: beta
        metrics: beta
        profiles: development
        traces: beta
      module: github.com/open-telemetry/opentelemetry-collector-contrib/processor/resourceprocessor
      fields:
        type: map
        children:
          attributes:
            type: list
            children:
              item:
                type: map
                children:
                  action:
                    type: string
                    doc: Action specifies the type of action to perform. The set of values are {INSERT, UPDATE, UPSERT, DELETE, HASH}. Both lower case and upper case are supported. INSERT -  Inserts the key/value to attributes when the key does not exist. No action is applied to attributes where the key already exists. Either Value, FromAttribute or FromContext must be set. UPDATE -  Updates an existing key with a value. No action is applied to attributes where the key does not exist. Either Value, FromAttribute or FromContext must be set. UPSERT -  Performs insert or update action depending on the attributes containing the key. The key/value is inserted to attributes that did not originally have the key. The key/value is updated for attributes where the key already existed. Either Value, FromAttribute or FromContext must be set. DELETE  - Deletes the attribute. If the key doesn't exist, no action is performed. HASH    - Calculates the SHA-1 hash of an existing value and overwrites the value with its SHA-1 hash result. If the feature gate `coreinternal.attraction.hash.sha256` is enabled, it uses SHA2-256 instead. EXTRACT - Extracts values using a regular expression rule from the input 'key' to target keys specified in the 'rule'. If a target key already exists, it will be overridden. CONVERT  - converts the type of an existing attribute, if convertable This is a required field.
                  converted_type:
                    type: string
                    doc: ConvertedType specifies the target type of an attribute to be converted If the key doesn't exist, no action is performed. If the value cannot be converted, the original value will be left as-is
                  default_value:
                    type: map
                    open: true
                    doc: DefaultValue specifies the value to use if Value/FromAttribute/FromContext doesn't provide a value (e.g., environment variable not set, attribute doesn't exist). Only used with INSERT, UPDATE, and UPSERT actions.
                  from_attribute:
                    type: string
                    doc: FromAttribute specifies the attribute to use to populate the value. If the attribute doesn't exist, no action is performed.
                  from_context:
                    type: string
                    doc: FromContext specifies the context value to use to populate the value. The values would be searched in client.Info.Metadata. If the key doesn't exist, no action is performed. If the key has multiple values the values will be joined with `;` separator.
                  key:
                    type: string
                    doc: Key specifies the attribute to act upon. This is a required field.
                  pattern:
                    type: string
                    doc: 'A regex pattern must be specified for the action EXTRACT. It uses the attribute specified by `key'' to extract values from The target keys are inferred based on the names of the matcher groups provided and the names will be inferred based on the values of the matcher group. Note: All subexpressions must have a name. Note: The value type of the source key must be a string. If it isn''t, no extraction will occur.'
                  value:
                    type: map
                    open: true
                    doc: Value specifies the value to populate for the key. The type of the value is inferred from the configuration.
                doc: ActionKeyValue specifies the attribute key to act upon.
            doc: AttributesActions specifies the list of actions to be applied on resource attributes. The set of actions are {INSERT, UPDATE, UPSERT, DELETE, HASH, EXTRACT}.
        doc: Config defines configuration for Resource processor.
    span:
      type: span
      signals:
        - traces
      stability:
        traces: alpha
      module: github.com/open-telemetry/opentelemetry-collector-contrib/processor/spanprocessor
      fields:
        type: map
        children:
          exclude:
            type: map
            children:
              attributes:
                type: list
                children:
                  item:
                    type: map
                    children:
                      key:
                        type: string
                        doc: Key specifies the attribute key.
                      value:
                        type: map
                        open: true
                        doc: Values specifies the value to match against. If it is not set, any value will match.
                    doc: Attribute specifies the attribute key and optional value to match against.
                doc: Attributes specifies the list of attributes to match against. All of these attributes must match for a match to occur. This is an optional field.
              libraries:
                type: list
                children:
                  item:
                    type: map
                    children:
                      name:
                        type: string
                      version:
                        type: string
                        doc: version match expected actual  match nil      <blank> yes nil      1       yes <blank>  <blank> yes <blank>  1       no 1        <blank> no 1        1       yes
                    doc: InstrumentationLibrary specifies the instrumentation library and optional version to match against.
                doc: Libraries specify the list of items to match the implementation library against. A match occurs if the span's implementation library matches at least one item in this list. This is an optional field.
              log_bodies:
                type: list
                children:
                  item:
                    type: string
                doc: LogBodies is a list of strings that the LogRecord's body field must match against.
              log_severity_number:
                type: map
                children:
                  match_undefined:
                    type: bool
                    doc: MatchUndefined controls whether logs with "undefined" severity matches. If this is true, entries with undefined severity will match.
                  min:
                    type: int
                    doc: Min is the lowest severity that may be matched. e.g. if this is plog.SeverityNumberInfo, INFO, WARN, ERROR, and FATAL logs will match.
                doc: LogSeverityNumber defines how to match against a log record's SeverityNumber, if defined.
              log_severity_texts:
                type: list
                children:
                  item:
                    type: string
                doc: LogSeverityTexts is a list of strings that the LogRecord's severity text field must match against.
              match_type:
                type: string
                doc: MatchType describes the type of pattern matching a FilterSet uses to filter strings.
              metric_names:
                type: list
                children:
                  item:
                    type: string
                doc: MetricNames is a list of strings to match metric name against. A match occurs if metric name matches at least one item in the list. This field is optional.
              regexp:
                type: map
                children:
                  cacheenabled:
                    type: bool
                    doc: CacheEnabled determines whether match results are LRU cached to make subsequent matches faster. Cache size is unlimited unless CacheMaxNumEntries is also specified.
                  cachemaxnumentries:
                    type: int
                    doc: CacheMaxNumEntries is the max number of entries of the LRU cache that stores match results. CacheMaxNumEntries is ignored if CacheEnabled is false.
                doc: Config represents the options for a NewFilterSet.
              resources:
                type: list
                children:
                  item:
                    type: map
                    children:
                      key:
                        type: string
                        doc: Key specifies the attribute key.
                      value:
                        type: map
                        open: true
                        doc: Values specifies the value to match against. If it is not set, any value will match.
                    doc: Attribute specifies the attribute key and optional value to match against.
                doc: Resources specify the list of items to match the resources against. A match occurs if the data's resources match at least one item in this list. This is an optional field.
              services:
                type: list
                children:
                  item:
                    type: string
                doc: Services specify the list of items to match service name against. A match occurs if the span's service name matches at least one item in this list. This is an optional field.
              span_kinds:
                type: list
                children:
                  item:
                    type: string
                doc: SpanKinds specify the list of items to match the span kind against. A match occurs if the span's span kind matches at least one item in this list. This is an optional field
              span_names:
                type: list
                children:
                  item:
                    type: string
                doc: SpanNames specify the list of items to match span name against. A match occurs if the span name matches at least one item in this list. This is an optional field.
            doc: 'Exclude specifies when this processor will not be applied to the input data which match the specified properties. Note: The `exclude` properties are checked after the `include` properties, if they exist, are checked. If `include` isn''t specified, the `exclude` properties are checked against all input data. This is an optional field. If neither `include` and `exclude` are set, all input data is processed. If `exclude` is set and `include` isn''t set, then all the input data that does not match the properties in this structure are processed.'
          include:
            type: map
            children:
              attributes:
                type: list
                children:
                  item:
                    type: map
                    children:
                      key:
                        type: string
                        doc: Key specifies the attribute key.
                      value:
                        type: map
                        open: true
                        doc: Values specifies the value to match against. If it is not set, any value will match.
                    doc: Attribute specifies the attribute key and optional value to match against.
                doc: Attributes specifies the list of attributes to match against. All of these attributes must match for a match to occur. This is an optional field.
              libraries:
                type: list
                children:
                  item:
                    type: map
                    children:
                      name:
                        type: string
                      version:
                        type: string
                        doc: version match expected actual  match nil      <blank> yes nil      1       yes <blank>  <blank> yes <blank>  1       no 1        <blank> no 1        1       yes
                    doc: InstrumentationLibrary specifies the instrumentation library and optional version to match against.
                doc: Libraries specify the list of items to match the implementation library against. A match occurs if the span's implementation library matches at least one item in this list. This is an optional field.
              log_bodies:
                type: list
                children:
                  item:
                    type: string
                doc: LogBodies is a list of strings that the LogRecord's body field must match against.
              log_severity_number:
                type: map
                children:
                  match_undefined:
                    type: bool
                    doc: MatchUndefined controls whether logs with "undefined" severity matches. If this is true, entries with undefined severity will match.
                  min:
                    type: int
                    doc: Min is the lowest severity that may be matched. e.g. if this is plog.SeverityNumberInfo, INFO, WARN, ERROR, and FATAL logs will match.
                doc: LogSeverityNumber defines how to match against a log record's SeverityNumber, if defined.
              log_severity_texts:
                type: list
                children:
                  item:
                    type: string
                doc: LogSeverityTexts is a list of strings that the LogRecord's severity text field must match against.
              match_type:
                type: string
                doc: MatchType describes the type of pattern matching a FilterSet uses to filter strings.
              metric_names:
                type: list
                children:
                  item:
                    type: string
                doc: MetricNames is a list of strings to match metric name against. A match occurs if metric name matches at least one item in the list. This field is optional.
              regexp:
                type: map
                children:
                  cacheenabled:
                    type: bool
                    doc: CacheEnabled determines whether match results are LRU cached to make subsequent matches faster. Cache size is unlimited unless CacheMaxNumEntries is also specified.
                  cachemaxnumentries:
                    type: int
                    doc: CacheMaxNumEntries is the max number of entries of the LRU cache that stores match results. CacheMaxNumEntries is ignored if CacheEnabled is false.
                doc: Config represents the options for a NewFilterSet.
              resources:
                type: list
                children:
                  item:
                    type: map
                    children:
                      key:
                        type: string
                        doc: Key specifies the attribute key.
                      value:
                        type: map
                        open: true
                        doc: Values specifies the value to match against. If it is not set, any value will match.
                    doc: Attribute specifies the attribute key and optional value to match against.
                doc: Resources specify the list of items to match the resources against. A match occurs if the data's resources match at least one item in this list. This is an optional field.
              services:
                type: list
                children:
                  item:
                    type: string
                doc: Services specify the list of items to match service name against. A match occurs if the span's service name matches at least one item in this list. This is an optional field.
              span_kinds:
                type: list
                children:
                  item:
                    type: string
                doc: SpanKinds specify the list of items to match the span kind against. A match occurs if the span's span kind matches at least one item in this list. This is an optional field
              span_names:
                type: list
                children:
                  item:
                    type: string
                doc: SpanNames specify the list of items to match span name against. A match occurs if the span name matches at least one item in this list. This is an optional field.
            doc: 'Include specifies the set of input data properties that must be present in order for this processor to apply to it. Note: If `exclude` is specified, the input data is compared against those properties after the `include` properties. This is an optional field. If neither `include` and `exclude` are set, all input data are processed. If `include` is set and `exclude` isn''t set, then all input data matching the properties in this structure are processed.'
          name:
            type: map
            children:
              from_attributes:
                type: list
                children:
                  item:
                    type: string
                doc: 'FromAttributes represents the attribute keys to pull the values from to generate the new span name. All attribute keys are required in the span to re-name a span. If any attribute is missing from the span, no re-name will occur. Note: The new span name is constructed in order of the `from_attributes` specified in the configuration. This field is required and cannot be empty.'
              separator:
                type: string
                doc: Separator is the string used to separate attributes values in the new span name. If no value is set, no separator is used between attribute values. Used with FromAttributes only.
              to_attributes:
                type: map
                children:
                  break_after_match:
                    type: bool
                    doc: BreakAfterMatch specifies if processing of rules should stop after the first match. If it is false rule processing will continue to be performed over the modified span name.
                  keep_original_name:
                    type: bool
                    doc: KeepOriginalName specifies if the original span name should be kept after processing the rules. If it is true the original span name will be kept, otherwise it will be replaced with the placeholders of the captured attributes.
                  rules:
                    type: list
                    children:
                      item:
                        type: string
                    doc: Rules is a list of rules to extract attribute values from span name. The values in the span name are replaced by extracted attribute names. Each rule in the list is a regex pattern string. Span name is checked against the regex. If it matches then all named subexpressions of the regex are extracted as attributes and are added to the span. Each subexpression name becomes an attribute name and subexpression matched portion becomes the attribute value. The matched portion in the span name is replaced by extracted attribute name. If the attributes already exist in the span then they will be overwritten. The process is repeated for all rules in the order they are specified. Each subsequent rule works on the span name that is the output after processing the previous rule.
                doc: ToAttributes specifies a configuration to extract attributes from span name.
            doc: 'Rename specifies the components required to re-name a span. The `from_attributes` field needs to be set for this processor to be properly configured. Note: The field name is `Rename` to avoid collision with the Name() method from config.NamedEntity'
          status:
            type: map
            children:
              code:
                type: string
                doc: 'Code is one of three values "Ok" or "Error" or "Unset". Please check: https://github.com/open-telemetry/opentelemetry-specification/blob/main/specification/trace/api.md#set-status'
              description:
                type: string
                doc: Description is an optional field documenting Error statuses.
            doc: SetStatus specifies status which should be set for this span.
        doc: Config is the configuration for the span processor. Prior to any actions being applied, each span is compared against the include properties and then the exclude properties if they are specified. This determines if a span is to be processed or not.
  receiver:
    host_metrics:
      type: host_metrics
      signals:
        - metrics
        - logs
      stability:
        logs: development
        metrics: beta
      module: github.com/open-telemetry/opentelemetry-collector-contrib/receiver/hostmetricsreceiver
      alias: hostmetrics
      fields:
        type: map
        children:
          collection_interval:
            type: duration
            doc: CollectionInterval sets how frequently the scraper should be called and used as the context timeout to ensure that scrapers don't exceed the interval.
          initial_delay:
            type: duration
            doc: InitialDelay sets the initial start delay for the scraper, any non positive value is assumed to be immediately.
          metadata_collection_interval:
            type: duration
            doc: Collection interval for metadata. Metadata of the particular entity is collected when the entity changes. In addition metadata of all entities is collected periodically even if no changes happen. Setting the duration to 0 will disable periodic collection (however will not impact metadata collection on changes).
          root_path:
            type: string
            doc: RootPath is the host's root directory (linux only).
          scrapers:
            type: map
            children:
              cpu: {}
              disk: {}
              filesystem: {}
              load: {}
              memory: {}
              network: {}
              nfs: {}
              paging: {}
              process: {}
              processes: {}
              system: {}
            doc: Map of scrapers to be run, keyed by scraper type.
          timeout:
            type: duration
            doc: Timeout is an optional value used to set scraper's context deadline.
        doc: Config defines configuration for HostMetrics receiver.
    hostmetrics:
      type: hostmetrics
      signals:
        - metrics
        - logs
      stability:
        logs: development
        metrics: beta
      module: github.com/open-telemetry/opentelemetry-collector-contrib/receiver/hostmetricsreceiver
      deprecated: renamed to "host_metrics" upstream; the old name still resolves for now
      aliasOf: host_metrics
      fields:
        type: map
        children:
          collection_interval:
            type: duration
            doc: CollectionInterval sets how frequently the scraper should be called and used as the context timeout to ensure that scrapers don't exceed the interval.
          initial_delay:
            type: duration
            doc: InitialDelay sets the initial start delay for the scraper, any non positive value is assumed to be immediately.
          metadata_collection_interval:
            type: duration
            doc: Collection interval for metadata. Metadata of the particular entity is collected when the entity changes. In addition metadata of all entities is collected periodically even if no changes happen. Setting the duration to 0 will disable periodic collection (however will not impact metadata collection on changes).
          root_path:
            type: string
            doc: RootPath is the host's root directory (linux only).
          scrapers:
            type: map
            children:
              cpu: {}
              disk: {}
              filesystem: {}
              load: {}
              memory: {}
              network: {}
              nfs: {}
              paging: {}
              process: {}
              processes: {}
              system: {}
            doc: Map of scrapers to be run, keyed by scraper type.
          timeout:
            type: duration
            doc: Timeout is an optional value used to set scraper's context deadline.
        doc: Config defines configuration for HostMetrics receiver.
    jaeger:
      type: jaeger
      signals:
        - traces
      stability:
        traces: beta
      module: github.com/open-telemetry/opentelemetry-collector-contrib/receiver/jaegerreceiver
      fields:
        type: map
        children:
          grpc:
            type: map
            children:
              auth:
                type: map
                children:
                  authenticator:
                    type: string
                    doc: AuthenticatorID specifies the name of the extension to use in order to authenticate the incoming data point.
                doc: Auth for this receiver
              dialer:
                type: map
                children:
                  timeout:
                    type: duration
                    doc: Timeout is the maximum amount of time a dial will wait for a connect to complete. The default is no timeout.
                doc: DialerConfig contains options for connecting to an address.
              endpoint:
                type: string
                doc: Endpoint configures the address for this network connection. For TCP and UDP networks, the address has the form "host:port". The host must be a literal IP address, or a host name that can be resolved to IP addresses. The port must be a literal port number or a service name. If the host is a literal IPv6 address it must be enclosed in square brackets, as in "[2001:db8::1]:80" or "[fe80::1%zone]:80". The zone specifies the scope of the literal IPv6 address as defined in RFC 4007.
              include_metadata:
                type: bool
                doc: Include propagates the incoming connection's metadata to downstream consumers.
              keepalive:
                type: map
                children:
                  enforcement_policy:
                    type: map
                    children:
                      min_time:
                        type: duration
                      permit_without_stream:
                        type: bool
                    doc: KeepaliveEnforcementPolicy allow configuration of the keepalive.EnforcementPolicy. The same default values as keepalive.EnforcementPolicy are applicable and get applied by the server. See https://godoc.org/google.golang.org/grpc/keepalive#EnforcementPolicy for details.
                  server_parameters:
                    type: map
                    children:
                      max_connection_age:
                        type: duration
                      max_connection_age_grace:
                        type: duration
                      max_connection_idle:
                        type: duration
                      time:
                        type: duration
                      timeout:
                        type: duration
                    doc: KeepaliveServerParameters allow configuration of the keepalive.ServerParameters. The same default values as keepalive.ServerParameters are applicable and get applied by the server. See https://godoc.org/google.golang.org/grpc/keepalive#ServerParameters for details.
                doc: Keepalive anchor for all the settings related to keepalive.
              max_concurrent_streams:
                type: int
                doc: MaxConcurrentStreams sets the limit on the number of concurrent streams to each ServerTransport. It has effect only for streaming RPCs.
              max_recv_msg_size_mib:
                type: int
                doc: MaxRecvMsgSizeMiB sets the maximum size (in MiB) of messages accepted by the server.
              middlewares:
                type: list
                children:
                  item:
                    type: map
                    children:
                      id:
                        type: string
                        doc: ID specifies the name of the extension to use.
                    doc: Middleware defines the extension ID for a middleware component.
                doc: Middlewares for the gRPC server.
              read_buffer_size:
                type: int
                doc: ReadBufferSize for gRPC server. See grpc.ReadBufferSize. (https://godoc.org/google.golang.org/grpc#ReadBufferSize).
              tls:
                type: map
                children:
                  ca_file:
                    type: string
                    doc: Path to the CA cert. For a client this verifies the server certificate. For a server this verifies client certificates. If empty uses system root CA. (optional)
                  ca_pem:
                    type: string
                    doc: In memory PEM encoded cert. (optional)
                  cert_file:
                    type: string
                    doc: Path to the TLS cert to use for TLS required connections. (optional)
                  cert_pem:
                    type: string
                    doc: In memory PEM encoded TLS cert to use for TLS required connections. (optional)
                  cipher_suites:
                    type: list
                    children:
                      item:
                        type: string
                    doc: CipherSuites is a list of TLS cipher suites that the TLS transport can use. If left blank, a safe default list is used. See https://go.dev/src/crypto/tls/cipher_suites.go for a list of supported cipher suites.
                  client_ca_file:
                    type: string
                    doc: Path to the TLS cert to use by the server to verify a client certificate. (optional) This sets the ClientCAs and ClientAuth to RequireAndVerifyClientCert in the TLSConfig. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional)
                  client_ca_file_reload:
                    type: bool
                    doc: Reload the ClientCAs file when it is modified (optional, default false)
                  curve_preferences:
                    type: list
                    children:
                      item:
                        type: string
                    doc: contains the elliptic curves that will be used in an ECDHE handshake, in preference order Defaults to empty list and "crypto/tls" defaults are used, internally.
                  include_system_ca_certs_pool:
                    type: bool
                    doc: If true, load system CA certificates pool in addition to the certificates configured in this struct.
                  key_file:
                    type: string
                    doc: Path to the TLS key to use for TLS required connections. (optional)
                  key_pem:
                    type: string
                    doc: In memory PEM encoded TLS key to use for TLS required connections. (optional)
                  max_version:
                    type: string
                    doc: MaxVersion sets the maximum TLS version that is acceptable. If not set, refer to crypto/tls for defaults. (optional)
                  min_version:
                    type: string
                    doc: MinVersion sets the minimum TLS version that is acceptable. If not set, TLS 1.2 will be used. (optional)
                  reload_interval:
                    type: duration
                    doc: ReloadInterval specifies the duration after which the certificate will be reloaded If not set, it will never be reloaded (optional)
                  tpm:
                    type: map
                    children:
                      auth:
                        type: string
                      enabled:
                        type: bool
                      owner_auth:
                        type: string
                      path:
                        type: string
                        doc: The path to the TPM device or Unix domain socket. For instance /dev/tpm0 or /dev/tpmrm0.
                    doc: Trusted platform module configuration
                doc: Configures the protocol to use TLS. The default value is nil, which will cause the protocol to not use TLS.
              transport:
                type: string
                doc: Transport to use. Allowed protocols are "tcp", "tcp4" (IPv4-only), "tcp6" (IPv6-only), "udp", "udp4" (IPv4-only), "udp6" (IPv6-only), "ip", "ip4" (IPv4-only), "ip6" (IPv6-only), "unix", "unixgram", "unixpacket" and "npipe" (Windows named pipes, Windows-only).
              write_buffer_size:
                type: int
                doc: WriteBufferSize for gRPC server. See grpc.WriteBufferSize. (https://godoc.org/google.golang.org/grpc#WriteBufferSize).
            doc: ServerConfig defines common settings for a gRPC server configuration.
          protocols:
            type: map
            children:
              grpc:
                type: map
                children:
                  auth:
                    type: map
                    children:
                      authenticator:
                        type: map
                        open: true
                  dialer:
                    type: map
                    children:
                      timeout:
                        type: duration
                  endpoint:
                    type: string
                  include_metadata:
                    type: bool
                  keepalive:
                    type: map
                    children:
                      enforcement_policy:
                        type: map
                        children:
                          min_time:
                            type: duration
                          permit_without_stream:
                            type: bool
                      server_parameters:
                        type: map
                        children:
                          max_connection_age:
                            type: duration
                          max_connection_age_grace:
                            type: duration
                          max_connection_idle:
                            type: duration
                          time:
                            type: duration
                          timeout:
                            type: duration
                  max_concurrent_streams:
                    type: int
                  max_recv_msg_size_mib:
                    type: int
                  middlewares:
                    type: list
                    children:
                      item:
                        type: map
                        children:
                          id:
                            type: map
                            open: true
                  read_buffer_size:
                    type: int
                  tls:
                    type: map
                    children:
                      ca_file:
                        type: string
                      ca_pem:
                        type: string
                      cert_file:
                        type: string
                      cert_pem:
                        type: string
                      cipher_suites:
                        type: list
                        children:
                          item:
                            type: string
                      client_ca_file:
                        type: string
                      client_ca_file_reload:
                        type: bool
                      curve_preferences:
                        type: list
                        children:
                          item:
                            type: string
                      include_insecure_cipher_suites:
                        type: bool
                      include_system_ca_certs_pool:
                        type: bool
                      key_file:
                        type: string
                      key_pem:
                        type: string
                      max_version:
                        type: string
                      min_version:
                        type: string
                      reload_interval:
                        type: duration
                      tpm:
                        type: map
                        children:
                          auth:
                            type: string
                          enabled:
                            type: bool
                          owner_auth:
                            type: string
                          path:
                            type: string
                  transport:
                    type: string
                  write_buffer_size:
                    type: int
              thrift_binary:
                type: map
                children:
                  endpoint:
                    type: string
                  max_packet_size:
                    type: int
                  queue_size:
                    type: int
                  socket_buffer_size:
                    type: int
                  workers:
                    type: int
              thrift_compact:
                type: map
                children:
                  endpoint:
                    type: string
                  max_packet_size:
                    type: int
                  queue_size:
                    type: int
                  socket_buffer_size:
                    type: int
                  workers:
                    type: int
              thrift_http:
                type: map
                children:
                  auth:
                    type: map
                    children:
                      authenticator:
                        type: map
                        open: true
                      request_params:
                        type: list
                        children:
                          item:
                            type: string
                  compression_algorithms:
                    type: list
                    children:
                      item:
                        type: string
                  cors:
                    type: map
                    children:
                      allowed_headers:
                        type: list
                        children:
                          item:
                            type: string
                      allowed_origins:
                        type: list
                        children:
                          item:
                            type: string
                      exposed_headers:
                        type: list
                        children:
                          item:
                            type: string
                      max_age:
                        type: int
                  dialer:
                    type: map
                    children:
                      timeout:
                        type: duration
                  endpoint:
                    type: string
                  idle_timeout:
                    type: duration
                  include_metadata:
                    type: bool
                  keep_alives_enabled:
                    type: bool
                  max_request_body_size:
                    type: int
                  middlewares:
                    type: list
                    children:
                      item:
                        type: map
                        children:
                          id:
                            type: map
                            open: true
                  read_header_timeout:
                    type: duration
                  read_timeout:
                    type: duration
                  response_headers: {}
                  tls:
                    type: map
                    children:
                      ca_file:
                        type: string
                      ca_pem:
                        type: string
                      cert_file:
                        type: string
                      cert_pem:
                        type: string
                      cipher_suites:
                        type: list
                        children:
                          item:
                            type: string
                      client_ca_file:
                        type: string
                      client_ca_file_reload:
                        type: bool
                      curve_preferences:
                        type: list
                        children:
                          item:
                            type: string
                      include_insecure_cipher_suites:
                        type: bool
                      include_system_ca_certs_pool:
                        type: bool
                      key_file:
                        type: string
                      key_pem:
                        type: string
                      max_version:
                        type: string
                      min_version:
                        type: string
                      reload_interval:
                        type: duration
                      tpm:
                        type: map
                        children:
                          auth:
                            type: string
                          enabled:
                            type: bool
                          owner_auth:
                            type: string
                          path:
                            type: string
                  transport:
                    type: string
                  write_timeout:
                    type: duration
          remote_sampling:
            type: map
            children:
              auth:
                type: map
                children:
                  authenticator:
                    type: map
                    open: true
                    doc: AuthenticatorID specifies the name of the extension to use in order to authenticate the incoming data point.
                doc: Auth configuration for outgoing RPCs.
              authority:
                type: string
                doc: WithAuthority parameter configures client to rewrite ":authority" header (godoc.org/google.golang.org/grpc#WithAuthority)
              balancer_name:
                type: string
                doc: Sets the balancer in grpclb_policy to discover the servers. Default is pick_first. https://github.com/grpc/grpc-go/blob/master/examples/features/load_balancing/README.md
              compression:
                type: string
                doc: The compression key for supported compression types within collector.
              endpoint:
                type: string
                doc: The target to which the exporter is going to send traces or metrics, using the gRPC protocol. The valid syntax is described at https://github.com/grpc/grpc/blob/master/doc/naming.md.
              headers:
                children:
                  item:
                    type: map
                    children:
                      name:
                        type: string
                      value:
                        type: string
                        doc: String alias that is marshaled and printed in an opaque way. To recover the original value, cast it to a string.
                    doc: Pair is an element of a MapList, and consists of a name and an opaque value.
                doc: The headers associated with gRPC requests.
              host_endpoint:
                type: string
              keepalive:
                type: map
                children:
                  permit_without_stream:
                    type: bool
                  time:
                    type: duration
                  timeout:
                    type: duration
                doc: The keepalive parameters for gRPC client. See grpc.WithKeepaliveParams. (https://godoc.org/google.golang.org/grpc#WithKeepaliveParams).
              middlewares:
                type: list
                children:
                  item:
                    type: map
                    children:
                      id:
                        type: map
                        open: true
                        doc: ID specifies the name of the extension to use.
                    doc: Middleware defines the extension ID for a middleware component.
                doc: Middlewares for the gRPC client.
              read_buffer_size:
                type: int
                doc: ReadBufferSize for gRPC client. See grpc.WithReadBufferSize. (https://godoc.org/google.golang.org/grpc#WithReadBufferSize).
              strategy_file:
                type: string
              strategy_file_reload_interval:
                type: duration
              tls:
                type: map
                children:
                  ca_file:
                    type: string
                    doc: Path to the CA cert. For a client this verifies the server certificate. For a server this verifies client certificates. If empty uses system root CA. (optional)
                  ca_pem:
                    type: string
                    doc: In memory PEM encoded cert. (optional)
                  cert_file:
                    type: string
                    doc: Path to the TLS cert to use for TLS required connections. (optional)
                  cert_pem:
                    type: string
                    doc: In memory PEM encoded TLS cert to use for TLS required connections. (optional)
                  cipher_suites:
                    type: list
                    children:
                      item:
                        type: string
                    doc: CipherSuites is a list of TLS cipher suites that the TLS transport can use. If left blank, a safe default list is used. See https://go.dev/src/crypto/tls/cipher_suites.go for a list of supported cipher suites.
                  curve_preferences:
                    type: list
                    children:
                      item:
                        type: string
                    doc: contains the elliptic curves that will be used in an ECDHE handshake, in preference order Defaults to empty list and "crypto/tls" defaults are used, internally.
                  include_insecure_cipher_suites:
                    type: bool
                  include_system_ca_certs_pool:
                    type: bool
                    doc: If true, load system CA certificates pool in addition to the certificates configured in this struct.
                  insecure:
                    type: bool
                    doc: In gRPC and HTTP when set to true, this is used to disable the client transport security. See https://godoc.org/google.golang.org/grpc#WithInsecure for gRPC. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional, default false)
                  insecure_skip_verify:
                    type: bool
                    doc: InsecureSkipVerify will enable TLS but not verify the certificate.
                  key_file:
                    type: string
                    doc: Path to the TLS key to use for TLS required connections. (optional)
                  key_pem:
                    type: string
                    doc: In memory PEM encoded TLS key to use for TLS required connections. (optional)
                  max_version:
                    type: string
                    doc: MaxVersion sets the maximum TLS version that is acceptable. If not set, refer to crypto/tls for defaults. (optional)
                  min_version:
                    type: string
                    doc: MinVersion sets the minimum TLS version that is acceptable. If not set, TLS 1.2 will be used. (optional)
                  reload_interval:
                    type: duration
                    doc: ReloadInterval specifies the duration after which the certificate will be reloaded If not set, it will never be reloaded (optional)
                  server_name_override:
                    type: string
                    doc: ServerName requested by client for virtual hosting. This sets the ServerName in the TLSConfig. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional)
                  tpm:
                    type: map
                    children:
                      auth:
                        type: string
                      enabled:
                        type: bool
                      owner_auth:
                        type: string
                      path:
                        type: string
                        doc: The path to the TPM device or Unix domain socket. For instance /dev/tpm0 or /dev/tpmrm0.
                    doc: Trusted platform module configuration
                doc: TLS struct exposes TLS client configuration.
              user_agent:
                type: string
                doc: UserAgent overrides the default user-agent header sent on gRPC requests. The default is derived from the build info. When empty, the caller controls the user-agent via grpc.WithUserAgent or similar options.
              wait_for_ready:
                type: bool
                doc: WaitForReady parameter configures client to wait for ready state before sending data. (https://github.com/grpc/grpc/blob/master/doc/wait-for-ready.md)
              write_buffer_size:
                type: int
                doc: WriteBufferSize for gRPC gRPC. See grpc.WithWriteBufferSize. (https://godoc.org/google.golang.org/grpc#WithWriteBufferSize).
            doc: RemoteSamplingConfig defines config key for remote sampling fetch endpoint
          thrift_binary:
            type: map
            children:
              endpoint:
                type: string
              max_packet_size:
                type: int
              queue_size:
                type: int
              socket_buffer_size:
                type: int
              workers:
                type: int
            doc: ProtocolUDP is the configuration for a UDP protocol.
          thrift_compact:
            type: map
            children:
              endpoint:
                type: string
              max_packet_size:
                type: int
              queue_size:
                type: int
              socket_buffer_size:
                type: int
              workers:
                type: int
            doc: ProtocolUDP is the configuration for a UDP protocol.
          thrift_http:
            type: map
            children:
              auth:
                type: map
                children:
                  authenticator:
                    type: string
                    doc: AuthenticatorID specifies the name of the extension to use in order to authenticate the incoming data point.
                  request_params:
                    type: list
                    children:
                      item:
                        type: string
                    doc: RequestParameters is a list of parameters that should be extracted from the request and added to the context. When a parameter is found in both the query string and the header, the value from the query string will be used.
                doc: Auth for this receiver
              compression_algorithms:
                type: list
                children:
                  item:
                    type: string
                doc: 'CompressionAlgorithms configures the list of compression algorithms the server can accept. Default: ["", "gzip", "zstd", "zlib", "snappy", "deflate"]'
              cors:
                type: map
                children:
                  allowed_headers:
                    type: list
                    children:
                      item:
                        type: string
                    doc: AllowedHeaders sets what headers will be allowed in CORS requests. The Accept, Accept-Language, Content-Type, and Content-Language headers are implicitly allowed. If no headers are listed, X-Requested-With will also be accepted by default. Include "*" to allow any request header.
                  allowed_origins:
                    type: list
                    children:
                      item:
                        type: string
                    doc: AllowedOrigins sets the allowed values of the Origin header for HTTP/JSON requests to an OTLP receiver. An origin may contain a wildcard (*) to replace 0 or more characters (e.g., "http://*.domain.com", or "*" to allow any origin).
                  exposed_headers:
                    type: list
                    children:
                      item:
                        type: string
                    doc: ExposedHeaders sets the value of the Access-Control-Expose-Headers response header, indicating which headers are safe to expose to the API of a CORS response.
                  max_age:
                    type: int
                    doc: MaxAge sets the value of the Access-Control-Max-Age response header. Set it to the number of seconds that browsers should cache a CORS preflight response for.
                doc: CORS configures the server for HTTP cross-origin resource sharing (CORS).
              endpoint:
                type: string
                doc: Endpoint configures the listening address for the server.
              idle_timeout:
                type: duration
                doc: IdleTimeout is the maximum amount of time to wait for the next request when keep-alives are enabled. If IdleTimeout is zero, the value of ReadTimeout is used. If both are zero, there is no timeout.
              include_metadata:
                type: bool
                doc: IncludeMetadata propagates the client metadata from the incoming requests to the downstream consumers
              keep_alives_enabled:
                type: bool
                doc: KeepAlivesEnabled controls whether HTTP keep-alives are enabled. By default, keep-alives are always enabled. Only very resource-constrained environments should disable them.
              max_request_body_size:
                type: int
                doc: 'MaxRequestBodySize sets the maximum request body size in bytes. Default: 20MiB.'
              middlewares:
                type: list
                children:
                  item:
                    type: map
                    children:
                      id:
                        type: string
                        doc: ID specifies the name of the extension to use.
                    doc: Middleware defines the extension ID for a middleware component.
                doc: Middlewares are used to add custom functionality to the HTTP server. Middleware handlers are called in the order they appear in this list, with the first middleware becoming the outermost handler.
              read_header_timeout:
                type: duration
                doc: ReadHeaderTimeout is the amount of time allowed to read request headers. The connection's read deadline is reset after reading the headers and the Handler can decide what is considered too slow for the body. If ReadHeaderTimeout is zero, the value of ReadTimeout is used. If both are zero, there is no timeout.
              read_timeout:
                type: duration
                doc: ReadTimeout is the maximum duration for reading the entire request, including the body. A zero or negative value means there will be no timeout. Because ReadTimeout does not let Handlers make per-request decisions on each request body's acceptable deadline or upload rate, most users will prefer to use ReadHeaderTimeout. It is valid to use them both.
              response_headers:
                type: list
                children:
                  item:
                    type: map
                    children:
                      name:
                        type: string
                      value:
                        type: string
                        doc: String alias that is marshaled and printed in an opaque way. To recover the original value, cast it to a string.
                    doc: Pair is an element of a MapList, and consists of a name and an opaque value.
                doc: Additional headers attached to each HTTP response sent to the client. Header values are opaque since they may be sensitive.
              tls:
                type: map
                children:
                  ca_file:
                    type: string
                    doc: Path to the CA cert. For a client this verifies the server certificate. For a server this verifies client certificates. If empty uses system root CA. (optional)
                  ca_pem:
                    type: string
                    doc: In memory PEM encoded cert. (optional)
                  cert_file:
                    type: string
                    doc: Path to the TLS cert to use for TLS required connections. (optional)
                  cert_pem:
                    type: string
                    doc: In memory PEM encoded TLS cert to use for TLS required connections. (optional)
                  cipher_suites:
                    type: list
                    children:
                      item:
                        type: string
                    doc: CipherSuites is a list of TLS cipher suites that the TLS transport can use. If left blank, a safe default list is used. See https://go.dev/src/crypto/tls/cipher_suites.go for a list of supported cipher suites.
                  client_ca_file:
                    type: string
                    doc: Path to the TLS cert to use by the server to verify a client certificate. (optional) This sets the ClientCAs and ClientAuth to RequireAndVerifyClientCert in the TLSConfig. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional)
                  client_ca_file_reload:
                    type: bool
                    doc: Reload the ClientCAs file when it is modified (optional, default false)
                  curve_preferences:
                    type: list
                    children:
                      item:
                        type: string
                    doc: contains the elliptic curves that will be used in an ECDHE handshake, in preference order Defaults to empty list and "crypto/tls" defaults are used, internally.
                  include_system_ca_certs_pool:
                    type: bool
                    doc: If true, load system CA certificates pool in addition to the certificates configured in this struct.
                  key_file:
                    type: string
                    doc: Path to the TLS key to use for TLS required connections. (optional)
                  key_pem:
                    type: string
                    doc: In memory PEM encoded TLS key to use for TLS required connections. (optional)
                  max_version:
                    type: string
                    doc: MaxVersion sets the maximum TLS version that is acceptable. If not set, refer to crypto/tls for defaults. (optional)
                  min_version:
                    type: string
                    doc: MinVersion sets the minimum TLS version that is acceptable. If not set, TLS 1.2 will be used. (optional)
                  reload_interval:
                    type: duration
                    doc: ReloadInterval specifies the duration after which the certificate will be reloaded If not set, it will never be reloaded (optional)
                  tpm:
                    type: map
                    children:
                      auth:
                        type: string
                      enabled:
                        type: bool
                      owner_auth:
                        type: string
                      path:
                        type: string
                        doc: The path to the TPM device or Unix domain socket. For instance /dev/tpm0 or /dev/tpmrm0.
                    doc: Trusted platform module configuration
                doc: TLS struct exposes TLS client configuration.
              write_timeout:
                type: duration
                doc: WriteTimeout is the maximum duration before timing out writes of the response. It is reset whenever a new request's header is read. Like ReadTimeout, it does not let Handlers make decisions on a per-request basis. A zero or negative value means there will be no timeout.
            doc: ServerConfig defines settings for creating an HTTP server.
        doc: Config defines configuration for Jaeger receiver.
    kafka:
      type: kafka
      signals:
        - traces
        - metrics
        - logs
        - profiles
      stability:
        logs: beta
        metrics: beta
        profiles: development
        traces: beta
      module: github.com/open-telemetry/opentelemetry-collector-contrib/receiver/kafkareceiver
      fields:
        type: map
        children:
          auth:
            type: map
            children:
              kerberos:
                type: map
                children:
                  config_file:
                    type: string
                  disable_fast_negotiation:
                    type: bool
                  keytab_file:
                    type: string
                  password:
                    type: string
                  realm:
                    type: string
                  service_name:
                    type: string
                  use_keytab:
                    type: bool
                  username:
                    type: string
                doc: Kerberos holds Kerberos authentication configuration.
              plain_text:
                type: map
                children:
                  password:
                    type: string
                  username:
                    type: string
                doc: 'PlainText is an alias for SASL/PLAIN authentication. Deprecated [v0.123.0]: use SASL with Mechanism set to PLAIN instead.'
              sasl:
                type: map
                children:
                  aws_msk:
                    type: map
                    children:
                      region:
                        type: string
                        doc: Region is the AWS region the MSK cluster is based in
                    doc: AWSMSK holds configuration specific to AWS MSK.
                  mechanism:
                    type: string
                    doc: 'SASL Mechanism to be used, possible values are: (PLAIN, AWS_MSK_IAM_OAUTHBEARER, OAUTHBEARER, SCRAM-SHA-256 or SCRAM-SHA-512).'
                  oauthbearer_token_source:
                    type: map
                    open: true
                    doc: ID of type "extension" providing a TokenSource for OAUTHBEARER Mechanism, typically named "oauth2client"
                  password:
                    type: string
                    doc: Password to be used on authentication
                  username:
                    type: string
                    doc: Username to be used on authentication
                  version:
                    type: int
                    doc: 'Version is ignored, and exists for backwards compatibility in config parsing. Deprecated [v0.153.0]: this field is a no-op since the migration to franz-go, which negotiates the SASL handshake version automatically. This config will be removed in a future release.'
                doc: SASL holds SASL authentication configuration.
              tls:
                type: map
                children:
                  ca_file:
                    type: string
                    doc: Path to the CA cert. For a client this verifies the server certificate. For a server this verifies client certificates. If empty uses system root CA. (optional)
                  ca_pem:
                    type: string
                    doc: In memory PEM encoded cert. (optional)
                  cert_file:
                    type: string
                    doc: Path to the TLS cert to use for TLS required connections. (optional)
                  cert_pem:
                    type: string
                    doc: In memory PEM encoded TLS cert to use for TLS required connections. (optional)
                  cipher_suites:
                    type: list
                    children:
                      item:
                        type: string
                    doc: CipherSuites is a list of TLS cipher suites that the TLS transport can use. If left blank, a safe default list is used. See https://go.dev/src/crypto/tls/cipher_suites.go for a list of supported cipher suites.
                  curve_preferences:
                    type: list
                    children:
                      item:
                        type: string
                    doc: contains the elliptic curves that will be used in an ECDHE handshake, in preference order Defaults to empty list and "crypto/tls" defaults are used, internally.
                  include_insecure_cipher_suites:
                    type: bool
                  include_system_ca_certs_pool:
                    type: bool
                    doc: If true, load system CA certificates pool in addition to the certificates configured in this struct.
                  insecure:
                    type: bool
                    doc: In gRPC and HTTP when set to true, this is used to disable the client transport security. See https://godoc.org/google.golang.org/grpc#WithInsecure for gRPC. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional, default false)
                  insecure_skip_verify:
                    type: bool
                    doc: InsecureSkipVerify will enable TLS but not verify the certificate.
                  key_file:
                    type: string
                    doc: Path to the TLS key to use for TLS required connections. (optional)
                  key_pem:
                    type: string
                    doc: In memory PEM encoded TLS key to use for TLS required connections. (optional)
                  max_version:
                    type: string
                    doc: MaxVersion sets the maximum TLS version that is acceptable. If not set, refer to crypto/tls for defaults. (optional)
                  min_version:
                    type: string
                    doc: MinVersion sets the minimum TLS version that is acceptable. If not set, TLS 1.2 will be used. (optional)
                  reload_interval:
                    type: duration
                    doc: ReloadInterval specifies the duration after which the certificate will be reloaded If not set, it will never be reloaded (optional)
                  server_name_override:
                    type: string
                    doc: ServerName requested by client for virtual hosting. This sets the ServerName in the TLSConfig. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional)
                  tpm:
                    type: map
                    children:
                      auth:
                        type: string
                      enabled:
                        type: bool
                      owner_auth:
                        type: string
                      path:
                        type: string
                        doc: The path to the TPM device or Unix domain socket. For instance /dev/tpm0 or /dev/tpmrm0.
                    doc: Trusted platform module configuration
                doc: 'TLS holds TLS configuration for connecting to Kafka brokers. Deprecated [v0.124.0]: use ClientConfig.TLS instead. This will be used only if ClientConfig.TLS is not set.'
            doc: Authentication holds Kafka authentication details.
          autocommit:
            type: map
            children:
              enable:
                type: bool
                doc: Whether or not to auto-commit updated offsets back to the broker. (default enabled).
              interval:
                type: duration
                doc: How frequently to commit updated offsets. Ineffective unless auto-commit is enabled (default 1s)
            doc: AutoCommit controls the auto-commit functionality of the consumer.
          brokers:
            type: list
            children:
              item:
                type: string
            doc: Brokers holds the list of Kafka bootstrap servers (default localhost:9092).
          client_id:
            type: string
            doc: ClientID holds the client ID advertised to Kafka, which can be used for enforcing ACLs, throttling quotas, and more (default "otel-collector")
          conn_idle_timeout:
            type: duration
            doc: ConnIdleTimeout specifies the time after which idle connections are not reused and may be closed.
          error_backoff:
            type: map
            children:
              enabled:
                type: bool
                doc: Enabled indicates whether to not retry sending batches in case of export failure.
              initial_interval:
                type: duration
                doc: InitialInterval the time to wait after the first failure before retrying.
              max_elapsed_time:
                type: duration
                doc: MaxElapsedTime is the maximum amount of time (including retries) spent trying to send a request/batch. Once this value is reached, the data is discarded. If set to 0, the retries are never stopped.
              max_interval:
                type: duration
                doc: MaxInterval is the upper bound on backoff interval. Once this value is reached the delay between consecutive retries will always be `MaxInterval`.
              multiplier:
                type: float
                doc: Multiplier is the value multiplied by the backoff interval bounds
              randomization_factor:
                type: float
                doc: RandomizationFactor is a random factor used to calculate next backoffs Randomized interval = RetryInterval * (1 ± RandomizationFactor)
            doc: ErrorBackoff controls backoff/retry behavior when the next consumer returns an error.
          group_id:
            type: string
            doc: GroupID specifies the ID of the consumer group that will be consuming messages from (default "otel-collector").
          group_instance_id:
            type: string
            doc: GroupInstanceID specifies the ID of the consumer
          group_rebalance_strategies:
            type: list
            children:
              item:
                type: string
                doc: GroupRebalanceStrategy defines the strategy to use for partition assignment.
            doc: GroupRebalanceStrategies specifies the ordered strategies to advertise for partition assignment. Kafka selects the first strategy supported by every member of the consumer group. Built-in values are "range", "roundrobin", "sticky", and "cooperative-sticky". Any other value is treated as the component ID of a registered extension that implements kgo.GroupBalancer. When omitted, the franz-go default applies, currently "cooperative-sticky". This field is mutually exclusive with GroupRebalanceStrategy.
          group_rebalance_strategy:
            type: string
            doc: 'GroupRebalanceStrategy specifies the strategy to use for partition assignment. Accepts the same values as GroupRebalanceStrategies. Defaults to "cooperative-sticky". Deprecated [v0.154.0]: use GroupRebalanceStrategies instead.'
          header_extraction:
            type: map
            children:
              extract_headers:
                type: bool
              headers:
                type: list
                children:
                  item:
                    type: string
            doc: HeaderExtraction controls extraction of headers from Kafka records.
          heartbeat_interval:
            type: duration
            doc: HeartbeatInterval controls the Kafka consumer group coordination heartbeat interval. Heartbeats ensure the consumer's session remains active.
          initial_offset:
            type: string
            doc: InitialOffset specifies the initial offset to use if no offset was previously committed. Must be `latest` or `earliest` (default "latest").
          logs:
            type: map
            children:
              encoding:
                type: string
                doc: Encoding holds the expected encoding of messages for the signal type Defaults to "otlp_proto".
              exclude_topics:
                type: list
                children:
                  item:
                    type: string
                doc: Optional exclude topics option, used only in regex mode.
              topics:
                type: list
                children:
                  item:
                    type: string
                doc: 'Topics holds the name of the Kafka topics from which messages of the signal type should be consumed. The default depends on the signal type: - "otlp_spans" for traces - "otlp_metrics" for metrics - "otlp_logs" for logs - "otlp_profiles" for profiles'
            doc: Logs holds configuration about how logs should be consumed.
          max_fetch_size:
            type: int
            doc: The maximum bytes per fetch from Kafka (default "1048576")
          max_fetch_wait:
            type: duration
            doc: The maximum amount of time to wait for MinFetchSize bytes to be available before the broker returns a response (default 250ms)
          max_partition_fetch_size:
            type: int
            doc: MaxPartitionFetchSize defines the maximum number of bytes to fetch per partition (default "1048576")
          message_marking:
            type: map
            children:
              after:
                type: bool
                doc: If true, the messages are marked after the pipeline execution
              on_error:
                type: bool
                doc: 'If false, only the successfully processed messages are marked. This only applies to non-permanent errors. It has no impact if After is set to false. Note: this can block the entire partition in case a message processing returns a non-permanent error.'
              on_permanent_error:
                type: bool
                doc: 'If false, only the successfully processed messages are marked. This only applies to permanent errors. It has no impact if After is set to false. Default value inherits from OnError for backward compatibility. Note: this can block the entire partition in case a message processing returns a permanent error.'
            doc: MessageMarking controls the way the messages are marked as consumed.
          metadata:
            type: map
            children:
              full:
                type: bool
                doc: Whether to maintain a full set of metadata for all topics, or just the minimal set that has been necessary so far. The full set is simpler and usually more convenient, but can take up a substantial amount of memory if you have many topics and partitions. Defaults to true.
              refresh_interval:
                type: duration
                doc: RefreshInterval controls the frequency at which cluster metadata is refreshed. Defaults to 10 minutes.
              retry:
                type: map
                children:
                  backoff:
                    type: duration
                    doc: How long to wait for leader election to occur before retrying (default 250ms). Similar to the JVM's `retry.backoff.ms`.
                  max:
                    type: int
                    doc: The total number of times to retry a metadata request when the cluster is in the middle of a leader election or at startup (default 3).
                doc: Retry configuration for metadata. This configuration is useful to avoid race conditions when broker is starting at the same time as collector.
            doc: Metadata holds metadata-related configuration for producers and consumers.
          metrics:
            type: map
            children:
              encoding:
                type: string
                doc: Encoding holds the expected encoding of messages for the signal type Defaults to "otlp_proto".
              exclude_topics:
                type: list
                children:
                  item:
                    type: string
                doc: Optional exclude topics option, used only in regex mode.
              topics:
                type: list
                children:
                  item:
                    type: string
                doc: 'Topics holds the name of the Kafka topics from which messages of the signal type should be consumed. The default depends on the signal type: - "otlp_spans" for traces - "otlp_metrics" for metrics - "otlp_logs" for logs - "otlp_profiles" for profiles'
            doc: Metrics holds configuration about how metrics should be consumed.
          min_fetch_size:
            type: int
            doc: The minimum bytes per fetch from Kafka (default "1")
          profiles:
            type: map
            children:
              encoding:
                type: string
                doc: Encoding holds the expected encoding of messages for the signal type Defaults to "otlp_proto".
              exclude_topics:
                type: list
                children:
                  item:
                    type: string
                doc: Optional exclude topics option, used only in regex mode.
              topics:
                type: list
                children:
                  item:
                    type: string
                doc: 'Topics holds the name of the Kafka topics from which messages of the signal type should be consumed. The default depends on the signal type: - "otlp_spans" for traces - "otlp_metrics" for metrics - "otlp_logs" for logs - "otlp_profiles" for profiles'
            doc: Profiles holds configuration about how profiles should be consumed.
          protocol_version:
            type: string
            doc: ProtocolVersion defines the Kafka protocol version that the client will assume it is running against.
          rack_id:
            type: string
            doc: RackID provides the rack identifier for this client to enable rack-aware replica selection when supported by the brokers. This maps to Kafka's standard "client.rack" setting. By default, this is empty.
          resolve_canonical_bootstrap_servers_only:
            type: bool
            doc: 'ResolveCanonicalBootstrapServersOnly is ignored, and exists for backwards compatibility in config parsing. Deprecated [v0.153.0]: this field is a no-op since the migration to franz-go, which has no direct equivalent to the associated Sarama config. This config will be removed in a future release.'
          session_timeout:
            type: duration
            doc: SessionTimeout controls the Kafka consumer group session timeout. The session timeout is used to detect the consumer's liveness.
          telemetry:
            type: map
            children:
              metrics:
                type: map
                children:
                  kafka_receiver_records_delay:
                    type: map
                    children:
                      enabled:
                        type: bool
                    doc: KafkaReceiverRecordsDelay controls whether the metric kafka_receiver_records_delay that measures the time in seconds between producing and receiving a batch of records will be reported or not. This metric is not reported by default because it may slow down high-volume consuming.
                doc: MetricsConfig provides config for optional receiver metrics.
            doc: Telemetry controls optional telemetry configuration.
          tls:
            type: map
            children:
              ca_file:
                type: string
                doc: Path to the CA cert. For a client this verifies the server certificate. For a server this verifies client certificates. If empty uses system root CA. (optional)
              ca_pem:
                type: string
                doc: In memory PEM encoded cert. (optional)
              cert_file:
                type: string
                doc: Path to the TLS cert to use for TLS required connections. (optional)
              cert_pem:
                type: string
                doc: In memory PEM encoded TLS cert to use for TLS required connections. (optional)
              cipher_suites:
                type: list
                children:
                  item:
                    type: string
                doc: CipherSuites is a list of TLS cipher suites that the TLS transport can use. If left blank, a safe default list is used. See https://go.dev/src/crypto/tls/cipher_suites.go for a list of supported cipher suites.
              curve_preferences:
                type: list
                children:
                  item:
                    type: string
                doc: contains the elliptic curves that will be used in an ECDHE handshake, in preference order Defaults to empty list and "crypto/tls" defaults are used, internally.
              include_insecure_cipher_suites:
                type: bool
              include_system_ca_certs_pool:
                type: bool
                doc: If true, load system CA certificates pool in addition to the certificates configured in this struct.
              insecure:
                type: bool
                doc: In gRPC and HTTP when set to true, this is used to disable the client transport security. See https://godoc.org/google.golang.org/grpc#WithInsecure for gRPC. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional, default false)
              insecure_skip_verify:
                type: bool
                doc: InsecureSkipVerify will enable TLS but not verify the certificate.
              key_file:
                type: string
                doc: Path to the TLS key to use for TLS required connections. (optional)
              key_pem:
                type: string
                doc: In memory PEM encoded TLS key to use for TLS required connections. (optional)
              max_version:
                type: string
                doc: MaxVersion sets the maximum TLS version that is acceptable. If not set, refer to crypto/tls for defaults. (optional)
              min_version:
                type: string
                doc: MinVersion sets the minimum TLS version that is acceptable. If not set, TLS 1.2 will be used. (optional)
              reload_interval:
                type: duration
                doc: ReloadInterval specifies the duration after which the certificate will be reloaded If not set, it will never be reloaded (optional)
              server_name_override:
                type: string
                doc: ServerName requested by client for virtual hosting. This sets the ServerName in the TLSConfig. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional)
              tpm:
                type: map
                children:
                  auth:
                    type: string
                  enabled:
                    type: bool
                  owner_auth:
                    type: string
                  path:
                    type: string
                    doc: The path to the TPM device or Unix domain socket. For instance /dev/tpm0 or /dev/tpmrm0.
                doc: Trusted platform module configuration
            doc: TLS holds TLS-related configuration for connecting to Kafka brokers. By default the client will use an insecure connection unless SASL/AWS_MSK_IAM_OAUTHBEARER auth is configured.
          traces:
            type: map
            children:
              encoding:
                type: string
                doc: Encoding holds the expected encoding of messages for the signal type Defaults to "otlp_proto".
              exclude_topics:
                type: list
                children:
                  item:
                    type: string
                doc: Optional exclude topics option, used only in regex mode.
              topics:
                type: list
                children:
                  item:
                    type: string
                doc: 'Topics holds the name of the Kafka topics from which messages of the signal type should be consumed. The default depends on the signal type: - "otlp_spans" for traces - "otlp_metrics" for metrics - "otlp_logs" for logs - "otlp_profiles" for profiles'
            doc: Traces holds configuration about how traces should be consumed.
          use_leader_epoch:
            type: bool
            doc: 'When enabled, the consumer uses the leader epoch returned by brokers (KIP-320) to detect log truncation. Setting this to false clears the leader epoch from fetch offsets, disabling KIP-320. Disabling can improve compatibility with brokers that don’t fully support leader epochs (e.g., Azure Event Hubs), at the cost of losing automatic log-truncation safety. NOTE: this is experimental and may be removed in a future release.'
        doc: Config defines configuration for Kafka receiver.
    nop:
      type: nop
      signals:
        - traces
        - metrics
        - logs
        - profiles
      stability:
        logs: beta
        metrics: beta
        profiles: alpha
        traces: beta
      module: go.opentelemetry.io/collector/receiver/nopreceiver
    otlp:
      type: otlp
      signals:
        - traces
        - metrics
        - logs
        - profiles
      stability:
        logs: stable
        metrics: stable
        profiles: alpha
        traces: stable
      module: go.opentelemetry.io/collector/receiver/otlpreceiver
      fields:
        type: map
        children:
          protocols:
            type: map
            children:
              grpc:
                type: map
                children:
                  auth:
                    type: map
                    children:
                      authenticator:
                        type: string
                        doc: id of an authenticator extension
                  endpoint:
                    type: string
                    doc: host:port to listen on, e.g. 0.0.0.0:4317
                  include_metadata:
                    type: bool
                  keepalive:
                    type: map
                    children:
                      enforcement_policy:
                        type: map
                        children:
                          min_time:
                            type: duration
                          permit_without_stream:
                            type: bool
                      server_parameters:
                        type: map
                        children:
                          max_connection_age:
                            type: duration
                          max_connection_age_grace:
                            type: duration
                          max_connection_idle:
                            type: duration
                          time:
                            type: duration
                          timeout:
                            type: duration
                  max_concurrent_streams:
                    type: int
                  max_recv_msg_size_mib:
                    type: int
                  read_buffer_size:
                    type: int
                  tls:
                    type: map
                    children:
                      ca_file:
                        type: string
                      ca_pem:
                        type: string
                      cert_file:
                        type: string
                      cert_pem:
                        type: string
                      cipher_suites:
                        type: list
                        children:
                          item:
                            type: string
                      client_ca_file:
                        type: string
                      client_ca_file_reload:
                        type: bool
                      include_system_ca_certs_pool:
                        type: bool
                      key_file:
                        type: string
                      key_pem:
                        type: string
                      max_version:
                        type: string
                      min_version:
                        type: string
                      reload_interval:
                        type: duration
                  transport:
                    type: string
                    enum:
                      - tcp
                      - tcp4
                      - tcp6
                      - udp
                      - udp4
                      - udp6
                      - ip
                      - ip4
                      - ip6
                      - unix
                      - unixgram
                      - unixpacket
                  write_buffer_size:
                    type: int
              http:
                type: map
                children:
                  auth:
                    type: map
                    children:
                      authenticator:
                        type: string
                  compression_algorithms:
                    type: list
                    children:
                      item:
                        type: string
                  cors:
                    type: map
                    children:
                      allowed_headers:
                        type: list
                        children:
                          item:
                            type: string
                      allowed_origins:
                        type: list
                        children:
                          item:
                            type: string
                      max_age:
                        type: int
                  endpoint:
                    type: string
                    doc: host:port to listen on, e.g. 0.0.0.0:4318
                  include_metadata:
                    type: bool
                  logs_url_path:
                    type: string
                  max_request_body_size:
                    type: int
                  metrics_url_path:
                    type: string
                  response_headers:
                    type: map
                    open: true
                  tls:
                    type: map
                    children:
                      ca_file:
                        type: string
                      ca_pem:
                        type: string
                      cert_file:
                        type: string
                      cert_pem:
                        type: string
                      cipher_suites:
                        type: list
                        children:
                          item:
                            type: string
                      client_ca_file:
                        type: string
                      client_ca_file_reload:
                        type: bool
                      include_system_ca_certs_pool:
                        type: bool
                      key_file:
                        type: string
                      key_pem:
                        type: string
                      max_version:
                        type: string
                      min_version:
                        type: string
                      reload_interval:
                        type: duration
                  traces_url_path:
                    type: string
            doc: at least one of grpc or http must be enabled
        required:
          - protocols
    prometheus:
      type: prometheus
      signals:
        - metrics
      stability:
        metrics: beta
      module: github.com/open-telemetry/opentelemetry-collector-contrib/receiver/prometheusreceiver
      fields:
        type: map
        children:
          api_server:
            type: map
            children:
              enabled:
                type: bool
              max_connections:
                type: int
              server_config:
                type: map
                children:
                  auth:
                    type: map
                    children:
                      authenticator:
                        type: map
                        open: true
                        doc: AuthenticatorID specifies the name of the extension to use in order to authenticate the incoming data point.
                      request_params:
                        type: list
                        children:
                          item:
                            type: string
                        doc: RequestParameters is a list of parameters that should be extracted from the request and added to the context. When a parameter is found in both the query string and the header, the value from the query string will be used.
                    doc: Auth for this receiver
                  compression_algorithms:
                    type: list
                    children:
                      item:
                        type: string
                    doc: 'CompressionAlgorithms configures the list of compression algorithms the server can accept. Default: ["", "gzip", "zstd", "zlib", "snappy", "deflate"]'
                  cors:
                    type: map
                    children:
                      allowed_headers:
                        type: list
                        children:
                          item:
                            type: string
                        doc: AllowedHeaders sets what headers will be allowed in CORS requests. The Accept, Accept-Language, Content-Type, and Content-Language headers are implicitly allowed. If no headers are listed, X-Requested-With will also be accepted by default. Include "*" to allow any request header.
                      allowed_origins:
                        type: list
                        children:
                          item:
                            type: string
                        doc: AllowedOrigins sets the allowed values of the Origin header for HTTP/JSON requests to an OTLP receiver. An origin may contain a wildcard (*) to replace 0 or more characters (e.g., "http://*.domain.com", or "*" to allow any origin).
                      exposed_headers:
                        type: list
                        children:
                          item:
                            type: string
                        doc: ExposedHeaders sets the value of the Access-Control-Expose-Headers response header, indicating which headers are safe to expose to the API of a CORS response.
                      max_age:
                        type: int
                        doc: MaxAge sets the value of the Access-Control-Max-Age response header. Set it to the number of seconds that browsers should cache a CORS preflight response for.
                    doc: CORS configures the server for HTTP cross-origin resource sharing (CORS).
                  dialer:
                    type: map
                    children:
                      timeout:
                        type: duration
                  endpoint:
                    type: string
                    doc: Endpoint configures the listening address for the server.
                  idle_timeout:
                    type: duration
                    doc: IdleTimeout is the maximum amount of time to wait for the next request when keep-alives are enabled. If IdleTimeout is zero, the value of ReadTimeout is used. If both are zero, there is no timeout.
                  include_metadata:
                    type: bool
                    doc: IncludeMetadata propagates the client metadata from the incoming requests to the downstream consumers
                  keep_alives_enabled:
                    type: bool
                    doc: KeepAlivesEnabled controls whether HTTP keep-alives are enabled. By default, keep-alives are always enabled. Only very resource-constrained environments should disable them.
                  max_request_body_size:
                    type: int
                    doc: 'MaxRequestBodySize sets the maximum request body size in bytes. Default: 20MiB.'
                  middlewares:
                    type: list
                    children:
                      item:
                        type: map
                        children:
                          id:
                            type: map
                            open: true
                            doc: ID specifies the name of the extension to use.
                        doc: Middleware defines the extension ID for a middleware component.
                    doc: Middlewares are used to add custom functionality to the HTTP server. Middleware handlers are called in the order they appear in this list, with the first middleware becoming the outermost handler.
                  read_header_timeout:
                    type: duration
                    doc: ReadHeaderTimeout is the amount of time allowed to read request headers. The connection's read deadline is reset after reading the headers and the Handler can decide what is considered too slow for the body. If ReadHeaderTimeout is zero, the value of ReadTimeout is used. If both are zero, there is no timeout.
                  read_timeout:
                    type: duration
                    doc: ReadTimeout is the maximum duration for reading the entire request, including the body. A zero or negative value means there will be no timeout. Because ReadTimeout does not let Handlers make per-request decisions on each request body's acceptable deadline or upload rate, most users will prefer to use ReadHeaderTimeout. It is valid to use them both.
                  response_headers:
                    children:
                      item:
                        type: map
                        children:
                          name:
                            type: string
                          value:
                            type: string
                            doc: String alias that is marshaled and printed in an opaque way. To recover the original value, cast it to a string.
                        doc: Pair is an element of a MapList, and consists of a name and an opaque value.
                    doc: Additional headers attached to each HTTP response sent to the client. Header values are opaque since they may be sensitive.
                  tls:
                    type: map
                    children:
                      ca_file:
                        type: string
                        doc: Path to the CA cert. For a client this verifies the server certificate. For a server this verifies client certificates. If empty uses system root CA. (optional)
                      ca_pem:
                        type: string
                        doc: In memory PEM encoded cert. (optional)
                      cert_file:
                        type: string
                        doc: Path to the TLS cert to use for TLS required connections. (optional)
                      cert_pem:
                        type: string
                        doc: In memory PEM encoded TLS cert to use for TLS required connections. (optional)
                      cipher_suites:
                        type: list
                        children:
                          item:
                            type: string
                        doc: CipherSuites is a list of TLS cipher suites that the TLS transport can use. If left blank, a safe default list is used. See https://go.dev/src/crypto/tls/cipher_suites.go for a list of supported cipher suites.
                      client_ca_file:
                        type: string
                        doc: Path to the TLS cert to use by the server to verify a client certificate. (optional) This sets the ClientCAs and ClientAuth to RequireAndVerifyClientCert in the TLSConfig. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional)
                      client_ca_file_reload:
                        type: bool
                        doc: Reload the ClientCAs file when it is modified (optional, default false)
                      curve_preferences:
                        type: list
                        children:
                          item:
                            type: string
                        doc: contains the elliptic curves that will be used in an ECDHE handshake, in preference order Defaults to empty list and "crypto/tls" defaults are used, internally.
                      include_insecure_cipher_suites:
                        type: bool
                      include_system_ca_certs_pool:
                        type: bool
                        doc: If true, load system CA certificates pool in addition to the certificates configured in this struct.
                      key_file:
                        type: string
                        doc: Path to the TLS key to use for TLS required connections. (optional)
                      key_pem:
                        type: string
                        doc: In memory PEM encoded TLS key to use for TLS required connections. (optional)
                      max_version:
                        type: string
                        doc: MaxVersion sets the maximum TLS version that is acceptable. If not set, refer to crypto/tls for defaults. (optional)
                      min_version:
                        type: string
                        doc: MinVersion sets the minimum TLS version that is acceptable. If not set, TLS 1.2 will be used. (optional)
                      reload_interval:
                        type: duration
                        doc: ReloadInterval specifies the duration after which the certificate will be reloaded If not set, it will never be reloaded (optional)
                      tpm:
                        type: map
                        children:
                          auth:
                            type: string
                          enabled:
                            type: bool
                          owner_auth:
                            type: string
                          path:
                            type: string
                            doc: The path to the TPM device or Unix domain socket. For instance /dev/tpm0 or /dev/tpmrm0.
                        doc: Trusted platform module configuration
                    doc: TLS struct exposes TLS client configuration.
                  transport:
                    type: string
                  write_timeout:
                    type: duration
                    doc: WriteTimeout is the maximum duration before timing out writes of the response. It is reset whenever a new request's header is read. Like ReadTimeout, it does not let Handlers make decisions on a per-request basis. A zero or negative value means there will be no timeout.
                doc: ServerConfig defines settings for creating an HTTP server.
            doc: APIServer has the settings to enable the receiver to host the Prometheus API server in agent mode. This allows the user to call the endpoint to get the config, service discovery, and targets for debugging purposes.
          config:
            children:
              global:
                type: map
                children:
                  always_scrape_classic_histograms:
                    type: bool
                    doc: Whether to always scrape classic histograms alongside native histograms.
                  body_size_limit:
                    type: string
                    doc: Maximum size of uncompressed response body. E.g. "100MB". 0 means no limit.
                  convert_classic_histograms_to_nhcb:
                    type: bool
                    doc: Whether to convert classic histograms to native histogram custom buckets.
                  evaluation_interval:
                    type: duration
                    doc: How frequently to evaluate rules. Default is 1m.
                  external_labels:
                    type: map
                    open: true
                    doc: Labels added to all time series and alerts when communicating with external systems.
                  extra_scrape_metrics:
                    type: bool
                    doc: Whether to add extra metrics to each scrape result (scrape_body_size_bytes, etc.).
                  keep_dropped_targets:
                    type: int
                    doc: How many dropped targets to keep for UI purposes. 0 means no limit.
                  label_limit:
                    type: int
                    doc: Per-scrape limit on number of labels per sample. 0 means no limit.
                  label_name_length_limit:
                    type: int
                    doc: Per-scrape limit on label name length. 0 means no limit.
                  label_value_length_limit:
                    type: int
                    doc: Per-scrape limit on label value length. 0 means no limit.
                  metric_name_escaping_scheme:
                    type: string
                    enum:
                      - allow-utf-8
                      - underscore
                      - dots
                      - values
                    doc: Escaping scheme for non-standard metric names.
                  metric_name_validation_scheme:
                    type: string
                    enum:
                      - utf8
                      - legacy
                    doc: Validation scheme for metric names.
                  query_log_file:
                    type: string
                    doc: File to which PromQL queries are logged.
                  rule_query_offset:
                    type: duration
                    doc: Delay rule evaluation relative to the query timestamp.
                  sample_limit:
                    type: int
                    doc: Per-scrape limit on number of accepted samples. 0 means no limit.
                  scrape_failure_log_file:
                    type: string
                    doc: File to which scrape failures are logged.
                  scrape_interval:
                    type: duration
                    doc: How frequently to scrape targets. Default is 1m.
                  scrape_native_histograms:
                    type: bool
                    doc: Whether to scrape native histograms.
                  scrape_protocols:
                    type: list
                    children:
                      item:
                        type: string
                        enum:
                          - PrometheusProto
                          - PrometheusText0.0.4
                          - PrometheusText1.0.0
                          - OpenMetricsText0.0.1
                          - OpenMetricsText1.0.0
                        doc: Content-type negotiation protocol.
                    doc: Accepted content-type negotiation protocols in preference order.
                  scrape_timeout:
                    type: duration
                    doc: Timeout per scrape request. Default is 10s.
                  target_limit:
                    type: int
                    doc: Per-scrape limit on number of targets. 0 means no limit.
                doc: Default values applied to all scrape jobs.
              otlp:
                type: map
                children:
                  convert_histograms_to_nhcb:
                    type: bool
                    doc: Whether to convert OTLP histograms to native histogram custom buckets.
                  ignore_resource_attributes:
                    type: list
                    children:
                      item:
                        type: string
                    doc: Resource attributes to exclude from promotion.
                  keep_identifying_resource_attributes:
                    type: bool
                    doc: Whether to keep identifying resource attributes.
                  label_name_preserve_multiple_underscores:
                    type: bool
                    doc: Whether to preserve multiple consecutive underscores in label names.
                  label_name_underscore_sanitization:
                    type: bool
                    doc: Whether to sanitize label names by replacing non-alphanumeric characters with underscores.
                  promote_all_resource_attributes:
                    type: bool
                    doc: Promote all resource attributes to metric labels.
                  promote_resource_attributes:
                    type: list
                    children:
                      item:
                        type: string
                    doc: Specific resource attributes to promote to metric labels.
                  promote_scope_metadata:
                    type: bool
                    doc: Whether to promote instrumentation scope metadata to metric labels.
                  translation_strategy:
                    type: string
                    doc: Strategy for translating OTLP metrics to Prometheus format.
                doc: Configuration for OTLP metric translation.
              runtime:
                type: map
                children:
                  gogc:
                    type: int
                    doc: Target percentage for Go GC. Default is 75.
                doc: Runtime configuration.
              scrape_configs:
                type: list
                children:
                  item:
                    type: map
                    children:
                      always_scrape_classic_histograms:
                        type: bool
                      authorization:
                        type: map
                        children:
                          credentials:
                            type: string
                            doc: Credentials value (mutually exclusive with credentials_file/credentials_ref).
                          credentials_file:
                            type: string
                            doc: File containing credentials.
                          credentials_ref:
                            type: string
                            doc: Reference to credentials in the credential store.
                          type:
                            type: string
                            doc: Authorization type. Default is "Bearer".
                        doc: HTTP Authorization header credentials.
                      azure_sd_configs:
                        type: list
                        children:
                          item:
                            type: map
                            open: true
                        doc: Azure service discovery configurations.
                      basic_auth:
                        type: map
                        children:
                          password:
                            type: string
                          password_file:
                            type: string
                            doc: File containing the password.
                          password_ref:
                            type: string
                            doc: Reference to a password in the credential store.
                          username:
                            type: string
                          username_file:
                            type: string
                            doc: File containing the username.
                          username_ref:
                            type: string
                            doc: Reference to a username in the credential store.
                        doc: HTTP Basic authentication credentials.
                      bearer_token:
                        type: string
                        doc: Bearer token for authentication (mutually exclusive with bearer_token_file).
                      bearer_token_file:
                        type: string
                        doc: File containing bearer token (mutually exclusive with bearer_token).
                      body_size_limit:
                        type: string
                        doc: Maximum response body size. E.g. "100MB". 0 means no limit.
                      consul_sd_configs:
                        type: list
                        children:
                          item:
                            type: map
                            open: true
                        doc: HashiCorp Consul service discovery configurations.
                      convert_classic_histograms_to_nhcb:
                        type: bool
                      digitalocean_sd_configs:
                        type: list
                        children:
                          item:
                            type: map
                            open: true
                      dns_sd_configs:
                        type: list
                        children:
                          item:
                            type: map
                            open: true
                        doc: DNS SRV record service discovery configurations.
                      ec2_sd_configs:
                        type: list
                        children:
                          item:
                            type: map
                            open: true
                        doc: AWS EC2 service discovery configurations.
                      enable_compression:
                        type: bool
                        doc: Whether to request compressed responses. Default is true.
                      enable_http2:
                        type: bool
                        doc: Whether to use HTTP/2. Default is true.
                      eureka_sd_configs:
                        type: list
                        children:
                          item:
                            type: map
                            open: true
                      extra_scrape_metrics:
                        type: bool
                      fallback_scrape_protocol:
                        type: string
                        enum:
                          - PrometheusProto
                          - PrometheusText0.0.4
                          - PrometheusText1.0.0
                          - OpenMetricsText0.0.1
                          - OpenMetricsText1.0.0
                        doc: Fallback protocol if negotiation fails.
                      file_sd_configs:
                        type: list
                        children:
                          item:
                            type: map
                            open: true
                        doc: File-based service discovery configurations.
                      follow_redirects:
                        type: bool
                        doc: Whether to follow HTTP redirects. Default is true.
                      gce_sd_configs:
                        type: list
                        children:
                          item:
                            type: map
                            open: true
                        doc: Google Compute Engine service discovery configurations.
                      hetzner_sd_configs:
                        type: list
                        children:
                          item:
                            type: map
                            open: true
                      honor_labels:
                        type: bool
                        doc: Whether to honor labels from the scraped target over Prometheus-set labels.
                      honor_timestamps:
                        type: bool
                        doc: Whether to honor timestamps from the scraped target.
                      http_headers:
                        type: map
                        open: true
                        doc: Custom HTTP headers sent during scraping.
                      http_sd_configs:
                        type: list
                        children:
                          item:
                            type: map
                            open: true
                        doc: HTTP endpoint service discovery configurations.
                      ionos_sd_configs:
                        type: list
                        children:
                          item:
                            type: map
                            open: true
                      job_name:
                        type: string
                        doc: Name of the scrape job. Must be unique across all scrape configs.
                      keep_dropped_targets:
                        type: int
                      kubernetes_sd_configs:
                        type: list
                        children:
                          item:
                            type: map
                            open: true
                        doc: Kubernetes service discovery configurations.
                      label_limit:
                        type: int
                      label_name_length_limit:
                        type: int
                      label_value_length_limit:
                        type: int
                      linode_sd_configs:
                        type: list
                        children:
                          item:
                            type: map
                            open: true
                      marathon_sd_configs:
                        type: list
                        children:
                          item:
                            type: map
                            open: true
                      metric_name_escaping_scheme:
                        type: string
                        enum:
                          - allow-utf-8
                          - underscore
                          - dots
                          - values
                      metric_name_validation_scheme:
                        type: string
                        enum:
                          - utf8
                          - legacy
                      metric_relabel_configs:
                        type: list
                        children:
                          item:
                            type: map
                            children:
                              action:
                                type: string
                                enum:
                                  - replace
                                  - lowercase
                                  - uppercase
                                  - keep
                                  - drop
                                  - keepequal
                                  - dropequal
                                  - hashmod
                                  - labelmap
                                  - labeldrop
                                  - labelkeep
                                  - templatereplace
                                doc: Action to perform. Default is "replace".
                              if:
                                type: string
                                doc: PromQL metric selector used as a pre-filter (native histograms only).
                              labels:
                                type: map
                                open: true
                                doc: Labels to set for the labelkeep/labeldrop/labelmap actions.
                              modulus:
                                type: int
                                doc: Modulus for the hashmod action.
                              regex:
                                type: string
                                doc: Regular expression matched against the concatenated source label values. Default is "(.*)".
                              replacement:
                                type: string
                                doc: Replacement value for the target label. Default is "$1".
                              separator:
                                type: string
                                doc: Separator between source label values. Default is ";".
                              source_labels:
                                type: list
                                children:
                                  item:
                                    type: string
                                doc: Labels whose values are concatenated to form the input to the regex.
                              target_label:
                                type: string
                                doc: Label to write the replacement value into.
                            doc: Relabeling rule applied to labels or samples.
                        doc: List of relabel configs applied to scraped samples.
                      metrics_path:
                        type: string
                        doc: HTTP path for scraping. Default is /metrics.
                      moby_sd_configs:
                        type: list
                        children:
                          item:
                            type: map
                            open: true
                        doc: Docker/Moby service discovery configurations.
                      native_histogram_bucket_limit:
                        type: int
                        doc: Maximum number of buckets per native histogram sample.
                      native_histogram_min_bucket_factor:
                        type: float
                        doc: Minimum bucket factor for native histograms.
                      no_proxy:
                        type: string
                        doc: Comma-separated list of hosts to bypass the proxy.
                      nomad_sd_configs:
                        type: list
                        children:
                          item:
                            type: map
                            open: true
                      oauth2:
                        type: map
                        children:
                          audience:
                            type: string
                            doc: JWT audience.
                          claims:
                            type: map
                            open: true
                            doc: Additional JWT claims.
                          client_certificate_key:
                            type: string
                          client_certificate_key_file:
                            type: string
                          client_certificate_key_id:
                            type: string
                          client_certificate_key_ref:
                            type: string
                          client_id:
                            type: string
                          client_secret:
                            type: string
                          client_secret_file:
                            type: string
                          client_secret_ref:
                            type: string
                          endpoint_params:
                            type: map
                            open: true
                            doc: Additional parameters for the token endpoint.
                          grant_type:
                            type: string
                            doc: OAuth2 grant type. Default is "client_credentials".
                          iss:
                            type: string
                            doc: JWT issuer.
                          no_proxy:
                            type: string
                          proxy_connect_header:
                            type: map
                            open: true
                          proxy_from_environment:
                            type: bool
                          proxy_url:
                            type: string
                          scopes:
                            type: list
                            children:
                              item:
                                type: string
                            doc: OAuth2 scopes.
                          signature_algorithm:
                            type: string
                          tls_config:
                            type: map
                            children:
                              ca:
                                type: string
                                doc: CA certificate as a string (mutually exclusive with ca_file/ca_ref).
                              ca_file:
                                type: string
                                doc: Path to the CA certificate file.
                              ca_ref:
                                type: string
                                doc: Reference to a CA certificate in the credential store.
                              cert:
                                type: string
                                doc: Client certificate as a string (mutually exclusive with cert_file/cert_ref).
                              cert_file:
                                type: string
                                doc: Path to the client certificate file.
                              cert_ref:
                                type: string
                                doc: Reference to a client certificate in the credential store.
                              insecure_skip_verify:
                                type: bool
                                doc: Whether to skip TLS certificate verification.
                              key:
                                type: string
                                doc: Client private key as a string (mutually exclusive with key_file/key_ref).
                              key_file:
                                type: string
                                doc: Path to the client private key file.
                              key_ref:
                                type: string
                                doc: Reference to a client private key in the credential store.
                              max_version:
                                type: string
                                doc: Maximum TLS version. E.g. "TLS13".
                              min_version:
                                type: string
                                doc: Minimum TLS version. E.g. "TLS12".
                              server_name:
                                type: string
                                doc: Server name for SNI verification (overrides the hostname in the URL).
                            doc: TLS client configuration.
                          token_url:
                            type: string
                            doc: URL for fetching the access token.
                        doc: OAuth 2.0 authentication configuration.
                      openstack_sd_configs:
                        type: list
                        children:
                          item:
                            type: map
                            open: true
                      outscale_sd_configs:
                        type: list
                        children:
                          item:
                            type: map
                            open: true
                      ovhcloud_sd_configs:
                        type: list
                        children:
                          item:
                            type: map
                            open: true
                      params:
                        type: map
                        open: true
                        doc: HTTP URL query parameters added to every scrape.
                      proxy_connect_header:
                        type: map
                        open: true
                        doc: Headers sent to the proxy during CONNECT requests.
                      proxy_from_environment:
                        type: bool
                        doc: Whether to use proxy settings from environment variables.
                      proxy_url:
                        type: string
                        doc: HTTP proxy URL.
                      puppetdb_sd_configs:
                        type: list
                        children:
                          item:
                            type: map
                            open: true
                      relabel_configs:
                        type: list
                        children:
                          item:
                            type: map
                            children:
                              action:
                                type: string
                                enum:
                                  - replace
                                  - lowercase
                                  - uppercase
                                  - keep
                                  - drop
                                  - keepequal
                                  - dropequal
                                  - hashmod
                                  - labelmap
                                  - labeldrop
                                  - labelkeep
                                  - templatereplace
                                doc: Action to perform. Default is "replace".
                              if:
                                type: string
                                doc: PromQL metric selector used as a pre-filter (native histograms only).
                              labels:
                                type: map
                                open: true
                                doc: Labels to set for the labelkeep/labeldrop/labelmap actions.
                              modulus:
                                type: int
                                doc: Modulus for the hashmod action.
                              regex:
                                type: string
                                doc: Regular expression matched against the concatenated source label values. Default is "(.*)".
                              replacement:
                                type: string
                                doc: Replacement value for the target label. Default is "$1".
                              separator:
                                type: string
                                doc: Separator between source label values. Default is ";".
                              source_labels:
                                type: list
                                children:
                                  item:
                                    type: string
                                doc: Labels whose values are concatenated to form the input to the regex.
                              target_label:
                                type: string
                                doc: Label to write the replacement value into.
                            doc: Relabeling rule applied to labels or samples.
                        doc: List of relabel configs applied to target labels before scraping.
                      sample_limit:
                        type: int
                      scaleway_sd_configs:
                        type: list
                        children:
                          item:
                            type: map
                            open: true
                      scheme:
                        type: string
                        enum:
                          - http
                          - https
                        doc: HTTP scheme for scraping. Default is http.
                      scrape_failure_log_file:
                        type: string
                      scrape_interval:
                        type: duration
                        doc: Override for global scrape_interval.
                      scrape_native_histograms:
                        type: bool
                      scrape_protocols:
                        type: list
                        children:
                          item:
                            type: string
                            enum:
                              - PrometheusProto
                              - PrometheusText0.0.4
                              - PrometheusText1.0.0
                              - OpenMetricsText0.0.1
                              - OpenMetricsText1.0.0
                            doc: Content-type negotiation protocol.
                      scrape_timeout:
                        type: duration
                        doc: Override for global scrape_timeout.
                      stackit_sd_configs:
                        type: list
                        children:
                          item:
                            type: map
                            open: true
                      static_configs:
                        type: list
                        children:
                          item:
                            type: map
                            children:
                              labels:
                                type: map
                                open: true
                                doc: Labels assigned to all metrics scraped from these targets.
                              targets:
                                type: list
                                children:
                                  item:
                                    type: string
                                doc: Targets in host:port format (scheme and path from the job config are used).
                            doc: Statically configured scrape targets.
                        doc: List of statically configured targets.
                      target_limit:
                        type: int
                      tls_config:
                        type: map
                        children:
                          ca:
                            type: string
                            doc: CA certificate as a string (mutually exclusive with ca_file/ca_ref).
                          ca_file:
                            type: string
                            doc: Path to the CA certificate file.
                          ca_ref:
                            type: string
                            doc: Reference to a CA certificate in the credential store.
                          cert:
                            type: string
                            doc: Client certificate as a string (mutually exclusive with cert_file/cert_ref).
                          cert_file:
                            type: string
                            doc: Path to the client certificate file.
                          cert_ref:
                            type: string
                            doc: Reference to a client certificate in the credential store.
                          insecure_skip_verify:
                            type: bool
                            doc: Whether to skip TLS certificate verification.
                          key:
                            type: string
                            doc: Client private key as a string (mutually exclusive with key_file/key_ref).
                          key_file:
                            type: string
                            doc: Path to the client private key file.
                          key_ref:
                            type: string
                            doc: Reference to a client private key in the credential store.
                          max_version:
                            type: string
                            doc: Maximum TLS version. E.g. "TLS13".
                          min_version:
                            type: string
                            doc: Minimum TLS version. E.g. "TLS12".
                          server_name:
                            type: string
                            doc: Server name for SNI verification (overrides the hostname in the URL).
                        doc: TLS client configuration.
                      track_timestamps_staleness:
                        type: bool
                        doc: Whether to track staleness of scraped timestamps.
                      triton_sd_configs:
                        type: list
                        children:
                          item:
                            type: map
                            open: true
                      uyuni_sd_configs:
                        type: list
                        children:
                          item:
                            type: map
                            open: true
                      vultr_sd_configs:
                        type: list
                        children:
                          item:
                            type: map
                            open: true
                      xds_sd_configs:
                        type: list
                        children:
                          item:
                            type: map
                            open: true
                        doc: xDS (Envoy) service discovery configurations.
                      zookeeper_sd_configs:
                        type: list
                        children:
                          item:
                            type: map
                            open: true
                    doc: Configuration for a scrape job.
              storage:
                type: map
                children:
                  exemplars:
                    type: map
                    children:
                      max_exemplars:
                        type: int
                        doc: Maximum number of exemplars stored in memory. 0 disables exemplar storage.
                    doc: Exemplar storage configuration.
                  tsdb:
                    type: map
                    children:
                      out_of_order_time_window:
                        type: duration
                        doc: Time window for accepting out-of-order samples.
                      retention:
                        type: map
                        children:
                          percentage:
                            type: float
                            doc: Maximum storage usage as a percentage of available disk. 0 means no limit.
                          size:
                            type: string
                            doc: Maximum storage size. E.g. "10GB". 0 means no limit.
                          time:
                            type: duration
                            doc: Retention duration. Default is 15d.
                        doc: TSDB data retention configuration.
                      stale_series_compaction_threshold:
                        type: float
                        doc: Fraction of chunks that must be stale to trigger compaction.
                    doc: TSDB storage configuration.
                doc: Storage configuration.
              tracing:
                type: map
                children:
                  client_type:
                    type: string
                    enum:
                      - http
                      - grpc
                    doc: Transport protocol for trace export. Default is grpc.
                  compression:
                    type: string
                    enum:
                      - gzip
                    doc: Compression for trace export.
                  endpoint:
                    type: string
                    doc: OTLP endpoint to send traces to.
                  headers:
                    type: map
                    open: true
                    doc: HTTP headers sent with trace requests.
                  insecure:
                    type: bool
                    doc: Whether to skip TLS for the trace endpoint.
                  sampling_fraction:
                    type: float
                    doc: Fraction of traces to sample. 0 disables tracing. 1 enables full tracing.
                  timeout:
                    type: duration
                    doc: Timeout for trace export requests.
                  tls_config:
                    type: map
                    children:
                      ca:
                        type: string
                        doc: CA certificate as a string (mutually exclusive with ca_file/ca_ref).
                      ca_file:
                        type: string
                        doc: Path to the CA certificate file.
                      ca_ref:
                        type: string
                        doc: Reference to a CA certificate in the credential store.
                      cert:
                        type: string
                        doc: Client certificate as a string (mutually exclusive with cert_file/cert_ref).
                      cert_file:
                        type: string
                        doc: Path to the client certificate file.
                      cert_ref:
                        type: string
                        doc: Reference to a client certificate in the credential store.
                      insecure_skip_verify:
                        type: bool
                        doc: Whether to skip TLS certificate verification.
                      key:
                        type: string
                        doc: Client private key as a string (mutually exclusive with key_file/key_ref).
                      key_file:
                        type: string
                        doc: Path to the client private key file.
                      key_ref:
                        type: string
                        doc: Reference to a client private key in the credential store.
                      max_version:
                        type: string
                        doc: Maximum TLS version. E.g. "TLS13".
                      min_version:
                        type: string
                        doc: Minimum TLS version. E.g. "TLS12".
                      server_name:
                        type: string
                        doc: Server name for SNI verification (overrides the hostname in the URL).
                    doc: TLS client configuration.
                doc: OTLP tracing configuration for Prometheus internal traces.
            doc: PromConfig is a redeclaration of promconfig.Config because we need custom unmarshaling as prometheus "config" uses `yaml` tags.
          discovery_reload_on_startup:
            type: bool
            doc: DiscoveryReloadOnStartup enables discovering targets immediately on start up as opposed to waiting for the configured interval before initializing the scrape pools.
          initial_scrape_offset:
            type: duration
            doc: InitialScrapeOffset adds a fixed delay before the initial scrape of targets. This duration is applied on top of the receiver's internal load balancing offset. It avoids readiness races and backend rate limits immediately after startup.
          scrape_on_shutdown:
            type: bool
            doc: 'ScrapeOnShutdown enables a final scrape before the receiver closes. NOTE: This final scrape ignores the configured scrape interval.'
          target_allocator:
            type: map
            children:
              auth:
                type: map
                children:
                  authenticator:
                    type: map
                    open: true
                    doc: AuthenticatorID specifies the name of the extension to use in order to authenticate the incoming data point.
                doc: Auth configuration for outgoing HTTP calls.
              collector_id:
                type: string
              compression:
                type: string
                doc: The compression key for supported compression types within collector.
              compression_params:
                type: map
                children:
                  level:
                    type: int
                doc: Advanced configuration options for the Compression
              cookies:
                type: map
                open: true
                doc: Cookies configures the cookie management of the HTTP client.
              disable_keep_alives:
                type: bool
                doc: 'DisableKeepAlives, if true, disables HTTP keep-alives and will only use the connection to the server for a single HTTP request. WARNING: enabling this option can result in significant overhead establishing a new HTTP(S) connection for every request. Before enabling this option please consider whether changes to idle connection settings can achieve your goal.'
              endpoint:
                type: string
                doc: 'The target URL to send data to (e.g.: http://some.url:9411/v1/traces).'
              force_attempt_http2:
                type: bool
                doc: 'Enabling ForceAttemptHTTP2 forces the HTTP transport to use the HTTP/2 protocol. By default, this is set to true. NOTE: HTTP/2 does not support settings such as MaxConnsPerHost, MaxIdleConnsPerHost and MaxIdleConns.'
              headers:
                children:
                  item:
                    type: map
                    children:
                      name:
                        type: string
                      value:
                        type: string
                        doc: String alias that is marshaled and printed in an opaque way. To recover the original value, cast it to a string.
                    doc: Pair is an element of a MapList, and consists of a name and an opaque value.
                doc: Additional headers attached to each HTTP request sent by the client. Existing header values are overwritten if collision happens. Header values are opaque since they may be sensitive.
              http_scrape_config: {}
              http_sd_config:
                doc: PromHTTPSDConfig is a redeclaration of promHTTP.SDConfig because we need custom unmarshaling as prometheus "config" uses `yaml` tags.
              http2_ping_timeout:
                type: duration
                doc: HTTP2PingTimeout if there's no response to the ping within the configured value, the connection will be closed. If not set or set to 0, it defaults to 15s.
              http2_read_idle_timeout:
                type: duration
                doc: This is needed in case you run into https://github.com/golang/go/issues/59690 https://github.com/golang/go/issues/36026 HTTP2ReadIdleTimeout if the connection has been idle for the configured value send a ping frame for health check 0s means no health check will be performed.
              idle_conn_timeout:
                type: duration
                doc: IdleConnTimeout is the maximum amount of time a connection will remain open before closing itself. By default, it is set to 90 seconds.
              interval:
                type: duration
              max_conns_per_host:
                type: int
                doc: MaxConnsPerHost limits the total number of connections per host, including connections in the dialing, active, and idle states. Default is 0 (unlimited).
              max_idle_conns:
                type: int
                doc: MaxIdleConns is used to set a limit to the maximum idle HTTP connections the client can keep open. By default, it is set to 100. Zero means no limit.
              max_idle_conns_per_host:
                type: int
                doc: MaxIdleConnsPerHost is used to set a limit to the maximum idle HTTP connections the host can keep open. If zero, [net/http.DefaultMaxIdleConnsPerHost] is used.
              middlewares:
                type: list
                children:
                  item:
                    type: map
                    children:
                      id:
                        type: map
                        open: true
                        doc: ID specifies the name of the extension to use.
                    doc: Middleware defines the extension ID for a middleware component.
                doc: Middlewares are used to add custom functionality to the HTTP client. Middleware handlers are called in the order they appear in this list, with the first middleware becoming the outermost handler.
              proxy_url:
                type: string
                doc: ProxyURL setting for the collector
              read_buffer_size:
                type: int
                doc: ReadBufferSize for HTTP client. See http.Transport.ReadBufferSize. Default is 0.
              timeout:
                type: duration
                doc: Timeout parameter configures `http.Client.Timeout`. Default is 0 (unlimited).
              tls:
                type: map
                children:
                  ca_file:
                    type: string
                    doc: Path to the CA cert. For a client this verifies the server certificate. For a server this verifies client certificates. If empty uses system root CA. (optional)
                  ca_pem:
                    type: string
                    doc: In memory PEM encoded cert. (optional)
                  cert_file:
                    type: string
                    doc: Path to the TLS cert to use for TLS required connections. (optional)
                  cert_pem:
                    type: string
                    doc: In memory PEM encoded TLS cert to use for TLS required connections. (optional)
                  cipher_suites:
                    type: list
                    children:
                      item:
                        type: string
                    doc: CipherSuites is a list of TLS cipher suites that the TLS transport can use. If left blank, a safe default list is used. See https://go.dev/src/crypto/tls/cipher_suites.go for a list of supported cipher suites.
                  curve_preferences:
                    type: list
                    children:
                      item:
                        type: string
                    doc: contains the elliptic curves that will be used in an ECDHE handshake, in preference order Defaults to empty list and "crypto/tls" defaults are used, internally.
                  include_insecure_cipher_suites:
                    type: bool
                  include_system_ca_certs_pool:
                    type: bool
                    doc: If true, load system CA certificates pool in addition to the certificates configured in this struct.
                  insecure:
                    type: bool
                    doc: In gRPC and HTTP when set to true, this is used to disable the client transport security. See https://godoc.org/google.golang.org/grpc#WithInsecure for gRPC. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional, default false)
                  insecure_skip_verify:
                    type: bool
                    doc: InsecureSkipVerify will enable TLS but not verify the certificate.
                  key_file:
                    type: string
                    doc: Path to the TLS key to use for TLS required connections. (optional)
                  key_pem:
                    type: string
                    doc: In memory PEM encoded TLS key to use for TLS required connections. (optional)
                  max_version:
                    type: string
                    doc: MaxVersion sets the maximum TLS version that is acceptable. If not set, refer to crypto/tls for defaults. (optional)
                  min_version:
                    type: string
                    doc: MinVersion sets the minimum TLS version that is acceptable. If not set, TLS 1.2 will be used. (optional)
                  reload_interval:
                    type: duration
                    doc: ReloadInterval specifies the duration after which the certificate will be reloaded If not set, it will never be reloaded (optional)
                  server_name_override:
                    type: string
                    doc: ServerName requested by client for virtual hosting. This sets the ServerName in the TLSConfig. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional)
                  tpm:
                    type: map
                    children:
                      auth:
                        type: string
                      enabled:
                        type: bool
                      owner_auth:
                        type: string
                      path:
                        type: string
                        doc: The path to the TPM device or Unix domain socket. For instance /dev/tpm0 or /dev/tpmrm0.
                    doc: Trusted platform module configuration
                doc: TLS struct exposes TLS client configuration.
              write_buffer_size:
                type: int
                doc: WriteBufferSize for HTTP client. See http.Transport.WriteBufferSize. Default is 0.
            doc: ClientConfig defines settings for creating an HTTP client.
          trim_metric_suffixes:
            type: bool
        doc: Config defines configuration for Prometheus receiver.
    zipkin:
      type: zipkin
      signals:
        - traces
      stability:
        traces: beta
      module: github.com/open-telemetry/opentelemetry-collector-contrib/receiver/zipkinreceiver
      fields:
        type: map
        children:
          auth:
            type: map
            children:
              authenticator:
                type: map
                open: true
                doc: AuthenticatorID specifies the name of the extension to use in order to authenticate the incoming data point.
              request_params:
                type: list
                children:
                  item:
                    type: string
                doc: RequestParameters is a list of parameters that should be extracted from the request and added to the context. When a parameter is found in both the query string and the header, the value from the query string will be used.
            doc: Auth for this receiver
          compression_algorithms:
            type: list
            children:
              item:
                type: string
            doc: 'CompressionAlgorithms configures the list of compression algorithms the server can accept. Default: ["", "gzip", "zstd", "zlib", "snappy", "deflate"]'
          cors:
            type: map
            children:
              allowed_headers:
                type: list
                children:
                  item:
                    type: string
                doc: AllowedHeaders sets what headers will be allowed in CORS requests. The Accept, Accept-Language, Content-Type, and Content-Language headers are implicitly allowed. If no headers are listed, X-Requested-With will also be accepted by default. Include "*" to allow any request header.
              allowed_origins:
                type: list
                children:
                  item:
                    type: string
                doc: AllowedOrigins sets the allowed values of the Origin header for HTTP/JSON requests to an OTLP receiver. An origin may contain a wildcard (*) to replace 0 or more characters (e.g., "http://*.domain.com", or "*" to allow any origin).
              exposed_headers:
                type: list
                children:
                  item:
                    type: string
                doc: ExposedHeaders sets the value of the Access-Control-Expose-Headers response header, indicating which headers are safe to expose to the API of a CORS response.
              max_age:
                type: int
                doc: MaxAge sets the value of the Access-Control-Max-Age response header. Set it to the number of seconds that browsers should cache a CORS preflight response for.
            doc: CORS configures the server for HTTP cross-origin resource sharing (CORS).
          dialer:
            type: map
            children:
              timeout:
                type: duration
          endpoint:
            type: string
            doc: Endpoint configures the listening address for the server.
          idle_timeout:
            type: duration
            doc: IdleTimeout is the maximum amount of time to wait for the next request when keep-alives are enabled. If IdleTimeout is zero, the value of ReadTimeout is used. If both are zero, there is no timeout.
          include_metadata:
            type: bool
            doc: IncludeMetadata propagates the client metadata from the incoming requests to the downstream consumers
          keep_alives_enabled:
            type: bool
            doc: KeepAlivesEnabled controls whether HTTP keep-alives are enabled. By default, keep-alives are always enabled. Only very resource-constrained environments should disable them.
          max_request_body_size:
            type: int
            doc: 'MaxRequestBodySize sets the maximum request body size in bytes. Default: 20MiB.'
          middlewares:
            type: list
            children:
              item:
                type: map
                children:
                  id:
                    type: map
                    open: true
                    doc: ID specifies the name of the extension to use.
                doc: Middleware defines the extension ID for a middleware component.
            doc: Middlewares are used to add custom functionality to the HTTP server. Middleware handlers are called in the order they appear in this list, with the first middleware becoming the outermost handler.
          parse_string_tags:
            type: bool
            doc: If enabled the zipkin receiver will attempt to parse string tags/binary annotations into int/bool/float. Disabled by default
          read_header_timeout:
            type: duration
            doc: ReadHeaderTimeout is the amount of time allowed to read request headers. The connection's read deadline is reset after reading the headers and the Handler can decide what is considered too slow for the body. If ReadHeaderTimeout is zero, the value of ReadTimeout is used. If both are zero, there is no timeout.
          read_timeout:
            type: duration
            doc: ReadTimeout is the maximum duration for reading the entire request, including the body. A zero or negative value means there will be no timeout. Because ReadTimeout does not let Handlers make per-request decisions on each request body's acceptable deadline or upload rate, most users will prefer to use ReadHeaderTimeout. It is valid to use them both.
          response_headers:
            children:
              item:
                type: map
                children:
                  name:
                    type: string
                  value:
                    type: string
                    doc: String alias that is marshaled and printed in an opaque way. To recover the original value, cast it to a string.
                doc: Pair is an element of a MapList, and consists of a name and an opaque value.
            doc: Additional headers attached to each HTTP response sent to the client. Header values are opaque since they may be sensitive.
          tls:
            type: map
            children:
              ca_file:
                type: string
                doc: Path to the CA cert. For a client this verifies the server certificate. For a server this verifies client certificates. If empty uses system root CA. (optional)
              ca_pem:
                type: string
                doc: In memory PEM encoded cert. (optional)
              cert_file:
                type: string
                doc: Path to the TLS cert to use for TLS required connections. (optional)
              cert_pem:
                type: string
                doc: In memory PEM encoded TLS cert to use for TLS required connections. (optional)
              cipher_suites:
                type: list
                children:
                  item:
                    type: string
                doc: CipherSuites is a list of TLS cipher suites that the TLS transport can use. If left blank, a safe default list is used. See https://go.dev/src/crypto/tls/cipher_suites.go for a list of supported cipher suites.
              client_ca_file:
                type: string
                doc: Path to the TLS cert to use by the server to verify a client certificate. (optional) This sets the ClientCAs and ClientAuth to RequireAndVerifyClientCert in the TLSConfig. Please refer to https://godoc.org/crypto/tls#Config for more information. (optional)
              client_ca_file_reload:
                type: bool
                doc: Reload the ClientCAs file when it is modified (optional, default false)
              curve_preferences:
                type: list
                children:
                  item:
                    type: string
                doc: contains the elliptic curves that will be used in an ECDHE handshake, in preference order Defaults to empty list and "crypto/tls" defaults are used, internally.
              include_insecure_cipher_suites:
                type: bool
              include_system_ca_certs_pool:
                type: bool
                doc: If true, load system CA certificates pool in addition to the certificates configured in this struct.
              key_file:
                type: string
                doc: Path to the TLS key to use for TLS required connections. (optional)
              key_pem:
                type: string
                doc: In memory PEM encoded TLS key to use for TLS required connections. (optional)
              max_version:
                type: string
                doc: MaxVersion sets the maximum TLS version that is acceptable. If not set, refer to crypto/tls for defaults. (optional)
              min_version:
                type: string
                doc: MinVersion sets the minimum TLS version that is acceptable. If not set, TLS 1.2 will be used. (optional)
              reload_interval:
                type: duration
                doc: ReloadInterval specifies the duration after which the certificate will be reloaded If not set, it will never be reloaded (optional)
              tpm:
                type: map
                children:
                  auth:
                    type: string
                  enabled:
                    type: bool
                  owner_auth:
                    type: string
                  path:
                    type: string
                    doc: The path to the TPM device or Unix domain socket. For instance /dev/tpm0 or /dev/tpmrm0.
                doc: Trusted platform module configuration
            doc: TLS struct exposes TLS client configuration.
          transport:
            type: string
          write_timeout:
            type: duration
            doc: WriteTimeout is the maximum duration before timing out writes of the response. It is reset whenever a new request's header is read. Like ReadTimeout, it does not let Handlers make decisions on a per-request basis. A zero or negative value means there will be no timeout.
        doc: Config defines configuration for Zipkin receiver.
